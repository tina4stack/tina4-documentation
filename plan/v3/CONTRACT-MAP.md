# Tina4 v3 - Contract and Spec Map

> The living index that ties every audited feature to its machine-checked
> contract, its decisions, and its proven-in-all-four status. This is the
> backbone of a future formal Tina4 language specification.
> **Last synced:** 2026-08-13 (3.13.99 pass:  fixtures)

> **Adversarial re-audit started 2026-08-08:** a zero-skip live-lab migration
> baseline still omitted contradictory public paths (generated code migration
> not discoverable; failed rollback erasing history). Every row previously
> labelled closed is being re-checked under `99-feature-reaudit.md`. Existing
> proven counts remain true for the invariants they name; they are not treated
> as proof that the fixture is complete.
>
> **Portability target, 2026-08-08:** this map must converge on the clean-room
> implementation formula in [PORTING-FORMULA.md](PORTING-FORMULA.md). A feature
> is not fully specified until another language can implement it from the
> language-neutral contract packet without reading an existing framework.
> That packet is also the parity oracle run back against every current language;
> the flow is audit -> neutral contract -> all implementations, never permanent
> promotion of one language as the master.
>
> **3.14.0 stability boundary:** breaking changes are permitted before 3.14.0
> to establish the correct, simple parity contract. They require conformance
> proof and migration notes, not automatic compatibility aliases. The resulting
> audited contracts become the stable baseline at 3.14.0.

## Why this map exists

Tina4 is one framework in four languages. A "spec" that lives only as prose
drifts the moment one implementation changes. So the real specification is being
built as **machine-checked contracts**: a shared JSON fixture per subsystem,
whose invariants are proven by a named test in every framework and gated by one
checker (`scripts/audit-contract-fixtures.py`, ADR-0024 rule 6). A contract that
is green is a behaviour all four frameworks are held to.

This file is the map from features to those contracts. It answers one question
per feature: **is its behaviour specified, decided, and proven in all four - or
still owed?** As each feature is audited it moves down the pipeline below, and
its row here is updated. Keeping this map current is how the audit work
accumulates into a language spec instead of scattering.

## The spec, in three layers

1. **[MASTER-SPEC.md](MASTER-SPEC.md)** - the prose API contract per feature
   (last full pass 2026-03-21). Readable, but it predates the contract fixtures,
   so where a fixture exists **the fixture is authoritative for behaviour** and
   MASTER-SPEC is the narrative around it. A future formal spec is produced by
   folding the fixtures + ADRs back into this document.
2. **Contract fixtures** (`fixtures/*_contract.json`) - the machine-checked,
   four-way-proven behavioural invariants. The authoritative layer.
3. **ADRs** (`decisions/ADR-*.md`) - the decisions behind the contracts (41
   allocated). An invariant cites the ADR that settled it.

## The pipeline (how a feature reaches the spec)

A feature is "specified" only when it has walked all seven steps. This mirrors the
audit method in [98-feature-audit.md](98-feature-audit.md).

1. **Audit** - measure LOC/CC/MI four-way, read all four, pick the best mechanism.
2. **Plan** - park it as `features/NNN-<name>.md` (133 exist).
3. **Decide** - any behaviour fork gets an `ADR-NNNN.md`.
4. **Fixture** - write `fixtures/<name>_contract.json` with the invariants + the
   conformance cases as data.
5. **Prove** - a named suite carrying those case names in ALL FOUR; the auditor
   green; the full suites green on the lab.
6. **Fold** - reflect the proven contract into MASTER-SPEC.
7. **Package for a new language** - complete the ten-part porting capsule in
   `PORTING-FORMULA.md` and prove that it contains no "copy runtime X" gaps.

Each public capability and selectable provider has one whole-number packet in
`features/`, based on `features/FEATURE-TEMPLATE.md`. The 3.14 catalog has no
decimal provider members. Combined historical plans remain archive evidence.

## Layer 2: machine-checked contract ledger

Source of truth for the counts: `python3 scripts/audit-contract-fixtures.py`.
Re-run it and re-sync this table whenever a fixture changes.

| Subsystem | Feature # | Fixture | Invariants | Proven | Owed | ADRs | Named suites (all 4) |
|---|---|---|---:|---:|---:|---|---|
| Structured logger | 2 | `logger_contract.json` | 8 | 8 | 0 | 0041 | yes (real per-language runners, no mocks: real files/temp dirs, a real pty for ANSI-on-tty, real worker_threads/Atomics lock contention, real concurrent child processes for the file-integrity witness, a real forked/spawned child for state-discard; 59 shared cases + 1 extra per language proving Decision 8's independent TINA4_LOG_FILE_LEVEL directly (fixture predates the 2026-08-10 override). Obeys Decision 8 (separate console/file levels, sink-aware is_enabled) and Decision 20 (single-file in-process lock only; PHP's flock exceeds the floor via a real OS lock, Node's real child-process witness exceeds it too). Framework fixes found by wiring the shared fixture, all mutation-proved: Python's and PHP's bootstraps invented explicit defaults (e.g. `configure(level=..., production=...)`) instead of a plain lazy `configure()` (Ruby's bootstrap was already correct); Python/PHP/Ruby request dispatch was each missing `clear_request_id`/`clearRequestId` in a `finally` (a leaked id could bleed into the next request) -- Node needed neither fix, its bootstrap never calls `configure()` at all (env read lazily on first use) and `Log.runWithRequestId` already scopes the id via AsyncLocalStorage, discarding it structurally when the call returns. PHP's context circular-detection was unsound (`spl_object_id` on a cast copy, which has no stable identity) and rebuilt reference-aware (one level deeper than Python/Ruby/Node, documented and accepted, since PHP arrays are copy-on-write values, not true references); Node's logger re-read `TINA4_LOG_LEVEL`/`TINA4_LOG_FORMAT` fresh on every call instead of a stable resolve-once snapshot (LOG-C05/C06), had no independent `TINA4_LOG_FILE_LEVEL` (Decision 8), and exposed the prohibited `warn` alias -- removed, with every real call site across the framework (server.ts, websocket.ts, websocketBackplane.ts, env.ts, baseModel.ts) converted to `warning`. Each language's rewrite implements its own honest adaptation of Decision 20's single-file lock floor: Ruby a real `Mutex`, PHP `flock` (exceeds the floor -- also serializes real OS processes), Node a `SharedArrayBuffer`+`Atomics` lock (proven for real via a worker_threads holder), Python `threading.Lock`. 2026-08-13) |
| Database adapter | 3 | `adapter_contract.json` | 8 | 8 | 0 | 0044 | yes (real per-language runners against real SQLite -- Python `tina4_python.database`, PHP `Tina4\Database\SQLite3Adapter`, Ruby `Tina4::Drivers::SqliteDriver`, Node `node:sqlite` -- plus real PostgreSQL/MySQL/MSSQL/Firebird for adapter-provider-substitutability where the lab provisions them; NO mocks; the granular STRUCTURAL contract only -- the adapter's real write-path behaviour (RETURNING last-id, affected counts, filterless-write guard, executeMany atomicity) was already proven four-way via the five provider fixtures (pg/mysql/mssql/firebird/odbc, feature 3 context) and is not re-proven here. Call-count witnesses use a REAL adapter subclass that counts calls to each contract method while every call still hits a real database ("instrumented_real_adapter", the fixture's own name for the pattern) -- not a mock. Framework fixes, all mutation-proved: DBA-S03 (adapter boundary excludes engine-neutral composition) found insert/update/delete still defined directly on Python's `DatabaseAdapter` base class -- extracted to a new `SqlCrudMixin` every built-in SQL adapter mixes in separately, so the six SQL adapters still have fully working insert/update/delete while the DECLARED `DatabaseAdapter` class itself no longer does (MongoDB, which never used the shared CRUD path, is unaffected); PHP's `DatabaseAdapter` interface still declared `query`/`insert`/`update`/`delete`/`lastInsertId`/`error` as required and was missing `connect`/`getDatabaseType`/`autocommit` entirely -- interface corrected, `ConnectAliasTrait` gives every adapter a `connect()` forwarding to the existing `open()`, ALL SEVEN PHP classes implementing `DatabaseAdapter` needed the fix (`CachedDatabase` and `Database` itself both `implements DatabaseAdapter` too -- both were missing the three new required methods, which would have been a hard PHP fatal at class-load time for every request); Ruby's `DatabaseAdapter::CONTRACT` was PURE FICTION -- it declared `open`/`get_tables`/`table_exists` while every one of the seven real drivers actually implements `connect`/`tables`/`table_exists?`, declared `fetch`/`autocommit` as required with NO driver anywhere implementing either, and required `last_insert_id`/`error` (ADR-0044 says these leave the adapter) while omitting `execute_many`/`fetch_one` (ADR-0044 says these are now required) -- corrected to the real fourteen, with a working generic default on the module for `execute_many`/`fetch_one`/`fetch`/`table_exists?`/`autocommit`/`supports_atomic_batch` (SQLite/Postgres already had native primitives under other names and keep them); `get_database_type` existed on ZERO of Ruby's seven drivers -- added to all seven (would have made EVERY `Database.new` for mysql/mssql/firebird/odbc/mongodb raise at construction the moment the new registration-time `DatabaseAdapter.validate!` check landed, since nothing overrode the raising stub); Node's `types.ts` interface was missing `getDatabaseType`/`autocommit` entirely and ALL SIX network-native adapter classes (Postgres/MySQL/MSSQL/Firebird/Mongo/ODBC) were missing both -- added, along with `CachedDatabaseAdapter` and `Database` itself (both implement `DatabaseAdapter`), caught immediately by `npm run typecheck`. `executeMany` structural fixes, per language: Python's SQLite adapter opened a transaction even for an EMPTY batch (DBA-B01) and did not reject a ragged/short parameter set before writing (DBA-B05, SQLite silently bound the missing position NULL) -- both fixed, plus `execute_many`'s own BEGIN/COMMIT/ROLLBACK now goes through `start_transaction`/`commit`/`rollback` (not a raw connection call) so transaction ownership is actually observable; PHP's SQLite3 adapter had the SAME ragged-parameter gap (`SQLite3Stmt` silently binds a missing trailing position NULL rather than erroring) -- fixed with an explicit `paramCount()` check; PHP's `Database::executeMany()` was FACADE-composed (built its own transaction, looped `$this->execute()` per row/chunk) even though `SQLite3Adapter::executeMany()` already existed as a real, unused primitive -- rewritten to delegate to the adapter's `executeMany()` exactly once, wrapped in the SAME `startTransaction()`/`commit()`/`rollback()` bracketing (their existing nested-transaction depth-counter already handled the join-vs-own decision correctly); Ruby's `Database#execute_many` was ALSO fully facade-composed with its own thread-pin transaction handling -- rewritten the same way, delegating to the driver's new `execute_many` default; Node's `Database.executeMany()` was the worst of the four -- it looped `adapterExecute()` per chunk/row AND returned ONE RESULT PER ROW (`unknown[]`, callers indexed into it) instead of Python/PHP/Ruby's count-or-DatabaseResult -- BREAKING CHANGE (pre-3.14.0, ADR-0044 names this exact gap): rewritten to delegate to the adapter's own `executeMany`/`executeManyAsync` exactly once via a new `adapterExecuteMany()` helper and return the SAME shared `{success, affectedRows, lastId}` shape `insert`/`update`/`delete` already use; a caller indexing into the old per-row array must switch to reading the aggregate result (one pre-existing regression spec, `executeManyFacadeTxn.test.ts`, asserted the old shape and was updated, not deleted -- its atomicity coverage is unchanged and re-proven against the new code path). Two more REAL bugs the new wiring surfaced while proving the SAME cases against real PostgreSQL: Node's `CachedDatabaseAdapter` (the query-cache wrapper EVERY connection goes through, cache on or off) had `executeAsync`/`insertAsync`/`updateAsync`/`deleteAsync` passthroughs to the wrapped adapter but no `executeManyAsync` twin, so the new `adapterExecuteMany()` helper's async-preference check found nothing and fell through to the wrapped adapter's SYNCHRONOUS `executeMany()` -- the throwing "Use executeManyAsync()" stub on every async-native adapter, turning EVERY standalone `db.executeMany()` call against Postgres/MySQL/MSSQL/Firebird/Mongo into a hard throw (caught by the pre-existing `executeManyFacadeTxn.test.ts`, not a new test -- exactly what running the existing suite after a structural change is for); Node's `SQLiteAdapter.executeMany()` unconditionally issued a raw `BEGIN TRANSACTION` with no `_inTransaction` guard (unlike its own `startTransaction()`), so once the facade started bracketing its own transaction around the ONE delegated call, a standalone batch hit "cannot start a transaction within a transaction" -- fixed to own the transaction only when not already inside one, via the SAME guarded `startTransaction()`/`commit()`/`rollback()` its facade counterpart uses. Lifecycle fixes: Python's `SQLiteAdapter.connect()` was NOT idempotent (a second call unconditionally opened and leaked a fresh native connection) -- fixed; Node's `SQLiteAdapter.close()` was not idempotent either (`node:sqlite`'s `DatabaseSync.close()` throws on an already-closed database) -- fixed with a closed flag. `getColumns`/`primaryKeyPosition` (Feature 5 Decision 7 amendment, folded into ADR-0044): added to SQLite in all four languages (Python/PHP/Ruby/Node) with `primary_key()`/`primaryKey()` sorting by it so a composite `PRIMARY KEY (b, a)` returns `(b, a)`, not table-column order -- proven for real with a composite-key SQLite table in every language; NOT extended to PostgreSQL/MySQL/MSSQL/Firebird/ODBC in this pass (their `get_columns`/`getColumns` still report `primary_key`/`primaryKey` as a boolean only) -- flagged, not silently regressed, since it is additive follow-up work outside the 40 named cases. `unsupported_atomic_batch` (DBA-P02, ADR-0044): a NEW `supports_atomic_batch`/`supportsAtomicBatch` capability (default true) was added to every language's adapter base/module/interface, checked once before any row of a >1-row batch is written -- proven with a real, fully-functional SQLite-backed adapter whose flag is set false in-test (the same "real minimal test adapter" pattern the fixture itself uses for DBA-S02), not against a real standalone MongoDB (the lab's `TINA4_TEST_MONGO_URI`/ODBC DSN are not wired for the `Database`-facade-routable adapter path this fixture exercises -- MongoDB's own first-class-SQL-adapter status remains a separate, explicitly OWED follow-up per the feature doc's three sub-points, not attempted here). NOT attempted (reported, not silently skipped): Node's pre-existing sync/async method-name split on the five network-native adapters (`execute()`/`executeAsync()` etc., each with a throwing "Use xAsync" sync stub) -- touches the hottest read/write path across five adapter files plus the dozens of existing call sites already built around dispatching to the `*Async` twin when present, assessed too large/risky for this pass; DBA-S04 (node contract has one usable async surface) is instead proven for the property that matters in practice -- the PUBLIC facade never reaches a throwing sync stub, for a genuinely sync engine (SQLite, where no split exists at all) and, through the same `*Async`-preferring dispatch helpers, for a genuinely async one. adapter-provider-substitutability exercised real SQLite always, plus real PostgreSQL/MySQL/MSSQL/Firebird wherever the lab's `TINA4_TEST_*` env made them reachable (never a bare skip -- `TINA4_REQUIRE_SERVICES` upgrades a missing configured service to a hard failure in all four languages' existing harnesses). Auditor: 268 invariants, 260 -> 268 proven, 8 -> 0 owed.) |
| Router + dispatch | 31 | `dispatch_contract.json` | 8 | 8 | 0 | 0010-0013 | yes |
| Route groups | 32 | `route_groups_contract.json` | 6 | 6 | 0 | RG-DEC-01, RG-DEC-02, RG-04 | yes (real TestClient dispatch python/php/ruby, real `startServer`+socket node, no mocks; +1 invariant 2026-08-13 (real-bug pre-merge audit): a route path containing a literal regex metacharacter matches ONLY that literal path - CONFIRMED broken in PHP and Node.js (a literal segment was interpolated into the compiled regex unescaped, so `/blocked-xss(1)` compiled `(1)` as a capture group and the exact registered path 404'd); fixed by quoting every literal segment (`preg_quote`/JS metachar-escape) before compilation, leaving only `{param}`/`{param:type}` as a capture group; Python (`re.escape`)/Ruby (`Regexp.escape`) were already correct - code-wins, re-proven empirically. Mutation-proved in PHP and Node: reverting the escape reproduces the exact 404 plus an over-loose match (a literal `.` behaving as a regex wildcard, and the paren route wrongly ALSO matching its de-parenthesised form); restored. RG-DEC-01 converged Python/Ruby/Node onto PHP's prefix+path join grammar [`Tina4/Router.php` `addRoute` -- the reference: one separator, single leading slash, no trailing slash, any run of slashes collapsed to one] via ONE ported helper per language [`_join_group_path`/`join_group_path`/`joinGroupPath`] -- PHP needed no code change, only a new suite proving it; before the fix, `group("/api")` + `get("users")` silently mis-registered at `/apiusers` in Python (`path = cls._group_prefix + path`) and Ruby (`"#{@prefix}#{path}"`), and Node did NO normalization at all [the worst of the four -- its group prefix wasn't even trailing-slash-trimmed on construction, so it alone also reds `trailing_slash_on_the_prefix_is_normalized` under mutation]; RG-DEC-02 ledger = DOCUMENT ONLY [OWNER-DECISIONS.md Batch 4 ratifies just the slash-normalization convergence for feature 32 -- Ruby's group-level `auth_handler:` stays a Ruby-only convenience, no new group-auth surface added to Python/PHP/Node, not a security gap since writes are secure-by-default everywhere regardless of group middleware]; RG-04 [group middleware never opens the write-auth gate, locked by feature 79] re-proven not reopened. STALE-corrected, not re-flagged: RG-03 [Ruby group `auth_handler` never dispatched] and RG-06 [group suite Python-only] were already fixed before this feature. Mutation-proved Python/Ruby/Node: reverting each fix reds `group_prefix_joins_to_a_single_slash_path` + `nested_groups_concatenate_the_prefix` [Node also reds `trailing_slash_on_the_prefix_is_normalized`]; restored. LOC: PHP +0 [already correct], Python +21, Ruby +18, Node +20 -- one small ported helper per language, zero new deps. 2026-08-13) |
| CSRF protection | 37 | `csrf_contract.json` | 11 | 11 | 0 | SEC-01, CSRF-DEC-01/02 | yes (real HS256 pipeline, no mocks; 2026-08-11) |
| Health check | 38 | `health_contract.json` | 5 | 5 | 0 | 0016 | yes |
| JWT + session | 64-65 | `session_contract.json` | 7 | 7 | 0 | 0021, 0024 | yes (5 carry a witness rule; +1 invariant 2026-08-13 (real-bug pre-merge audit): the built-in server (`tina4 serve`) emits a first-time session Set-Cookie and a replay resumes it - CONFIRMED broken in PHP's Tina4\Server (the raw-socket engine): it never triggers PHP's `headers_sent()`, so `Router::emitSessionCookie()` took the native `setcookie()` branch, writing into a void nothing reads back - a first-time login under `tina4 serve` emitted NO Set-Cookie at all, silently breaking session auth on the framework's own recommended server (feature 131 found and deliberately deferred this exact gap). Fixed with a narrowly-scoped `Response::rawSocket` flag `Tina4\Server` sets (kept independent of `isTesting()`, which also changes SSE stream materialisation); `emitSessionCookie()` reads `headers_sent() || isTesting() || isRawSocket()`; `App::__invoke()` (Apache/nginx/FPM/`php -S`) never sets it, so that path is unchanged - verified with real sockets on all three PHP contexts (raw-socket Server, `php -S`, and `App::handle()`'s header-emitting tail, proving no double Set-Cookie). Python's core.server, Ruby's Tina4::WebServer (WEBrick) and Node's node:http server have no such CGI-heritage SAPI split - code-wins, re-proven empirically against a real spawned/real socket server. Mutation-proved in PHP: reverting the flag reproduces 200 OK with zero Set-Cookie lines; restored.) |
| Cache backends | 72 | `cache_contract.json` | 8 | 8 | 0 | 0020, 0024 | yes |
| Queue backends | 89 | `queue_contract.json` | 7 | 7 | 0 | 0022-0024 | yes |
| Swagger / OpenAPI | 45 | `swagger_contract.json` | 13 | 13 | 0 | 0004, 0041 | yes (added 2026-08-07; +3 2026-08-13: node boot-snapshot + shared /__feedback exclusion rule + gated 401/summary/tags/description shape, SWAG-DEC-01/02) |
| DocStore | 95 | `docstore_contract.json` | 9 | 9 | 0 | 0024, 0025, 0035, 0036 | yes |
| tina4-css | 62 | `tina4css_contract.json` | 1 | 1 | 0 | 0004 | yes |
| Messenger | 88 | `messenger_contract.json` | 14 | 14 | 0 | 0004, 0041, 0042 | yes (real GreenMail) |
| Paginated results | 24 | `pagination_contract.json` | 8 | 8 | 0 | 0043, PAGE-DEC-01 | yes (real 250-row SQLite; incl. the AutoCrud REST endpoint; PAGE-DEC-01 2026-08-12: page<1 clamped to page 1 in Python/Ruby/Node - PHP already correct, verified not fixed - so offset=(page-1)*per_page is never negative (was a real PostgreSQL :55432 "OFFSET must not be negative" driver error, and a silent page:0 envelope on SQLite); an oversized ?limit=/?per_page= is capped at 100 (MAX_PER_PAGE in Python/PHP/Ruby, Node's own DEFAULT_ROW_CAP reused) in all four - re-measured while implementing, the cap gap was NOT Node-only as first suspected; Node also dropped its duplicate envelope-side limit/offset/page recomputation (buildQuery now returns the exact clamped values it used); real SQLite + real PostgreSQL :55432 tina4/tina4, mutation-proved: revert the clamp -> the postgres case reproduces the real negative-offset driver error; revert the cap -> the oversized-limit case echoes the requested value) |
| Dev admin dashboard | 127 | `devadmin_contract.json` | 6 | 6 | 0 | DEVADMIN-DEC-01..06 | yes (real-dispatch, no mocks; 2026-08-11) |
| Static assets | 41 | `static_contract.json` | 5 | 5 | 0 | 0050, ST-DEC-01 | yes (real symlinks + temp dirs; 2026-08-11) |
| HTTP compression + ETag | 40 | `compression_etag_contract.json` | 6 | 6 | 0 | CE-DEC-01, CE-DEC-02 | yes (real server + real HTTP + real gzip decode, no mocks; CE-DEC-01 PARITY -- ported Python's gzip (>1024 bytes, Accept-Encoding gzip, compressible content-type, level 6) + strong dynamic ETag + conditional-GET to PHP `gzencode`/Ruby `Zlib`/Node `node:zlib gzipSync` (zero new deps, stdlib only -- PHP/Ruby/Node previously had no compression primitive at all); CE-DEC-02 pinned ONE weak static ETag `W/"<size>-<mtime>"` (decimal, integer-second mtime) across all four (was 4-way divergent: Python strong md5, PHP mtime-size decimal, Ruby mtime-size hex, Node size-mtimeMs reversed+fractional), fixed Python's 304 to preserve ETag/Last-Modified (CE-PY-304-DROPS-VALIDATORS -- PHP/Ruby/Node's static 304 already preserved them), and unified Python's If-None-Match onto the RFC-7232 weak/list/wildcard semantics PHP/Ruby/Node already had (CE-INM-SEMANTICS, reused not reimplemented via a shared matcher per language). Mutation-proved per language: disabling the gzip branch reds the compression case; reinstating Python's `headers: []` 304 (and the Node/PHP/Ruby equivalent validator-drop) reds the 304-preservation case; both restored. Static-file ETag pinned by requesting a real file with a FIXED mtime and asserting the byte-exact header. Node's dynamic path required a new PROLOGUE stage (`compressionEtagIntercept`, monkey-patching `res.write`/`res.end` since Node has no deferred response object) -- fixed unrelated pre-existing drift in Node's `dispatchPipeline.test.ts` lock-in test found while wiring it in (a stale search target meant it verified nothing). 2026-08-12) |
| Request id / correlation | 43 | `requestid_contract.json` | 4 | 4 | 0 | RID-DEC-01/02 | yes (real pipeline + real HTTP; real log file; 2026-08-11) |
| Frond tag path confinement | 53 | `frondtags_contract.json` | 4 | 4 | 0 | TAG-DEC-01 | yes (real templates + real symlink + real dirs; one shared loader per lang; 2026-08-11) |
| MongoDB SQL provider | 14 | `mongosql_contract.json` | 4 | 4 | 0 | MONGO-DEC-01 | yes (real MongoDB; fail-closed parse + one shared filterless-write guard per lang; +14b truncate() 1=1 tautology -> match-all `{}` so truncate() actually EMPTIES the collection all four (Python/Ruby/Node parsed `1 = 1` to the field filter `{"1":1}` = matched nothing = silent no-op; PHP already special-cased it), the match is TIGHT so `id = 1` stays scoped, and Node carries an explicit match-all signal past its extra parsed-filter guard while a blank/absent WHERE is still rejected; mutation-proved; 2026-08-12) |
| Security headers | 36 | `securityheaders_contract.json` | 2 | 2 | 0 | SECHDR-DEC-01, SECHDR-DEC-02 | yes (real pipeline: py `server.handle`, php `Router::dispatch`, ruby `RackApp#call`, node real HTTP; secure-by-default register + HTTPS-guarded HSTS + CSP `default-src 'self'`; PHP class renamed; mutation-proved; 2026-08-11) |
| Port takeover | 129 | `porttakeover_contract.json` | 2 | 2 | 0 | TAKEOVER-DEC-01, TAKEOVER-DEC-02, TAKEOVER-DEC-03 | yes (real processes on real ports, no mocks; ONE shared identity-checked helper per lang reused by the CLI + runtime paths; PID-file Tina4 identity + dev-gate + `TINA4_NO_TAKEOVER`/`--no-kill` opt-out; runtime path raises on a foreign holder; mutation-proved; 2026-08-11) |
| Dual test port | 128 | `dual_port_contract.json` | 4 | 4 | 0 | DUALPORT-DEC-01, DUALPORT-DEC-02 | yes (real server boot on a real ephemeral port pair, no mocks -- python subprocess.Popen, php proc_open, ruby spawn via Tina4::WebServer/Tina4.run!, node real startServer over a real socket; DUALPORT-DEC-01 ports Node's aiPortRange.test.ts real-sockets shape into NEW suites in all three other languages (Node's own file is untouched, a separate narrower regression) -- base+1000 serves the same app, refuses the reload WS upgrade (never 101; Ruby's WEBrick has no rack.hijack at all so every upgrade answers 426 on both ports, proven instead via a plain-GET AI-port-specific 404 marker), TINA4_NO_AI_PORT leaves only the base port, and a pre-bound busy AI port warns + skips non-fatally; found + fixed a REAL crash while writing the PHP busy-port case (DUALPORT-PHP-CRASH, uncatalogued: `Tina4\Server::start()` assigned `stream_socket_server()`'s FALSE return straight onto `$this->aiSocket`, and the accept loop's `!== null` guard let that false reach `stream_select()` as an invalid resource, crashing the WHOLE process instead of skipping -- fixed with an explicit `$this->aiSocket = null;` reset on every failure branch instead of leaving FALSE sitting in the property, separately mutation-proved by reverting to the un-reset assignment); DUALPORT-DEC-02 (low, per-repo regressions, not part of this shared fixture) single-sources Ruby's CLI `resolve_config(:port)` onto `TINA4_PORT` > `PORT` (was bare-PORT-only, so main/AI/`Tina4.mcp_port` supervisor-proxy ports derived from different bases under `tina4ruby serve`) and aligns PHP's `Server::__construct` default 7146 -> 7145 (bin/tina4php's documented default; two PRE-EXISTING PHP tests had been locking in the wrong value and now assert 7145); mutation-proved in all four; 2026-08-13) |
| Dynamic framework version | 130 | `version_contract.json` | 4 | 4 | 0 | VERSION-DEC-01, VERSION-DEC-02, VERSION-DEC-03 | yes (real server boot + real HTTP + real CLI subprocess, no mocks -- python child process over conftest.boot_child_server, php proc_open of the SAME dual_port_server.php fixture app DualPortContractTest uses, ruby spawn via ShutdownProbe (Tina4::WebServer), node real in-process startServer(); every language: health GET, dashboard GET, a real JSON-RPC `initialize` POST to the mounted MCP endpoint, and a real CLI subprocess (`commands --json`) all read back and compared. VERSION-DEC-01 (single-resolver convergence): PHP had THREE independent version sources that drifted in a plain checkout -- `App::$VERSION` (correct), the CLI's `tina4FrameworkVersion()` (read composer.json's absent `version` key, then `vendor/composer/installed.json`'s absent self-entry, floor `0.0.0`), and the MCP `serverInfo` (constructor default `1.0.0`, never overridden). Fixed: `tina4FrameworkVersion()` now reads `\Tina4\App::$VERSION` directly (an autoloaded class DEFINITION only, no `App::start()` side effects -- still boot-free; the stale docblock claiming it mirrors a deleted `App::resolveVersion()` is deleted), and `Tina4\Bootstrap\MCP.php`'s `getDefaultServer()` passes `App::$VERSION` as the third constructor arg. Node had ONE latent split: `server.ts`'s `readPackageVersion()` was a FIXED `../../../package.json` (three levels up) with no fallback -- correct in the monorepo tree, silently `0.0.0` the moment `@tina4/core` is relocated (a published install, a pnpm store symlink); `devAdmin.ts` had its own two-fixed-path reader with the same floor; `mcp.ts`'s default dev server had the same unwired `1.0.0` gap as PHP. Fixed: ONE new module, `packages/core/src/version.ts`, ports the CLI's own robust walk-up algorithm (`packages/cli/src/bin.ts` `readCliVersion()` -- nearest package.json, not a fixed depth) into `@tina4/core`; `server.ts`/`devAdmin.ts`/`mcp.ts` all import the same `TINA4_VERSION` now (three readers collapse into one; the CLI keeps its own small copy on purpose -- it deliberately avoids an eager `@tina4/core` import so a bare `tina4nodejs --help` stays cheap, and both copies run the identical algorithm). A FOURTH instance of the SAME MCP-default-version gap was found by direct verification against the live source, not assumed correct from the audit's "Python/Ruby are the reference" framing: `tina4_python/mcp/__init__.py` `_get_default_server()` and `lib/tina4/mcp.rb` `Tina4._default_mcp_server` both had the identical unwired-constructor-default bug -- fixed the same minimal way (the one default-server call site in each now passes the resolved version explicitly); the RESOLVERS (`_resolve_version()`, `Tina4::VERSION`) were untouched, matching the ground rule that Ruby/Python's actual resolvers are the reference and stay so. VERSION-DEC-02 (cross-source drift test) is this fixture itself, in all four (Python/Ruby already had a narrower shape via `test_version_constant.py`/`version_spec.rb`, both left as-is). VERSION-DEC-03 (RATIFIED, all four): a default `Tina4/<version>` User-Agent is now sent by the framework's own outbound HTTP client on every request (`tina4_python.api.Api._build_request`, `Tina4\Api::baseHeaders`, `Tina4::API#initialize`'s `@headers` seed, `@tina4/core` `Api#buildRequest`) -- a caller-supplied `User-Agent` always wins (verified: Python's urllib and Ruby's Net::HTTP each silently supply their OWN generic default, `Python-urllib/3.x` / `Ruby`, when the framework does not set one -- both now suppressed by the Tina4 default). PHP's `runtime_version_equals_the_package_manifest` case is special-cased: composer.json genuinely carries no `version` key BY DESIGN (Packagist derives it from git tags), so the suite locks in the absence itself (and would catch a future drift if one is ever added). Ruby has no ASCII-art boot banner (unlike Python/PHP/Node) -- its closest analog is the separate `tina4ruby version` CLI subcommand (a distinct code path from `commands --json`), used as the "banner" leg there. Node's `no_surface_reports_a_placeholder_version` case carries an EXTRA assertion: it copies a real `@tina4/core` build into a fresh temp dir with NO monorepo ancestor (one level of nesting, not three) and asserts the imported copy's resolved version is not `0.0.0` -- the exact relocated-layout scenario the old fixed-depth-3 reader could not survive. Mutation-proved (2026-08-13): (a) reverting PHP's `tina4FrameworkVersion()` to the old composer/installed.json read turns `every_reporting_surface_agrees` + `no_surface_reports_a_placeholder_version` RED (CLI manifest reports `0.0.0`); (b) reverting Node's `resolveFrameworkVersion()` to the old fixed-depth-3 read turns ONLY the relocated-layout assertion RED (`0.0.0`) while every in-tree surface stays green, confirming the bug's latent nature; (c) stripping the default User-Agent from each of the four Api clients turns `the_outbound_http_client_sends_a_tina4_version_user_agent` RED in each (Python falls to urllib's `Python-urllib/3.x`, Ruby to Net::HTTP's `Ruby`, PHP/Node to no header at all) -- all three restored green after confirming RED. The version NUMBER was not changed anywhere -- this feature converges resolvers onto the manifest each repo already declares; App::$VERSION/Tina4::VERSION/pyproject.toml/package.json all stayed at `3.13.99` throughout. Framework-source LOC delta (numstat, doc/comment lines included): Python +24/-2 across 2 files (`api/__init__.py`, `mcp/__init__.py`); PHP +44/-32 across 3 files (`Api.php`, `Bootstrap/MCP.php`, `bin/tina4php` -- the CLI resolver's actual LOGIC shrank from ~22 lines/2 strategies to 6 lines/1 strategy despite a net +3 from added explanatory docblock); Ruby +14/-1 across 2 files (`api.rb`, `mcp.rb`); Node +36/-34 across 5 files (`api.ts`, `devAdmin.ts`, `index.ts`, `mcp.ts`, `server.ts`) PLUS one new 66-line shared module (`version.ts`) replacing what had been three independent, partially-broken readers -- a genuine reduction in DUPLICATE resolver logic (server.ts -12, devAdmin.ts -16) even though total file LOC (including the new module's docblock) does not shrink. ZERO new runtime dependencies in any language. Breaking (correctness/visibility fixes, no user migration): PHP CLI `--version`/`commands --json` now reports the real version (e.g. `3.13.99`) instead of `0.0.0` in a plain checkout; MCP `serverInfo` (all four languages' built-in dev server) now reports the real version instead of the constructor's generic `1.0.0` default; Node banner/health/dashboard now reports the real version instead of `0.0.0` in a relocated/published `@tina4/core` layout. Outbound HTTP requests from the framework's own client now carry `User-Agent: Tina4/<version>` in all four (previously ABSENT in Python/PHP/Node, and a generic interpreter default in Python/Ruby) -- a server keying off the missing/generic UA now sees the real one; a caller-supplied UA is preserved. Verified locally in all four (real boot/HTTP/CLI/capture-server, no mocks) before the lab gate. LAB GATE (.99 Linux lab, clean-room checkout of the exact pushed commits per repo, full-40-hash verified before running, `results:` line read in the SAME run, not backgrounded): `results: python=0 php=0 ruby=0 node=0`. Non-gating broader pass (adjacent version/MCP/Api suites) also fully clean: `broader: python=0 php=0 ruby=0 node=0`. Log `/home/andre/rel-3.13.99/version-gate-060410.log`. Lab-hygiene note (not a feature-130 defect): the gate's own stray-process cleanup step (`pkill -f 'app.py'`, copied from the established `lab-dualport.sh` template) is a bare substring match broad enough to also catch unrelated GPU/AI Docker containers on the shared host (tina4-rag/diffusion/vllm-embed/vllm-vision), which briefly bounced and self-healed via `restart:unless-stopped` (confirmed already at `restartCount=7` before this run, i.e. a pre-existing, recurring issue across prior feature gates, not unique to this one) -- flagged separately for a narrower pkill pattern across all `lab-*.sh` scripts. 2026-08-13) |
| In-process HTTP TestClient | 131 | `test_client_contract.json` | 2 | 2 | 0 | TC-DEC-01, TC-DEC-02 | yes (real in-process dispatch through the REAL front controller in every case, no mocks; the oracle additionally boots a REAL server on a real loopback socket -- python `conftest.boot_child_server` [a real child process], php `TestServer`/`php -S` [the established real-process helper], ruby `Tina4::WebServer` in a thread [ported from `spec/test_client_pipeline_spec.rb`'s own D6 oracle, the model for this fixture], node real `startServer()` on a real ephemeral port. TC-DEC-01 (Node-only structural fix): Node's TestClient RE-IMPLEMENTED the dispatch order (TC-NODE-REIMPL) instead of calling the server's real stage-walker, so the session stage never ran [a session-token auth regression was structurally unreachable through TestClient] and route middleware ran BEFORE the auth gate [inverted from the live gate-before-middleware order, ADR-0012]. Fixed by extracting `packages/core/src/server.ts`'s dispatch closure -- previously trapped inside `startServer()` -- into an exported `runDispatch(ctx, rawReq, rawRes)` parametrised by a new `DispatchContext` (router/middleware/filesystem roots/Frond) instead of closing over `startServer()`'s locals; TestClient now calls it via either the live server's own context (`getLiveDispatchContext()`, preferred when one is running in this process -- mirrors Ruby's `RackApp.current`) or a standalone one (`buildDispatchContext()`, for an explicitly-injected router -- preserves the pre-existing test-isolation contract of `new TestClient(router)`). TestClient's own hand-rolled matching/middleware/auth/fallback logic is DELETED, not reimplemented on top. A real, previously-unfixable correctness bug was found and fixed to make this refactor safe at all: TestClient's mock `ServerResponse` never flips Node's real `writableEnded`/`headersSent` (confirmed empirically -- bypassing the real stream internals), so the pipeline's `if (!res.raw.writableEnded) res.raw.end()` guards (several) would have fired a REDUNDANT second `end()` after every matched route once TestClient started running the full pipeline, and `compressionEtagIntercept`'s own buffered chunks [never cleared between calls] would have RESENT the body, silently DUPLICATING every response; fixed with an own-property `writableEnded` override backed by a single `ended` flag TestClient's own capture sets exactly once, making write()/end() idempotent after that (matches the real 'write/end after end is a no-op' contract). TC-DEC-02 (all four): TestResponse used to collapse a duplicate response header (two `Set-Cookie`) to the LAST value on read; fixed by keeping every value in emission order and adding a multi accessor (`get_all`/`getList`/`get_all`/`getHeaderList`) alongside the UNCHANGED back-compat single accessor. Python's ASGI header list was flattened straight into a dict at construction (a real collapse). PHP's TestResponse read ONLY `Response::getHeaders()` -- cookies live in a SEPARATE store (`Response::$cookies`, keyed by cookie name, precisely so more than one survives) that `getHeaders()` never included at all, so PHP's TestClient could not see a Set-Cookie AT ALL before this fix (a gap, not a collapse) -- fixed via a new shared builder, `Response::cookieHeaderLines(): array` (the SAME value-builder `Server.php`'s raw-socket writer now calls too, replacing its own copy -- Server.php net SHRANK). Ruby's `Response#to_rack` already folds a repeated Set-Cookie into one Rack-convention `\"\\n\"`-joined hash value; TestResponse splits it back into a list (general on any header, not Set-Cookie-special-cased, since a header value can never legitimately contain a raw newline). Node's TestResponse already received a real JS array from `ServerResponse.getHeaders()` for a multi-value header but collapsed it with `.join(', ')` -- unsafe specifically for Set-Cookie, whose own `Expires` attribute can itself contain a comma. TWO adjacent, PRE-EXISTING defects (not scope creep -- both blocked TC-DEC-01/02 from being provable at all) were found empirically while proving the session-login case for real, not assumed from the audit: (1) PHP -- the session-cookie WRITE side branched on `headers_sent()`, which a CLI process (TestClient, PHPUnit) never flips true, so the cookie was emitted via native `setcookie()` into a void nothing reads back; fixed via a new `Response::isTesting()` getter (backed by the ALREADY-EXISTING `testing` constructor flag, which TestClient had simply never opted into) that `Router::emitSessionCookie()` now also checks, and `TestClient::request()` now constructs `new Response(testing: true)`. The session-cookie READ side separately read PHP's raw `$_COOKIE` superglobal directly -- populated only by a real HTTP SAPI -- instead of the abstracted `$request->cookies` `Request::create()` already parses from the identical `Cookie` header; fixed to prefer `$request->cookies`, falling back to `$_COOKIE` (a real request already has both agree, so this is additive there). A live `Tina4\\Server` instance was separately confirmed, empirically [`curl` against a real running instance], to ALSO never emit a first-time session cookie, for the identical `headers_sent()` reason -- a genuine, previously-undiscovered production defect, deliberately NOT fixed here (out of scope for a TestClient-fidelity feature; flagged as a separate follow-up). (2) Node -- `sessionAutoStart` wrote the session `Set-Cookie` via `rawRes.setHeader(...)`, which REPLACES any Set-Cookie a route handler had already set via `response.cookie()` -- confirmed to affect the LIVE server too, not just TestClient; fixed to `rawRes.appendHeader(...)` (adds to an existing value, behaves exactly like `setHeader` when nothing is set yet). Framework-source LOC delta (numstat): Python +21/-1, 1 file (`test_client/__init__.py`); PHP +151/-24 across 4 files (`Response.php` +55 new `cookieHeaderLines()`/`isTesting()`, `Router.php` +39 the two session-cookie fixes, `Server.php` -24 [net SHRANK -- its own cookie-line builder replaced by a thin wrapper around the new shared one], `TestClient.php` the header multi-map + `testing: true`); Ruby +34/-1, 1 file (`test_client.rb`); Node testClient.ts itself SHRANK (280 -> 269 lines, -11 net despite heavy churn: deleted ~140 lines of hand-rolled route-matching/middleware-running/fallback-construction, replaced by a thin delegation to the real pipeline plus the `writableEnded` fix and the header multi-map) while `server.ts` grew (2306 -> 2446, +140) absorbing the hoisted, now-exported, now-reusable dispatch functions (`DispatchContext`, `runDispatch`, `buildDispatchContext`, `getLiveDispatchContext`) with full documentation -- combined dispatch+testClient+dispatchPipeline surface net +138 lines, almost entirely genuinely NEW capability (two new exported functions no prior code path offered) and doc density matching this codebase's norm, not duplicated logic (the point of the refactor is that the dispatch order now exists in exactly ONE place instead of two). ZERO new runtime dependencies in any language. Mutation-proved: (Node) reverting TestClient to the pre-fix re-implemented dispatch turns the oracle [`req.session` is undefined -> throws], the gate-order case [the marker fires before the 401], AND the session-login case all RED [restored green]; (Python/Ruby/PHP) reverting the header-list/cookie-merge fix turns `duplicate_response_headers_are_all_exposed` RED in each [restored green]; (PHP) additionally reverting either the `isTesting()` write-side fix or the `$request->cookies` read-side fix independently turns `a_session_login_then_authenticated_request_succeeds` RED [restored green]. Breaking (fidelity/correctness, no user migration): a Node route middleware that used to run on a would-be-401 request no longer does [the correctness fix ADR-0012 requires]; Node/PHP session-auth via TestClient is now exercised where it structurally could not be before; Python/PHP/Ruby/Node `TestResponse` headers preserve duplicates [single `headers[name]`/`.headers.get(name)` unchanged = last value; new multi accessor added]. LAB GATE (.99 Linux lab, clean-room checkout of the exact pushed commits per repo, full-40-hash verified before running, `results:` line read in the SAME run, not backgrounded): `results: python=0 php=0 ruby=0 node=0`. Non-gating broader pass (adjacent TestClient/dispatch/session/middleware suites, same lab checkouts) also fully clean: python 11/11 (`test_test_client_auth.py` + `test_test_client_front_controller.py`), php 30/30 (`TestClientFrontControllerTest`+`TestClientAuthTest`+`RouterAuthSourcesTest`+`SessionCookieNameTest`), ruby 52/52 (`test_client_pipeline_spec`+`middleware_parity_spec`+`route_group_auth_spec`+`auth_check_spec`), node 38/38 across 5 files (`testClientAuth`+`testClientFrontController`+`dispatchPipeline`+`requestContract`+`sessionCookieName`). Nothing left running on the lab afterward (verified). Log `/home/andre/rel-3.13.99/lab-testclient.20260813-073732.log`. 2026-08-13) |
| Development error overlay | 126 | `overlay_contract.json` | 4 | 4 | 0 | OVERLAY-DEC-01, OVERLAY-DEC-02, OVERLAY-DEC-03, OVERLAY-DEC-04 | yes (real dispatch / a real thrown 500, no mocks; dead `render_production_error` DELETED in all four + a wired-path prod-no-leak test replaces the dead-sibling unit test; ONE redaction helper per lang masks Authorization/Cookie/Set-Cookie + password-like body/param keys; PHP now renders headers deliberately; 50-frame cap; guarded overlay render falls back to the safe page; gate unified on `is_debug_mode`; mutation-proved; 2026-08-12) |
| Inline testing | 132 | `inlinetesting_contract.json` | 3 | 3 | 0 | INLINE-DEC-01, INLINE-DEC-02 | yes (real `tina4 <lang> test` child process, no mocks; ONE wired surface -- the `@tests`/expect_* descriptor model -- discovered + run with a real exit code, 0 on pass / non-zero on fail; PHP `Testing::discover()` eval() RCE REMOVED (literal-only parser) + confined to an explicit tests dir (no blanket require of src); descriptor builders renamed assert_*->expect_* so they no longer collide with the xUnit assert_*; Python meta-test snapshots/restores the global registry; mutation-proved; 2026-08-12) |
| File upload | 44 | `fileupload_contract.json` | 3 | 3 | 0 | UP-DEC-02, UP-DEC-03 | yes (real multipart bytes through the real parser + real temp files + a real over-limit body, no mocks; a REPEATED file field name -> a LIST in all four (no silent drop; Python `_parse_multipart`/`_is_file_value`, PHP `parseMultipartBody`, Ruby a raw-body hand-scan since Rack collapses repeats, Node already listed), single stays a scalar descriptor; ONE safe-save helper per lang (`save_upload`/`saveUpload`/`Request::saveUpload`) strips path components + realpath-confines so a `../` name is written INSIDE the target dir and an unusable name is refused; a RUNNING per-chunk size guard brings PHP (`Server::enforceRequestLimits` on actual bytes) and Ruby (`read_stream_capped` over `rack.input`) to Python/Node parity, refusing 413 as the bytes arrive; mutation-proved; 2026-08-12) |
| ORM result caching | 25 | `ormcache_contract.json` | 3 | 3 | 0 | CACHE-DEC-01 | yes (real SQLite, real rows, real ORM writes, no mocks; CACHE-DEC-01 owner-override: KEEP the explicit `Model.cached()` and FIX its invalidation, do NOT drop it. Three fixes identical in all four: BUST ON ALL WRITES -- save AND delete AND force_delete AND restore through the ORM invalidate the cached read (before: only Python's save() busted; PHP/Ruby never busted; Node's drifted-in cached() never busted); TAG BY EVERY TABLE the query touches -- the model's own table plus every FROM/JOIN table -- so a cross-table JOIN cached on one model is busted when the OTHER model writes, while a write to an UNRELATED table leaves it intact (tag-scoped, never a wholesale flush); `ttl<=0` = NO-CACHE, not infinite. Node's `cached()` (which had drifted into existence since the audit as a per-class, untagged, never-busted store) rebuilt onto the ONE process-wide tag-aware query cache shared by all models; PHP switched off the untagged SQLTranslator static cache onto the existing tag-aware `QueryCache` (zero new deps). Proven POSITIVELY that it caches -- a DIRECT non-ORM db write between two reads is invisible to the second within-TTL read; mutation-proved: drop the write-bust -> stale-after-write RED, make ttl=0 cache -> ttl-zero RED, wholesale-flush -> unrelated-table RED; 2026-08-12) |
| Race-safe database next-id | 16 | `nextid_contract.json` | 2 | 2 | 0 | NEXTID-DEC-01, NEXTID-DEC-02 | yes (real PostgreSQL + MySQL + MongoDB, real concurrent callers, no mocks; NEXTID-DEC-01 fixes the generic get_next_id TOCTOU + the PostgreSQL first-use race: the generic tina4_sequences fallback is now ONE atomic `UPDATE ... SET current_value = current_value + 1 ... RETURNING current_value` (was UPDATE then a SEPARATE SELECT -- two concurrent callers read the same value and returned a DUPLICATE id), and PostgreSQL bootstraps with `CREATE SEQUENCE IF NOT EXISTS` + always draws from `nextval()` so two concurrent first-callers share ONE counter instead of the loser drawing a duplicate from a second one; proven with N concurrent callers each on their OWN connection (Python threads, PHP `pcntl_fork` children, Ruby threads, Node `Promise.all` over N independent adapters) returning N DISTINCT ids on a fresh PG table, through the generic fallback directly, and on MySQL's LAST_INSERT_ID path. NEXTID-DEC-02 gives MongoDB ONE dedicated atomic path in all four -- `findOneAndUpdate({_id: seq}, {$inc:{current_value:1}}, {upsert, returnDocument:after})` keyed by `_id` (built-in unique index = race-safe first-use) -- replacing PHP's swallow-to-`return 1` (a duplicate-PK collision) and Ruby's/Node's fall-through to the relational path where the `+ 1` UPDATE was dropped and every call returned the SAME id; proven monotonic (id2 > id1) and concurrency-safe (N concurrent -> N distinct). ZERO new runtime deps. Mutation-proved: revert the fallback to UPDATE-then-separate-SELECT -> the generic-concurrency case goes RED (a duplicate); drop the `$inc` -> the mongo-monotonic case goes RED (id2 == id1); 2026-08-12) |
| SQL translator | 7 | `sqltranslator_contract.json` | 2 | 2 | 0 | SQLTRANS-DEC-01, SQLTRANS-DEC-02, SQLTRANS-DEC-03 | yes (real MySQL + PostgreSQL, no mocks; SQLTRANS-DEC-01 DATA-CORRUPTION fix: the dialect rewrites (`\|\|`->CONCAT, ILIKE->LOWER LIKE, TRUE/FALSE->1/0) used to MANGLE STRING LITERALS -- a value `'a\|\|b'`, a label `'TRUE'`, an ILIKE-mentioning LIKE pattern -- and concat split the WHOLE statement (`SELECT a \|\| b FROM t` -> `CONCAT(SELECT a, b FROM t)`); all four now mask literals/comments -> rewrite -> restore and concat rewrites only the operand chain (Python `_mask_literals`, PHP `maskLiterals`, Ruby `mask_literals`, Node `maskLiterals`), and the multi-word ILIKE pattern the old greedy `\S+` truncated survives whole. SQLTRANS-DEC-02: concat/ilike are WIRED into the MySQL query path in Ruby (`MysqlDriver#translate_dialect`) and PHP (`MySQLAdapter::translateDialect`) to match the already-wired Python/Node adapters, so a portable `\|\|`/ILIKE query RUNS on real MySQL in all four; Ruby's dead+wrong `limit_to_rows`/`limit_to_top`/`placeholder_style` (drivers own pagination via SELECT FIRST/SKIP + OFFSET/FETCH and placeholders via `?`/`$1`) and the duplicate `SQLTranslator.query_key` (QueryCache.query_key is the live one) were DELETED -- one path per concern, less code. SQLTRANS-DEC-03: `BIGINT PRIMARY KEY AUTOINCREMENT` -> PostgreSQL BIGSERIAL / MySQL BIGINT AUTO_INCREMENT (was a plain BIGINT with the keyword stripped -- no sequence, insert-with-no-id failed the NOT NULL key); UPSERT/date-time omission documented in 007. ZERO new runtime deps; the MySQL wiring is a no-op unless a `\|\|`/ILIKE token is present. Mutation-proved: revert the literal-safe rewrite -> the literal cases RED; revert the PostgreSQL BIGINT branch -> the bigint case RED; 2026-08-12) |
| PostgreSQL provider (write-path oracle) | 9 | `pgprovider_contract.json` | 5 | 5 | 0 | PG-DEC-01 | yes (real PostgreSQL :55432, no mocks; PG-DEC-01: the adapter-contract test now asserts real write-path BEHAVIOUR with PostgreSQL as the reference model in all four, not just method presence. Five write-path behaviours pinned against a real SERIAL-key table (TRUNCATE/RESTART-per-case so the RETURNING id is deterministic), with durability read back on a SECOND fresh connection: (1) insert returns a DatabaseResult whose last_id is the RETURNING-generated key + affectedRows 1, and a second insert returns the NEXT id not a stale one; (2) update/delete report the EXACT affected count and last_id null; (3) a filterless update (no filter, no PK in data) and a filterless delete RAISE and change nothing; (4) a standalone execute_many owns ONE transaction -- a mid-batch UNIQUE violation (RETURNING forces the row-at-a-time loop) rolls the WHOLE batch back so a fresh connection sees ZERO rows, and empty input is a zero-row no-op; (5) fetch returns a native list of row maps + fetch_one one native record or null (value-correctness + list-of-maps shape is the cross-language gate, since INTEGER type mapping varies -- Python/Ruby/Node native int, PHP ext-pgsql string). ZERO framework code changed: the write path was already correct in all four (the oracle is healthy, as the 009 audit predicted); this feature is the shared no-mock behavioural test that pins it. Mutation-proved in all four: force execute_many to commit per row -> the mid-batch case goes RED (first row survives on the fresh connection); make a filterless delete run a full-table DELETE -> the delete-guard case goes RED (Python/PHP go RED on a plain guard-disable; Ruby/Node have adapter-level empty-WHERE belt-and-braces so the property mutation is the full-table delete); disable RETURNING last-id in Python -> the last-id case goes RED. The full 14-method adapter interface redesign (feature 3, ADR-0044, `adapter_contract.json`) remains OWED and is out of scope. 2026-08-12) |
| Write-path fail-loud gap (facade insert) | 3/9 | `write_path_faillloud_contract.json` | 2 | 2 | 0 | (extends feature 9's write-path contract; no new ADR) | yes (real SQLite + real PostgreSQL, no mocks; owner-flagged 2026-08-12, the exact out-of-scope defect the feature-28 seeder pass surfaced and deferred (see the seeder row above): Node's `Database.insert()` facade did NOT fail loud on a SQLite constraint violation -- it returned `{success:false, affectedRows:0, error}` instead of throwing, unlike update()/delete()/truncate()/execute()/fetch(), which already run their adapter result through the shared `assertWrote()` check (or PHP's equivalent explicit `if ($result === false) throw`). FIXED: `Database.insert()` (`database.ts`) now runs its result through the SAME `assertWrote(result, \"insert\", table)` update()/delete()/truncate() already call -- one call site. MEASURED, NOT FIXED (already correct): Python's and Ruby's `insert()` is a thin wrapper over `execute()`, which never catches a driver exception, so a constraint violation already propagated; PHP's `Database::insert()` already had the explicit throw. CHARACTERISED (unaffected, verified not to ripple): `BaseModel`/`ORM.save()`'s documented `this \| false` contract is UNCHANGED in all four -- Node's `save()` never calls `Database.insert()` at all (it builds its own INSERT and runs it through the already-fail-loud `adapterExecute()`, already wrapped in `save()`'s own try/catch); Python/PHP/Ruby's `save()` already wraps its `db.insert()`/`->insert()` call in try/except\|catch\|rescue and already converts the raise to `false`. Extended the existing per-repo `write_path_contract.json` fixture (shipped identically in all four repos' own test dirs, predates this ledger's convention) with `facade_insert_on_constraint_violation_throws` (a real PRIMARY KEY collision on the fixture's own table -- zero interpreter code changes needed, the shared runners already dispatch `op:\"insert\"` + `expect_raises` generically); added `save_on_constraint_violation_returns_false` to each language's `ormBaseContract` suite (a real UNIQUE index added for the case, mutation-proved as its own gate). Mutation-proved: reverting the `assertWrote()` call turns the Node write-path case RED with 'the op did not throw'; dropping the UNIQUE constraint the save() cases add turns all four RED with 'expected false, got \<the newly-saved row\>' -- both restored green. Framework-source LOC delta: Node `database.ts` +11/-1 (one call site + doc comment) -- the ONLY framework-source change across all four; Python/PHP/Ruby ZERO framework-source changes (fixture + new regression test only). ZERO new runtime deps. Breaking: `Database.insert()` now THROWS on a Node SQLite constraint violation where it used to return `{success:false}` silently -- a caller testing `.success===false` on an insert() return must switch to try/catch (matches the already-documented contract and update()/delete()/truncate()/execute()/fetch(), and the other three engines). `BaseModel.save()` is UNCHANGED (still returns `false`). Verified locally first (SQLite; Python/PHP/Ruby ZERO framework changes): Python 302 passed/22 skipped, PHP 347 tests/1 pre-existing unrelated error (reproduces identically in isolation -- a PHPUnit cross-file state leak in `SeederContractTest`, flagged separately, worked around in the lab script by running that one file alone), Ruby 418 examples/0 failures/22 pending, Node 24-file broader run all green + `npm run typecheck` clean (all missing/pending counts are local services this Mac does not run). LAB GATE (.99 Linux lab, clean-room checkout of the exact pushed commits, `results:` line READ in the same run, not backgrounded): the GATE (write_path_contract on SQLite AND real PostgreSQL + ormBaseContract on SQLite, per language) is `results: python=0 php=0 ruby=0 node=0` -- the facade-throws case passes on BOTH SQLite and real PostgreSQL in all four, confirming the fix (and the pre-existing PG-DEC-01 correctness) holds on the write-path oracle engine too, not SQLite alone. The separate, non-gating `broader:` due-diligence line (the wider ORM+write-path+seeder+autocrud suite family) is ALSO fully clean on the confirming re-run: `broader: python=0 php=0 ruby=0 node=0`. A first run of this due-diligence line read `node=1`, root-caused to a lab-SCRIPT defect, not a code defect: `export TINA4_REQUIRE_SERVICES=1` had been set globally (copied from the seeder-28 gate template, which DOES provision Firebird), so Node's `seederContract.test.ts` -- byte-identical to its pre-fix version, confirmed via `git diff HEAD~1` -- hard-exited via its own pre-existing `skip()` -> `process.exit(1)` the instant it saw Firebird unreachable, a service genuinely out of scope for this feature and never provisioned here on purpose. Reproduced locally with full stderr capture (`git diff HEAD~1 HEAD --stat -- test/seederContract.test.ts packages/orm/src/seeder.ts packages/orm/src/fakeData.ts` empty; direct re-run of the file under the same env exits 1 identically, zero unhandledRejection/uncaught-exception text anywhere in either lab log). Fixed by scoping `TINA4_REQUIRE_SERVICES=1` to each language's GATE subshell only (where PostgreSQL really is provisioned and required) and leaving it unset for the due-diligence subshells; re-deployed and re-ran the WHOLE gate end to end for a clean confirming green, independently re-scanned for any SKIP-AS-FAIL/unhandledRejection/uncaught text (zero found) and for the exact 4 pushed SHAs (`clean-room checkout at <sha> (matches pushed commit)`, all 4 confirmed). Log `/home/andre/rel-3.13.99/lab-writepath-faillloud.20260812-235422.log` (supersedes the earlier `...-234747.log`, kept on the lab for the record of the diagnosed-and-fixed script issue). 2026-08-12) |
| MySQL provider | 10 | `mysqlprovider_contract.json` | 4 | 4 | 0 | MYSQL-DEC-01, MYSQL-DEC-02 | yes (real MySQL :3306, no mocks; MYSQL-DEC-01: MySQL has no RETURNING, so the provider emulates it by RE-SELECTING the inserted row. Measured against all four live adapters, the RETURNING-*record* emulation exists ONLY in the Python adapter, and it fetched by a HARDCODED `id` column, so a table whose PK is NOT named `id` got a wrong/empty row ("Unknown column id"). Python now re-selects by the table's REAL primary key (introspected via the strict-quoted DESCRIBE, cached) -- never a hardcoded `id`; witnessed Python-only by the `returning_*` tests (revert -> RED). PHP/Ruby/Node do NOT emulate MySQL RETURNING and surface the generated key through the driver's own last_insert_id, which is tied to the AUTO_INCREMENT column and is therefore COLUMN-NAME-INDEPENDENT, so a non-`id`-PK insert already returns the correct generated last-id in all four -- pinned four-way as invariant 1 (the user-observable of MYSQL-RETURNING-ID). Per the owner governance rule (fix the broken one, don't mirror a mechanism the others don't need) the hardcoded-`id` fix is Python-adapter-local; the SAME pattern lives in Python's mssql.py + firebird.py for features 11/12 and is untouched here. MYSQL-DEC-02: (a) the DESCRIBE introspection took an unparameterised, unquoted table name in all four (Python/PHP/Ruby interpolated raw; Node back-quoted but did not ESCAPE an embedded backtick) -- now STRICT-quoted (backticks, embedded backticks doubled), schema-aware, in all four, so a crafted/odd name is ONE escaped identifier (a clean "unknown table", never runnable SQL) and an odd-but-valid name introspects; mutation-proved (revert the quote -> the odd-name case breaks). (b) the FIRST->LAST batch-id math was re-implemented inline in each MySQL adapter (PHP TWICE -- prepared + non-prepared) instead of the shared, otherwise-dead SQLTranslator.batch_last_id/batchLastId helper: Python + Ruby now route through it, PHP collapses its two blocks into ONE captureInsertId(), Node loops row-at-a-time so it has no block to de-dup; MEASURED on the lab, only RUBY's db.insert collapses a 3-row batch into one multi-row INSERT so batch_last_id is mutation-witnessed there (neutralise it -> the LAST-id case reports the FIRST id, RED), while Python/PHP/Node run db.insert row-at-a-time (green regression lock, behaviour-preserving de-dup); Node's adapter has NO first->last normalisation at all (a latent parity gap for a hand-written multi-row INSERT, flagged not fixed). All mutation-proofs demonstrated on the .99 lab: revert real-PK RETURNING -> "Unknown column id" RED (Python); revert strict DESCRIBE quote -> syntax error near the odd name RED (Python + Node); neutralise batch_last_id -> FIRST-id RED (Ruby). ZERO new runtime deps. 2026-08-12) |
| MSSQL provider | 11 | `mssqlprovider_contract.json` | 3 | 3 | 0 | MSSQL-DEC-01, MSSQL-DEC-02 | yes (real SQL Server :1433, no mocks; MSSQL-DEC-01 safe parameter handling: (a) Ruby's hand-rolled interpolator emitted a BAREWORD on its `else param.to_s` for any unrecognised type (a Symbol became `VALUES (foo)` -> an invalid/injected statement) -- it now emits ASCII-8BIT bytes as a `0x` varbinary literal and RAISES ArgumentError on a genuinely unbindable type, never a bareword (witnessed Ruby-only by `an unbindable parameter raises instead of a bareword`, revert -> RED); (b) Node inferred the TDS type from the JS type with NO Buffer branch, so a Buffer bound as NVarChar and corrupted binary writes -- it now maps Buffer -> VarBinary (checked first). The FOUR-WAY observable `a binary parameter round trips intact` is proven against a real VARBINARY column with a NUL-byte + high-byte payload: Python's pymssql binds bytes natively, Node binds VarBinary, PHP and Ruby inline a `0x` literal because FreeTDS (pdo_dblib/tiny_tds) CANNOT bind raw binary through a `?` placeholder -- MEASURED on the lab, a NUL byte breaks the TDS stream (even PDO::PARAM_LOB fails) while a `0x` literal round-trips byte-for-byte; a companion `a text parameter round trips intact` guards that binary detection never mis-routes UTF-8 text through the 0x path. MSSQL-DEC-02 real-PK RETURNING: SQL Server has no RETURNING, so the PYTHON adapter strips it and re-selected the inserted row by a HARDCODED `id` column, raising 'Invalid column name id' on a non-`id`-PK table; Python now re-selects by the table's REAL primary key (introspected, bracket-quoted with embedded `]` escaped, cached) -- the same defect+fix feature 10 applied to mysql.py, shared with firebird.py. Measured against all four live adapters, the RETURNING-*record* emulation exists ONLY in Python; PHP/Ruby/Node surface the generated key through SCOPE_IDENTITY, which is IDENTITY-column-tied and column-name-independent, so a non-`id`-PK insert already returns the correct last-id in all four -- pinned four-way as `mssql-nonid-pk-generated-id` (witnessed Python-only by the `returning_*` tests, revert -> 'Invalid column name id' RED). MSSQL-DEC-02 one pagination strategy: the strategy DIVERGED (Python/PHP/Ruby OFFSET/FETCH; Node `TOP n` for the first page); all four now use the ONE OFFSET/FETCH strategy -- Node's `fetchAsync` no longer emits TOP for a page-one read (TOP 0 remains only for the degenerate limit=0 zero-rows case OFFSET/FETCH cannot express), proven four-way by the offset window (a TOP-only strategy that ignores the offset returns the FIRST page for a later-page request; force TOP/drop offset -> RED). ZERO new runtime deps. 2026-08-12) |
| Firebird provider | 12 | `firebirdprovider_contract.json` | 5 | 5 | 0 | FB-DEC-01, FB-DEC-02, FB-DEC-03 | yes (real Firebird 5.0.4 :3050, no mocks; MEASURING db.insert/update/delete through the facade showed the Firebird write-contract broken in ALL FOUR, wider than the audit's Ruby/Node scoping. FB-DEC-02 generator last-id: db.insert().last_id is now the GEN_<TABLE>_ID key in all four -- Python via INSERT ... RETURNING * emulation (INSERT_RETURNING was "" so it returned None), PHP via native RETURNING * (the native adapter declared none -> 0), Ruby via a GEN_ID read (was nil), Node via a GEN_ID read (was null); column-name-independent so correct for a non-`id` PK too, pinned four-way as `firebird-nonid-pk-generated-id`. FB-DEC-02 real affected count: update/delete report the EXACT rows changed -- Python cursor.rowcount, Ruby the fb execute() return, PHP ibase_affected_rows on the transaction the statement ran on (it read the LINK's implicit tx -> 0), Node a Firebird 5 `... RETURNING 1` row count (node-firebird gives no DML count; Node had FABRICATED 1). FB-DEC-02 Python non-`id` PK: the emulated RETURNING re-select uses the table's REAL introspected+quoted primary key, not a hardcoded `id` (a non-`id`-PK insert raised a Firebird 'Dynamic SQL Error') -- the feature-10/11 fix shared with mysql.py/mssql.py, witnessed Python-only by the returning_* test. FB-DEC-03 blob: a binary blob (NUL + high bytes) round-trips byte-for-byte; Node reads the STREAMING blob FUNCTION into a Buffer (the decodeBlobs no-op asserted-not-verified a Buffer and leaked the function). FB-DEC-03 SRP: node-firebird's flaky SRP login (~12% historically) gets a bounded in-adapter retry in connect(); node-firebird moved from a devDependency to an optionalDependency of @tina4/orm. FB-DEC-01 (the no-mock rule, non-negotiable): the Ruby reconnect spec's RSpec doubles are replaced with a REAL forced-disconnect test (kill the adapter's own attachment via MON$ATTACHMENTS from a second connection -> "Connection shutdown") -- in fact NO framework had a real end-to-end reconnect test before (Python monkeypatched, PHP matcher-only, Node had none); Node gains the reconnect path so the invariant is four-way. FB-COLUMN-CASE-TRAP documented (unquoted identifiers fold to UPPERCASE; a quoted ALL-CAPS name cannot round-trip -- inherent, not fixed). Mutation-proved on the .99 lab: fabricate the count / read the wrong tx handle -> the affected cases RED (Node 3, PHP 2); revert the generator read / INSERT_RETURNING -> the last-id cases RED (Ruby 3, Python 3); no-op the blob decode -> the blob case RED (Node); a `WHERE id` re-select -> the non-`id`-PK witness RED (Python); neutralise the reconnect -> the reconnect case RED (Ruby 2). ZERO new runtime deps. 2026-08-12) |
| ODBC provider | 13 | `odbcprovider_contract.json` | 5 | 5 | 0 | ODBC-DEC-01, ODBC-DEC-02 | yes (real PostgreSQL over ODBC/psqlodbc 16, no mocks; ODBC is the generic escape hatch and its query/CRUD path had NEVER run against a real ODBC source in ANY language, so every finding was LATENT. ODBC-DEC-01 (the highest-value action): the lab provisions unixODBC + odbc-postgresql pointing at the real lab PostgreSQL (a `tina4_odbc` DB), and the shared write path runs through REAL ODBC in all four -- which caught two bugs the audit did not predict: (a) Ruby could NOT connect with a connection string at all -- `ODBC::Database.new(str)` routes to SQLConnect (a DSN NAME), so a `DRIVER={...}`/`DSN=...` string raised "[unixODBC]Invalid string or buffer length"; connect() now uses `#drvconnect` (SQLDriverConnect); (b) PHP's factory could not instantiate `odbc:///...` -- `parse_url()` choked on the braces/semicolons and threw "Cannot determine database type"; odbc is now special-cased before the URL parser. ODBC-DEC-02 (the mandated fixes): ODBC-PK-STUB -- get_columns/columns/getColumns hardcoded primary_key=false in ALL FOUR, so feature 4's filterless-write guard could not introspect the key and a PK-keyed `update(table, data)` threw; each adapter now reads the REAL PK (Python `cursor.primaryKeys`, Ruby `#primary_keys`, Node `connection.primaryKeys` -- all SQLPrimaryKeys; PHP via INFORMATION_SCHEMA because PDO_ODBC exposes no catalog call). ODBC-PY-QUIRKS -- Python's execute() ran `SELECT @@IDENTITY` after EVERY statement (a SQL-Server-ism) which errored on PostgreSQL AND, running on the same cursor, overwrote the INSERT rowcount to -1 (MEASURED: affected_rows -1 old vs 1 fixed); last-id is now driver-aware (@@IDENTITY only after an INSERT on a SQL Server DBMS), and connect() now HONOURS the username/password it ignored (appended as UID/PWD). ODBC-NODE-QUIRKS -- updateAsync had NO string-WHERE branch (a string filter was walked with Object.keys -> ["0","1",...] and params dropped) and executeManyAsync had NO owns-guard (nested in a caller's transaction its commit committed the OUTER transaction early); both fixed to match pg/mysql/mssql, insertAsync now routes a batch through executeManyAsync, and the factory passes separate creds through. ODBC-FAILLOUD -- PHP fetch/fetchOne SWALLOWED a query error to empty while execute raised; PHP now raises DatabaseException like PostgresAdapter (Python/Ruby already fail loud, pinned four-way). The write-path fixture supplies the PK explicitly on insert (ODBC has no reliable last-insert-id) and asserts row STATE read back on a fresh connection (the robust cross-binding observable); the credentials case runs against a real scram-authenticated PostgreSQL over the lab's non-loopback IP (creds only as params -> connects; wrong password -> raises). Mutation-proved on the .99 lab: revert the PK read to false -> the PK-keyed + non-id cases RED (all four); restore the true-original @@IDENTITY execute -> affected_rows -1, the insert case RED (Python); drop the credential append -> the credentials case RED (Python); neuter Node's string branch -> the string-where case RED; force Node owns=true -> the owns-guard case RED (3 rows survive rollback); make PHP/Ruby fetch swallow -> the fail-loud case RED; revert Ruby #drvconnect -> every case RED. The language bindings are the existing OPTIONAL ones, now DECLARED (pyproject `odbc` extra, Ruby Gemfile `:odbc` group, Node @tina4/orm optionalDependencies); unixODBC + the driver are OS packages on the lab. ZERO new runtime deps. 2026-08-12) |
| Background tasks | 47 | `backgroundtasks_contract.json` | 3 | 3 | 0 | BG-DEC-01, BG-DEC-02 | yes (real runtime, no mocks; BG-DEC-01: a task scheduled via `background()` RUNS under the PRODUCTION runtime, not just a dev built-in -- Python now starts tasks from the ASGI lifespan startup so they tick under a REAL uvicorn (BG-PY-PROD-NOOP; one shared `_spin_up_background_tasks` on both servers) + drives the REAL lifespan protocol in-process; PHP runs them in the REAL persistent socket server child + a LOUD FPM/Swoole SAPI guard (`App::backgroundSapiWarning`) that warns with the remedy, never a silent drop; Ruby a REAL OS thread, Node the REAL event-loop timer; BG-DEC-02: ONE surface pinned all four -- `background(cb,interval)` -> a HANDLE with a boolean `stop()` plus a `count` (Ruby gains `Tina4::Background.count` + a Hash-accessible `Task` handle so old descriptor reads still work; PHP gains `Tina4\BackgroundTask`, BREAKING from fluent `$this`; Node `stop()`->bool); mutation-proved; 2026-08-12) |
| ORM base class | 17 | `ormbase_contract.json` | 2 | 2 | 0 | ORM17-DEC-01 | yes (real SQLite, real rows, real ORM writes, no mocks; ORM17-DEC-01 = FIX + MAINTAINABILITY/less-code: remove the VESTIGIAL ORM base state, BEHAVIOUR-PRESERVING. Node BaseModel `_exists` was WRITTEN on find/load/save via `(x as any)._exists = true` but NEVER READ (grep-confirmed zero reads) -- the 3 assignments DELETED; Node's `tableFilter` is NOT vestigial (a declared static, read in findById/all/where/count/withTrashed + AutoCrud) so it STAYS. PHP `tableFilter` was READ in count() but NEVER DECLARED on the base (routed `__get` -> null -> always skipped) -- the count() read + its two frameworkProps exclusion-list entries DELETED; PHP `_exists` is NOT vestigial (READ in save() as the finder-loaded insert-vs-update shortcut) so it STAYS. Python/PHP/Node dead locals in delete/force_delete/restore (Python `table_sql`+`pk_db_col`, plus `pk`/`pk_value` in restore which has no null-check; PHP `$pkColumn`; Node `pkCol`) -- unread pk-where-refactor leftovers -- DELETED. Ruby carries NO vestigial state (no `_exists` -- it uses `@persisted`, READ in save; no `tableFilter`; its delete/force_delete/restore locals are all read by the missing-PK raise guard) so it removes NOTHING and carries the round-trip suite for parity only. The shared fixture is the behaviour-preserving SAFETY NET: a real save -> load -> query -> re-save/update -> count -> delete round-trip GREEN IDENTICALLY on the PRE- and POST-removal code -- the re-save-updates-in-place-not-a-duplicate case guards the `_exists` removal (the insert-vs-update decision never consulted it), the count-is-the-live-total case guards the `tableFilter` removal, and a deleted model ABSENT from find+count guards the delete dead-local removal. Net framework-source diff DOWN 19 lines (Python -8, PHP -5, Node -6, Ruby 0), ZERO new runtime deps; grep confirms 0 remaining references to the removed state. 2026-08-12) |
| ORM fields + column mapping | 18 | `ormfields_contract.json` | 5 | 5 | 0 | FIELD-DEC-01, FIELD-DEC-02, FIELD-DECIMAL-PRECISION, FIELD-PHP-DATETIME-HEURISTIC, FIELD-FK-RELATEDNAME-RUBY | yes (real PostgreSQL :55432 + MySQL :3306 + MSSQL :1433 + Firebird 5 :3050 + SQLite, NO mocks; FIELD-DEC-01 reconciles the field model BEHAVIOUR-BY-BEHAVIOUR while KEEPING each language's declaration surface -- Python field objects, PHP reflection on typed properties, Ruby DSL class-methods, Node declarative FieldDefinition. Five behaviours pinned four-way (or per-language where the declaration model is the reason): (1) PHP CALLABLE DEFAULTS close the one FIELD-DEC-01 gap -- a PHP typed property cannot hold a Closure default, so a fieldDefaults() method hook resolves them PER INSTANCE on construct and DROPS them from the DDL, matching Python _resolve_default / Ruby per-instance lambda / Node typeof===function; (2) DECIMAL(p,s) parity (FIELD-DECIMAL-PRECISION) -- MEASURED, NONE of the four emitted DECIMAL(p,s), so each got its idiomatic decimal path (Ruby fixes decimal_field's REAL bug that dropped the scale, Python adds DecimalField(precision,scale), Node adds type:'decimal'+precision/scale, PHP adds a $decimals overlay), FloatField/NumericField stay REAL (documented float default); (3) FIELD-ENGINE-DDL-MOCK -> REAL -- the Python monkeypatch mock (test_orm_v3_13_11::test_engine_dispatch asserting a SQL string) is DELETED and replaced by a REAL-engine test that CREATEs the table on PG/MySQL/MSSQL/Firebird and reads the column type from each engine's catalog (BOOLEAN->boolean/tinyint(1)/bit/INTEGER, JSON->jsonb/json/nvarchar(max)/BLOB SUB_TYPE TEXT, DECIMAL(12,4)->precision 12 scale 4), which caught Node mapping a Firebird bool to SMALLINT vs the others' INTEGER -- Node aligned to INTEGER; (4) PHP datetime heuristic (PHP-only) anchored from the /(_at$|date|time)/i substring to a whole-suffix pattern so updated_by/runtime/downtime are no longer misclassified while created_at/*_date/*_time still are; (5) Ruby FK auto related_name (Ruby-only) smart-pluralized via a new Tina4.pluralize (Category->categories, not the naive categorys). Also fixed Python save() to return False+log on a non-serializable JSON value (it RAISED out of save()). Declaration surface NOT converged; ZERO new runtime deps. Latent parity gap flagged: Python/PHP/Node still derive the FK has-many name as declaring-class-lowercased + 's'. 2026-08-12) |
| Input and request validation | 19 | `validation_contract.json` | 4 | 4 | 0 | VALID-DEC-01, VALID-DEC-02, VALID-TWO-MESSAGES, VALID-NODE-PUT-NOVALIDATE | yes (real SQLite for the save()-writes-nothing cases + a real node:http socket for the Node AutoCrud PUT, NO mocks; TWO validators kept and both REAL-tested: (1) VALID-PY-UNTESTED -- Python's request Validator was two `assert True` PLACEHOLDERS faking coverage, DELETED and replaced with pos+neg for every rule; (2) VALID-PHP-ENFORCE-UNTESTED -- PHP's validate-on-save enforcement had no test, now a $fields model with invalid data returns false from save() and a real COUNT(*) proves zero rows written; (3) VALID-RUBY-NULLONLY -- Ruby's ORM validate() was NULL-ONLY (length/type/format declared but never enforced), now enforces required+length+range+format+numeric-type via new field-DSL options, and the dead zero-caller validate_fields is DELETED; (4) VALID-NODE-PUT-NOVALIDATE -- Node's AutoCrud PUT did no validation, now validate(body,fields,isUpdate=true) 422s an invalid update while the partial-update mode lets a valid partial update through (isUpdate wired into baseModel.save() too), proven over a real socket; (5) VALID-TWO-MESSAGES -- the request Validator and the ORM validator now speak ONE canonical vocabulary (the Node request-Validator wording, field-prefixed with a space: "name is required", "name must be at most N characters", "code does not match the required format", in_list as compact JSON) across BOTH validators in all four. BREAKING (all correctness fixes): Ruby now rejects previously-accepted over-length/wrong-format input; Node AutoCrud PUT now 422s previously-accepted invalid updates; the unified vocabulary changes some strings (PHP colon removed, Node ORM regex wording, Python/Ruby in_list JSON). Only Node auto-wires AutoCrud request validation; Python/PHP/Ruby AutoCrud request-validation is feature-27 scope (reported, not widened). ZERO new runtime deps. Mutation-proven: disable Ruby max_length -> the over-length + max-length-message cases RED; remove Node's PUT validate -> the invalid PUT writes through (200) RED; stub PHP validate() -> 10 enforcement+message cases RED; all restore GREEN. 2026-08-12) || Soft delete | 20 | `softdelete_contract.json` | 6 | 6 | 0 | SOFTDEL-DEC-01, SOFTDEL-DEC-02 | yes (real SQLite + real PostgreSQL :55432, no mocks; SOFTDEL-DEC-02 create_table() now INJECTS the is_deleted flag column (INTEGER 0/1 default 0) for a soft-delete model that never declared it -- the main fix, in ALL FOUR: Python/PHP/Node inject the literal is_deleted, Ruby injects the CONFIGURABLE soft_delete_field (the audit scoped Ruby OUT; VERIFIED it had the same gap and fixed it too). SOFTDEL-PY-FILTER-MAPPING (Python-only): a `_soft_delete_column`/`_soft_delete_filter` helper routes every finder filter AND the delete()/restore() writes AND the injection through field_mapping, so a model that remaps is_deleted filters+writes on the mapped column (behaviour IDENTICAL for an unmapped model). SOFTDEL-PHP-RESTORE-UNTESTED: PHP's restore()/withTrashed() had NO tests -- now covered four-way (un-delete makes the row reappear with the flag cleared; trashed-inclusive read). SOFTDEL-FORCE-WAS-PHP (record correction, NO code change): the throw-instead-of-delete bug was PHP-ONLY (an undefined `$whereParams`); Python/Ruby were ALREADY correct (test-only lock-ins), Node's fix + composite-PK hardening had landed -- at HEAD force_delete works and is regression-tested four-way (the 'gone even from with_trashed' case asserts raw COUNT=0). NO MOCKS: row presence/absence is asserted by querying the real table (a soft-deleted row is COUNT=1 raw but absent from the finders; a force-deleted row is COUNT=0); under TINA4_REQUIRE_SERVICES a postgres skip is a hard failure. Breaking (additive): generated DDL gains an is_deleted column for soft-delete models. Mutation-proven: revert the injection -> the create-table-injects case RED; make force_delete a soft UPDATE -> the with_trashed case RED. 2026-08-12) |
| Declarative ORM relationships | 21 | `relationships_contract.json` | 6 | 6 | 0 | REL-DEC-01, REL-DEC-02 | yes (real SQLite + real PostgreSQL :55432, NO mocks; REL-DEC-01 read-side-only: NO framework emits a DB FK / ON DELETE clause (PHP/Ruby/Node were already read-side-only -- code wins over the audit, verified not "fixed"; Firebird no-FK rule), so deleting a parent leaves children in the DB; Python's phantom `on_delete=` param that silently did nothing is DROPPED -- declaring it now RAISES a clear read-side-only error (the other three never had the option). REL-DEC-02 fix bundle four-way: (1) REL-NODE-AUTOWIRE-DEAD (the big one) -- Node's DOCUMENTED declarative FK auto-wire produced NO accessors and had NO lazy load (post.author/author.posts missing, include did not populate, the eager grouping also mismatched an FK that round-tripped as \"1.0\" against an INTEGER pk); Node now attaches belongsTo/hasMany/hasOne LAZY accessors on BOTH sides (reusing the imperative path + the cross-model registry -- no parallel system) and normalises the eager join key, so declarative relationships FUNCTION and match Python/PHP/Ruby (toDict serialises a relation once loaded -- a sync serialiser cannot await a lazy read); (2) REL-SOFTDELETE-TRAVERSAL -- the `is_deleted = 0` filter is applied to lazy AND eager traversal in all four (Python's lazy + all four's eager did not filter; PHP's lazy already did via where()), so a soft-deleted child is COUNT=1 raw but ABSENT from parent.children; (3) REL-EAGER-UNBOUNDED -- the eager `WHERE fk IN (...)` list is CHUNKED (<=500 parents/query) so a huge parent set never builds an unbounded IN, and the lazy has_many is PAGED/uncapped (Python's silent 1000-cap + PHP/Ruby's 100-cap gone -- 1001 children all reachable); (4) REL-PHP-EAGERLOAD-STATIC -- the public static ORM::eagerLoad($rows,$include) with no $db called the INSTANCE getDb() statically and fatalled; it now resolves via the static resolveDb() (and the eager path merges the FK registry so an FK-wired has-many resolves through eager, matching __get). NO MOCKS: the no-N+1 case asserts ONE query per relation via a REAL executed-statement counter instrumenting the real adapter's fetch; the tail case inserts 1001 real children and reads them all back; presence/absence by raw COUNT. Breaking (additive/correctness): soft-deleted children no longer returned through traversal; Python `on_delete` warns->raises instead of silent no-op; Node declarative relationships now function + lazy-load. Node FK column stays TEXT-affinity (the \"1.0\" round-trip is a feature-18 field-coercion detail, flagged not widened -- the join-key normaliser makes traversal correct on every engine). ZERO new runtime deps; net new code is the Node lazy accessors + join-key normaliser + per-language soft-delete/chunk/paging. Mutation-proven: neuter the lazy soft-delete filter -> soft-deleted-child-excluded RED (Python/Ruby); revert PHP's static resolveDb -> public static eagerLoad ERRORS 'getDb() cannot be called statically'; no-op Node's _wireRelationshipAccessors -> lazy-attribute-access RED (declarative relationships dead again). 2026-08-12) |

| Imperative ORM relationships | 22 | `imperative_relationships_contract.json` | 3 | 3 | 0 | IMPREL-DEC-01, IMPREL-DEC-02 | yes (real SQLite + real PostgreSQL :55432, NO mocks; IMPREL-DEC-01 per-language IDIOM -- Python/PHP/Node keep their distinct instance-method API (has_one/has_many/belongs_to taking a related CLASS + fk); Ruby has NO separate builder, its imperative form is the DECLARATIVE class method INVOKED AT RUNTIME, and the owner's call was explicit: do NOT force a new API onto Ruby -- so the Ruby runner defines the relationship at runtime and Ruby's framework code is UNCHANGED. IMPREL-DEC-02 fixes in the three distinct-API languages: (1) IMPREL-NODE-ORPHAN -- Node's imperative hasOne/hasMany/belongsTo wrote the loaded relation onto this[tableName] but NEVER populated _relCache (which toDict reads), so an imperatively-loaded relation -- stored under the TABLE name -- was ORPHANED from serialization when the table name differs from the lowercased model name (model ImpRelPostNode, table imp_rel_post); the imperative methods now also populate _relCache[relKey] so an imperatively-loaded relation serializes; (2) IMPREL-PHP-PARALLEL -- PHP had TWO parallel impls of the three relationship queries (public imperative hasOne/hasMany/belongsTo + private hasOneMethod/hasManyMethod/belongsToMethod); DE-DUPLICATED onto the ONE public implementation (the three private methods DELETED, the declarative _loadRelationship now calls the public methods), and the public imperative methods -- which had NO test -- are now covered; net PHP LOC DOWN (ORM.php: 89 lines removed, 45 added = net -44); (3) IMPREL-PY-CAP -- the imperative has_many silently capped at 100 while the lazy path pages the whole set (feature 21), so the same relationship returned a different count by access path; unified to page/uncapped by default in Python, PHP AND Node (the same silent-100-cap existed on all three imperative has_many), an explicit limit still pages. NO MOCKS: 150 real children (> the old 100 cap) read back through both paths; serialized shape asserted against real rows; positive AND negative each case (no-include omits the relation; explicit limit still pages; dangling-FK belongs_to returns null). Breaking (correctness, pre-3.14.0): Node toDict now includes an imperatively-loaded relation (was omitted); the imperative has_many default cap changes from a silent 100 to the whole set (Python/PHP/Node). Mutation-proven: remove Node's imperative _relCache population -> imperative_relation_serializes RED (serialized=0); reintroduce the 100-cap in Python/PHP/Node -> imperative_and_lazy_same_row_count RED (imperative 100 vs lazy 150); Ruby unchanged (its cases assert the runtime-invoked class method already returns the full set -- parity, not a fix). ZERO new runtime deps. 2026-08-12) |
| ORM scopes | 23 | `scopes_contract.json` | 3 | 3 | 0 | SCOPE-DEC-01, SCOPE-DEC-02 | yes (real SQLite + real PostgreSQL :55432, NO mocks; SCOPE-DEC-01 FIX (PHP-only, the real bug) -- PHP's `$_scopes` was declared ONCE on the abstract ORM base and never redeclared per subclass, so PHP's static-property inheritance resolved EVERY model's `static::$_scopes` to the SAME shared array, keyed only by scope NAME: `User::scope('active','active=1')` then `Product::scope('active','discontinued=0')` silently OVERWROTE the shared entry, so both models' generated method ran the LAST-registered filter against their OWN table (a mismatched-column query, or silently wrong rows if the columns happened to overlap). Fixed by keying the ONE shared array by the calling class -- `static::$_scopes[static::class][name]` -- no change to how models are declared, no new redeclaration burden on every subclass. Python (a `@classmethod` binds `cls` to the actual caller and `setattr`s the generated method onto THAT class), Ruby (`scope` runs inside `class << self` on the base, but a class method's implicit receiver is the CALLING class, so `define_singleton_method` lands on the caller's OWN singleton class) and Node (a static method's `this` is the calling class, so the generated method is assigned directly onto it) were ALREADY per-class -- verified by reading the source AND by the collision case passing UNMODIFIED on all three; reported, not fixed. SCOPE-DEC-02 (feature doc, optional -- NOT separately ratified in OWNER-DECISIONS.md Batch 4, which lists only the SCOPE-DEC-01 fix, so the ledger's silence on compose/rebind/global-scope IS the decision): scopes stay TERMINAL LISTS -- `Model.scope(name, sql, params)` still registers a method that runs ONE fixed query and returns a materialized list; QueryBuilder remains the composition path and soft-delete the only always-on filter; no code change in any language for this half. Three invariants, all real-row proven: (1) two models registering the SAME scope name with DIFFERENT filters both return EXACTLY their own model's rows -- the collision case; (2) a scope respects the soft-delete filter because it runs through the shared `where()` finder -- a soft-deleted row is COUNT=1 raw but excluded from the scope result; (3) a scope pushes `limit`/`offset` to the database -- an explicit smaller limit is honoured exactly and two pages of the same 15-row scope (limit 5 offset 0 / limit 5 offset 5) are DISJOINT primary-key sets, proving offset reaches the DB rather than being silently discarded (the reconciled 2026-07-28 Ruby finding, now locked in four-way so it can never regress in any language). ZERO new runtime deps; PHP net -2 LOC (`Tina4/ORM.php`: -13/+11 -- a stale duplicate docblock deleted, the actual behaviour change is 3 lines: the `scope()` write and the two `__callStatic` reads, now keyed by `static::class`). Mutation-proved: reverting the `[static::class]` key back to a bare `[name]` key reproduces SCOPE-PHP-COLLISION -- `ScopeUserPhp::active()` runs Product's `discontinued = ?` filter against the `scope_users` table and RAISES "no such column: discontinued" on SQLite, turning `two_models_same_scope_name_return_different_rows` RED; restoring the fix returns it GREEN. 2026-08-12) |
| Mail redirect to a dev list | 88 (new) | `mail_redirect_contract.json` | 2 | 2 | 0 | MAIL-DEC-01 | yes (real GreenMail SMTP :3025 / IMAP :3143, NO mocks; owner-directed addition (Andre, 2026-08-12), not a re-audit: `TINA4_MAIL_REDIRECT_TO` is a SECOND, independent knob on `send()`'s real-SMTP path, alongside the existing `TINA4_MAIL_CAPTURE` dev-capture gate. Parse rule identical all four: comma-split, trim each address, drop blanks; empty/unset is a complete no-op. PRECEDENCE: `shouldCapture()` (capture truthy, or no SMTP host) is checked FIRST and short-circuits before the redirect code ever runs, exactly as before this feature existed. On the real-send path, a non-empty list REPLACES every recipient (To becomes the full redirect list, Cc/Bcc cleared so no original recipient -- direct, cc'd or bcc'd -- gets a copy) and preserves the originals (To+Cc+Bcc, comma-joined) in a new header, `X-Tina4-Original-To`; subject/body/attachments and `send()`'s return shape are unchanged. Small rewrite at the top of the real-send branch in all four (Python `tina4_python/messenger/__init__.py`, PHP `Tina4/Messenger.php`, Ruby `lib/tina4/messenger.rb`, Node `packages/core/src/messenger.ts`), reusing each framework's existing recipient-list handling and custom-header plumbing. ZERO new runtime deps. Proven against a REAL GreenMail on the .99 lab: a send to a real recipient with the redirect list set is fetched via real IMAP from BOTH dev mailboxes (one address padded with a leading space to prove the trim) and is absent from the real recipient's mailbox after a full poll; the fetched message carries `X-Tina4-Original-To` equal to the real recipient; the unset/negative control confirms normal delivery when redirect is off; and `TINA4_MAIL_CAPTURE` + a REAL SMTP host + the redirect list together prove capture wins -- nothing reaches GreenMail (dev list AND real recipient all empty) and the local DevMailbox holds exactly 1 captured message. All four green 3/3 on first measurement. Mutation-proved: the recipient-rewrite temporarily short-circuited to a no-op in each framework in turn turns `redirect_delivers_to_every_address_on_the_list` RED (the dev-list poll times out because the mail went to the real recipient instead); restoring returns it to green. Breaking: none -- purely additive, unset preserves current behaviour exactly. 2026-08-12) |
| ORM instance loading / hydration | 26 | `instance_loading_contract.json` | 5 | 5 | 0 | LOAD-DEC-01, LOAD-DEC-02 | yes (real SQLite + real PostgreSQL :55432 tina4/tina4, NO mocks; LOAD-DEC-01 the highest-value fix -- LOAD-PY-REVALIDATE: Python's hydration (`_populate`) reused `Field.validate()`, the SAME method `save()` uses, so a READ RE-ENFORCED write-path business constraints (required/length/range/regex/choices) -- a stored row that violated a constraint, or one TIGHTENED after the row was written, raised `ValueError` out of `cls(row)` and ABORTED THE WHOLE `select()`, not just the offending row. Fixed by splitting `Field` into a new `coerce()` (type coercion + JSON parse only) used by `_populate`, and the unchanged `validate()` (adds business-constraint enforcement) used only by `save()`/`ORM.validate()`/AutoCrud's PUT handler -- a behaviour-preserving refactor for the write path (shared `_coerce_type`/`_resolve_callable` helpers), read-only for hydration. LOAD-RUBY-ASYMMETRY + LOAD-RUBY-SIGNATURE (Ruby): `from_hash` (the finder path) JSON-decoded json columns but the instance `load()` fed the raw driver value straight to the setter -- the SAME row read via `load()` vs `find()` returned two different types for one column. Fixed: `load()` now hydrates via `from_hash` (ONE hydration path, the divergent raw-setattr loop DELETED) and its signature is aligned to `load(filter, params, include:)` matching Python/PHP exactly -- BREAKING: the old `load(id)` scalar-primary-key shortcut is REMOVED (it built the valid-but-wrong SQL fragment `WHERE <id>`, matching the table's first row rather than raising; a non-nil non-String filter now raises `ArgumentError` naming the fix). LOAD-NODE-SERIALIZE-OMIT (Node, LOAD-DEC-02): `toDict()`/`toJson()` silently omitted a relation requested via `include` but never eager-loaded (a synchronous serializer cannot lazy-load it) -- now calls `Log.warning()` naming the model, the relation and the fix; still never throws and the shape is unchanged (Node-scoped -- Python/PHP/Ruby relationships are lazy descriptors with nothing to omit). LOAD-JSON-ONLY (LOAD-DEC-02, the scalar read-coercion contract): PINNED as JSON-only per OWNER-DECISIONS.md Batch 5 ("pin the scalar read-coercion contract (JSON-only today)") -- only JSON columns parse to a native object on read in PHP/Ruby/Node (non-JSON scalars stay driver-typed, unchanged); Python's own separate, pre-existing scalar coercion (str->datetime, int/bool normalising) is TYPE COERCION not a business constraint and is untouched -- no scalar-coercion code changed in any of the four. Found DURING this pass, same symptom as LOAD-PY-REVALIDATE via a different mechanism: LOAD-PHP-NONNULLABLE-HYDRATE -- PHP's `$fields` business-constraint overlay was never consulted by `fill()` (no LOAD-PY-REVALIDATE twin there), but PHP's OWN type system did the same damage: the documented required-field idiom is a NON-NULLABLE typed property (`public string $name = '';`), and `fill()`'s plain assignment THREW an unhandled `TypeError` on a stored NULL, aborting `find()`/`all()` -- empirically reproduced against a real SQLite table before this pass touched the code. Fixed: `fill()` now catches ONLY a null-into-non-nullable `TypeError` and leaves the property at its class default (any OTHER `TypeError` -- a genuinely un-coercible non-null value -- still propagates); `#165`'s assignment-tracking moved to AFTER a successful set so a skipped assignment is correctly not recorded as caller-touched. Already-correct, reported not fixed: Ruby's dynamic typing and Node's erased TypeScript types were never at risk of a required/length/range read-time crash; Node's `load()` already delegated to `selectOne()` (one hydration path) and already had the `(filter, params, include)` shape. `partial_select_yields_partial_instance` is a pre-existing-behaviour regression lock (unselected fields keep their class defaults), not a fix. Mutation-proved in all four, 2026-08-12: reinstating Python's `field.validate()` call in `_populate` reproduces the exact `Field 'name' is required` `ValueError` out of hydration; reverting Ruby's `load()` to the raw setattr loop reproduces `expected a native Hash, got String`; reverting PHP's `fill()` try/catch reproduces the exact `Cannot assign null to property LoadContractItemV2::$name of type string` `TypeError`; reverting Node's `toDict()` warning call leaves no warning text captured while the relation is still (correctly) omitted. All four restored green after each proof. Maintainability: Python's read path is SIMPLER (`coerce()` carries no constraint branches vs `validate()`'s five); Ruby COLLAPSED to one hydration path (the divergent setter loop in `load()` deleted). ZERO new runtime deps. 2026-08-12) |
| Automatic CRUD from models | 27 | `autocrud_contract.json` | 6 | 6 | 0 | CRUD-DEC-01, CRUD-DEC-02 | yes (real server boot + real HTTP + real SQLite, NO mocks -- Python uvicorn child process, PHP `php -S` via the existing `TestServer` helper, Ruby `Tina4::TestClient` (dispatches through the SAME `RackApp#call` + `enforce_route_auth` a live socket uses -- this repo's own established no-mock pattern for exactly this class of test), Node real `node:http` via `startServer()` with a model discovered from a real `src/models/` dir; secure-by-default writes were ALREADY correct in all four (verified, not fixed) -- the audit's two real defects: CRUD-DEC-01 the invalid-create/update status was WRONG/inconsistent (Python 400, PHP HTTP 500 reading a stale/unrelated `$db->error()` when no DB call ever ran, Ruby HTTP 500 from `create()` returning the literal `false` then `.persisted?` raising `NoMethodError`, Node already 422) -- fixed to a consistent 422 with FIELD errors (from the model's own validation, never a DB error) in all four: PHP's `AutoCrud.php` now calls `$model->validate()` BEFORE `save()` and reads `$model->getError()` (save()'s own recorded cause) instead of the adapter's bare `error()`; Ruby's POST switches from `model_class.create(attrs)` + `.persisted?` to `model_class.new(attrs)` + `save()` -- the SAME safe pattern its own PUT handler already used; Python's per-field PUT loop is hardened so a type-coercion failure is 422 not an uncaught exception, and both handlers now check `save()`'s `False` return instead of a dead except-that-never-fires (save() never raises, only returns False). PUT validation was ALREADY functionally correct in Python/PHP/Ruby (load-then-overlay-then-validate naturally partial-updates with no separate isUpdate flag: an untouched required field keeps its already-valid loaded value) and in Node (feature 19, VALID-NODE-PUT-NOVALIDATE) -- reconfirmed, not re-fixed. CRUD-DEC-02 the write body was NOT column-allow-listed -- a client could set `is_deleted` (soft-delete's guarded flag) via a POST/PUT body, and on Python/PHP a client-supplied auto-increment PK on create could flip `save()`/`insert()` onto its update branch and silently overwrite an unrelated existing row (or claim/rename a row's own identity on Ruby/Node) -- fixed with a small allow-list/deny-list filter at each AutoCrud handler boundary (no ORM/Router changes): `is_deleted` is ALWAYS guarded; the primary key is stripped from the body on every UPDATE (the row is addressed by the URL `{id}` alone -- confirmed by reading each save()/update() path that a body PK would otherwise redirect the WHERE clause or rename the row's own identity column) and on CREATE except a genuinely natural (single-column, non-auto-increment) key, which stays client-suppliable as documented (each language's own Swagger-example builder already keeps such a key in the sample body). PHP's guard is deliberately a DENY-list of just `is_deleted` + the PK, not a strict allow-list of declared typed properties -- PHP supports fully dynamic (undeclared-property) models by design (`ORM::getModelProperties()` explicitly merges `__set()`-captured dynamic properties), and the existing `AutoCrudV3Test`'s own `CrudItem` fixture declares NO typed properties at all; MEASURED directly that a strict allow-list broke it (`testCreateItem`/`testUpdateItem`/`testCreateMultipleItems` went red, NOT NULL failures) before the design was corrected. Proven against a real soft-delete-enabled model that explicitly DECLARES `is_deleted` as a field (the worst case for the guard): a POST body of `{name, is_deleted:1, id:999999}` returns 201 with a FRESH id (never 999999) and a raw SQL read shows `is_deleted=0`; a follow-up PUT of `{is_deleted:1}` alone is a benign 200 no-op, still `is_deleted=0`. Six named cases (tokenless_write_returns_401, valid_authenticated_post_returns_201, invalid_post_returns_422_with_field_errors, invalid_put_is_rejected, mass_assignment_is_blocked, list_is_the_seven_key_envelope -- the last reconfirms the ADR-0043 7-key envelope on the AutoCrud list route specifically). Framework-source LOC delta (handler files only, small + surgical as intended): Python `crud/__init__.py` +76/-12, PHP `AutoCrud.php` +90/-4, Ruby `auto_crud.rb` +45/-4, Node `autoCrud.ts` +54/-2. ZERO new runtime deps. Breaking (correctness + security-hardening fixes): an invalid create/update now returns 422 where PHP/Ruby returned 500 (a correctness fix -- 500 was never the intended contract); the write body is allow-listed -- previously-accepted `is_deleted` and a client PK on create are now rejected/stripped (closes a mass-assignment hole). Mutation-proved on the .99 lab, all four individually: removing the `is_deleted` guard turns `mass_assignment_is_blocked` RED (the row's `is_deleted` really lands as 1 -- on Python/Node, whose test model also has soft-delete enabled, the row additionally vanishes from the list total, a second RED signal from the same mutation); forcing the write-security flag open turns `tokenless_write_returns_401` RED in every framework; each mutation restored to green before moving on. Verified 2026-08-12 against the .99 Linux lab in one consolidated run: `results: python=0 php=0 ruby=0 node=0`.) |
| Seeder and fake data | 28 | `seeder_contract.json` | 5 | 5 | 0 | SEED-DEC-01, SEED-DEC-02 | yes (real SQLite + real PostgreSQL :55432 + real MSSQL :1433 + real Firebird 5 :3050, NO mocks; SEED-DEC-01 (required) FIX -- SEED-PHP-BACKTICK: PHP's `seedTable` quoted identifiers with BACKTICKS (MySQL/SQLite only) so every INSERT raised a syntax error on PostgreSQL/Firebird (double-quote) and MSSQL (brackets) -- the dev-admin `POST /__dev/api/seed` endpoint delegates to `seedTable`, so dashboard seeding was BROKEN on those three engines; fixed by routing PHP's `seedTable` through the PARAMETERIZED `Database::insert()` adapter path (the same one `seedOrm` already used via `ORM::save()`), deleting the raw-SQL backtick-building code entirely. SEED-TABLE-SEED-INERT (universal) -- `seed_table`'s `seed` argument was a silent no-op in all four (it has no generators of its own to seed -- `field_map`/`columns`/`fieldMap` callables are opaque); the ledger explicitly ratified REMOVAL over threading a seeded FakeData through it (same principle as the no-op `ForeignKeyField` `on_delete`, feature 21) -- `seed_table`'s seed parameter now RAISES/THROWS on a non-default value in all four (Python `ValueError`, PHP `InvalidArgumentException`, Node `Error`; Ruby drops the keyword entirely so passing it raises Ruby's own `ArgumentError`), and determinism is achieved by the caller building their own seeded FakeData and closing over it in `field_map`/`columns`/`fieldMap` -- exactly the pattern `seed_orm`/`seed_models` already use internally (measured-then-fixed: Ruby's `seed_table` was, uniquely, NOT fully inert for the plain type-symbol columns form -- an internal FakeData WAS threaded by the old `seed:` kwarg on that one path -- but is unified anyway for consistency with the ratified decision and because the partial behaviour was a footgun that silently stopped working the moment a caller supplied their own generator callable). SEED-DEC-02 (low) fixes: Node's `forField()` always returned a field's declared default (the code's own comment promised "sometimes" -- dead) so every seeded row got an IDENTICAL value for a defaulted field, now used only PROBABILISTICALLY (a coin-flip on the same instance PRNG, still reproducible under a seed); Ruby's `FakeData#boolean` returned `0`/`1` (Integer) instead of a native `true`/`false`, fixed (with `_validate_types`'s boolean type-check updated to match -- it used to special-case Integer-as-boolean, now checks for a real boolean); Ruby's `seed_orm` idempotency short-circuit used to run UNCONDITIONALLY, silently skipping an explicit seed request whenever the target table already held >= count rows -- even rows UNRELATED to this seeder -- returning `seeded=0` with only an INFO log; it is now OPT-IN via `idempotent: true` (default `false`), matching Python/PHP/Node, which never had this behaviour. SEED-DETERMINISM-PERLANG (the RNG is per-language -- Python Mersenne Twister, PHP per-instance Mt19937, Ruby `Random`, Node mulberry32 -- so the SAME seed does NOT reproduce across languages, only within one) and SEED-SECRETS-DOC (the PRNG is NON-CRYPTOGRAPHIC, never for secrets/tokens) documented on the FakeData class docblock in all four; SEED-VOCAB-PARITY pins 27 generators (`name`/`first_name`/`last_name`/`email`/`phone`/`address`/`city`/`country`/`zip_code`/`company`/`job_title`/`sentence`/`paragraph`/`text`/`word`/`integer`/`numeric`/`boolean`/`date`/`datetime`/`uuid`/`url`/`color_hex`/`currency`/`ip_address`/`credit_card`/`choice`/`for_field`, idiomatic spelling per language) present and gated in all four. `seed_table_inserts_on_every_engine` is the direct regression guard for SEED-PHP-BACKTICK, proven on real PostgreSQL + MSSQL + Firebird (not SQLite-only); the other four invariants are engine-agnostic seeder-logic properties (RNG determinism, FK topo-order, failure counting, generator presence) proven on real SQLite + real PostgreSQL, since they route through the already adapter-contract-proven `insert()`/`ORM.save()` paths (features 9/10/11/12) and re-proving those a second time here would be redundant. LATENT NODE BUG found by the SAME lab run: Node's `seedTable()`/`seedOrm()` hand-built their own INSERT SQL with a hardcoded double-quote around every identifier -- safe on PostgreSQL/MSSQL/SQLite, but Firebird folds an UNQUOTED `CREATE TABLE` name to UPPERCASE, so the quoted lower-case INSERT target became a different, unfindable identifier ("Table unknown", SQL error -204) -- the identical bug CLASS as SEED-PHP-BACKTICK, undetected until this fixture exercised real Firebird. Fixed by adding `adapterInsert()` (`database.ts`, exported from `index.ts`) so both functions route through the adapter's own native `insert()`/`insertAsync()` (feature 3's shared `buildInsert()`/`Dialect`, which already quotes Firebird "only when it has to"), deleting the hand-rolled column-list/placeholder code; `adapterInsert()` also converts `SQLiteAdapter.insert()`'s non-throwing `{success:false}` return into a throw so `seedTable`/`seedOrm`'s per-row catch still counts SQLite failures correctly (a gap introduced by the new helper itself, caught by the existing P1 error-handling suite before push and fixed in the same commit). Framework-source LOC delta (behaviour files, numstat, docblock/comment additions included): Python `seeder/__init__.py` +52/-6; PHP `FakeData.php` +53/-18 and `DevAdmin.php` +5/-4 (the ~12-line raw-SQL backtick-building block was deleted and replaced by a single `insert()` call; the file grew on balance because the class/method docblocks were substantially expanded to document the two universal boundaries and the removed-seed-parameter behaviour -- not from new executable logic); Ruby `seeder.rb` +68/-19 and `dev_admin.rb` +13/-4; Node `seeder.ts` +43/-25, `database.ts` +33/-0 (new `adapterInsert` helper), `index.ts` +1/-1, `fakeData.ts` (orm) +6/-2, `fakeData.ts` (core) +15/-0 docblock-only, `devAdmin.ts` +4/-1. ZERO new runtime deps in any language (PHP reuses `Database::insert()`; Ruby reuses `Random`; Node reuses its own `insertAsync()`/`insert()` and the existing instance PRNG via `this.boolean()`). Mutation-proved on the .99 lab, then restored to the exact pushed commit before the final confirming green: reinstated PHP's backtick-quoted raw SQL in `seedTable` -> `seed_table_inserts_on_every_engine` went RED on PostgreSQL/MSSQL/Firebird with the real syntax-error-at-the-backtick message on each (SQLite stayed green); broke Python `seed_orm`'s seed threading (`fake = FakeData(seed=seed)` -> `FakeData(seed=None)`) -> `seeded_run_reproduces_identical_rows` went RED (two runs' rows diverged). Breaking (correctness fixes, pre-3.14.0): `seed_table`'s `seed` parameter is removed/raises in all four (it was already non-functional in three of four, and partially footgun-prone in the fourth); Ruby `FakeData#boolean` now returns a native boolean (was `0`/`1`); Ruby `seed_orm`'s idempotency skip is now opt-in via `idempotent:` (was unconditional). Verified 2026-08-12 against the .99 Linux lab in one consolidated run: `results: python=0 php=0 ruby=0 node=0` (PostgreSQL :55432, MSSQL :1433, Firebird 5 :3050, SQLite). Two pre-existing, OUT-OF-SCOPE defects surfaced incidentally and were NOT fixed here (flagged as separate follow-ups): Node's `Database.insert()` facade does not fail loud on a SQLite constraint violation (silently returns `{success:false}` instead of throwing, unlike `execute()`/`fetch()` -- feature 3/9 territory); Ruby's Firebird driver returns String-keyed `fetch()` rows where SQLite/PostgreSQL/MSSQL return Symbol keys (feature 12 territory, worked around with `row[:x] || row["x"]` in the new spec only). 2026-08-12) |
| HTTP request model | 29 | `request_contract.json` | 4 | 4 | 0 | REQ-DEC-01, REQ-DEC-02 | yes (real front-controller dispatch, NO mocks -- Python `TestClient`/`core.server.app`, PHP `TestClient`/`Router::dispatch`, Ruby `TestClient`/`RackApp#call`, Node `TestClient`/real `node:http` server for the IP case (matches `test/trustedProxy.test.ts`'s own gold-standard pattern); REQ-DEC-01 (required, security + BREAKING) THE fix -- REQ-PARAM-POLLUTION: `request.params` is now ROUTE-ONLY in all four. Python's `params` was query+route merged (`params = dict(query)` then route `.update()`d in -- a same-name collision already favoured route, but any OTHER client-controlled query key rode along in `params` under a route-derived-looking namespace); Ruby's `params` merged query+BODY+route the same way (`build_params`, deleted outright). PHP/Node were already route-only (the reference). Fixed: Python's `from_scope()` no longer seeds `params` from `query`, and the old `merge_route_params()` (merge-into-existing-dict) is renamed `attach_route_params()` and now OVERWRITES (`self.params = dict(self._route_params)`); Ruby's `path_params`/`path_params=` are renamed `params`/`params=` (REQ-ROUTE-PARAM-NAME -- unifies the accessor name with Python/PHP/Node, all three already `params`), wrapped in `IndifferentHash` so existing STRING- and SYMBOL-keyed call sites both keep resolving (`Route#match_path` captures path-param names as symbols; only `build_params`'s own `.to_s` conversion made string-keyed reads work before, so simply aliasing the raw hash would have silently broken every `request.params[\"id\"]`-style call site -- caught before it shipped). CROSS-CUTTING FALLOUT fixed in the same pass (every internal call site that read `request.params` expecting a QUERY value, now correctly reading `request.query`): Python `crud/__init__.py` (AutoCrud list pagination), `dev_admin/__init__.py` (24 call sites -- every `/__dev/api/*` filter/pagination read), `realtime/__init__.py` (`_form_value`'s no-route-segment fallback), `cli/__init__.py` (the `tina4 generate route` list-endpoint SCAFFOLD TEMPLATE TEXT -- was shipping a broken pagination reader into every new project), `gallery/auth/.../gallery_auth.py` (a real query-param bug caught in shipped example code); Ruby `graphql.rb` (GraphiQL-over-GET `?query=`/`?variables=`), `cli.rb` (the same scaffold-template bug), `gallery/auth/.../gallery_auth.rb` (the same shipped-example bug), `realtime.rb` (`form_value`'s now-permanently-dead route-segment fallback, removed). SECURITY regression caught and fixed in the SAME pass, not a separate finding: Python's CSRF `CsrfMiddleware` read `getattr(request,\"params\",None) or getattr(request,\"query\",None)` to reject a form token smuggled into the query string -- once `params` is route-only, on any PARAMETERIZED write route (e.g. `PUT /api/orders/{id}`) `params` is a non-empty, truthy dict, so the `or` fallback would have silently STOPPED reaching `query` and the query-string-token rejection would go dark exactly on parameterized routes; fixed by reading `query` directly, never `params`. Python's separately-broken `AuthMiddleware.before_request` (assigned the never-declared `request.auth`, raising `AttributeError` on every SUCCESSFUL authentication against the real class, hidden only by its own mock-based test) is fixed alongside REQ-PY-NO-USER below since it is the same field. Malformed/empty-body result (REQ-BODY-DIVERGE) pinned to the Python/PHP/Node 3-of-4 majority (same tie-break method this ledger already used for filter defaults, Batch 4): malformed JSON -> the RAW STRING (Ruby changed from `{}`, `json_body`'s own Hash-only/{}-on-failure convenience method is UNCHANGED for its own direct callers -- only the parity-facing `request.body` path changed); no-body -> each language's own native absence value (`None`/`null`/`nil`/`undefined`), checked BEFORE any content-type-specific parsing in all four now (Ruby's `parse_body` gained the same early return Python/PHP already had; `#body`'s own memoisation moved off `\|\|=` to an explicit computed-flag because `nil` is now a legitimate answer, not \"not yet computed\"). REQ-DEC-02: Python gains a mutable `user` field (REQ-PY-NO-USER, added to `__slots__`, set by the secure-by-default auth gate `_check_auth` AND by the fixed `AuthMiddleware.before_request` above) -- PHP/Ruby/Node already had it (Ruby's wiring reconfirmed working, not re-fixed). Immutability (REQ-IMMUTABILITY-DIVERGE) reconciled to the PHP `readonly` / Ruby writer-less-`attr_reader` reference (BOTH already correct -- reported, not fixed): Python blocks reassignment of its 14 core wire-derived fields once `from_scope()` completes (`__setattr__` guard keyed on a `_frozen` flag flipped at the end of construction; `params`/`session`/`user`/`_`-prefixed dispatch fields stay writable); Node marks the matching 8 `Tina4Request` TS interface fields `readonly` (compile-time only, via `npm run typecheck`'s monorepo-aware config -- a local `Writable<T>` mapped type lets `createRequest()` still build them exactly once, mirroring how PHP's own constructor writes its `readonly` properties). Header dash rule (REQ-HEADER-DASH-DIVERGE) reconciled to the PHP/Node reference (case-fold only, matching RFC 7230 3.2 -- no `-`/`_` substitution): Python drops its `-`->`_` remap, Ruby drops its `_`->`-` remap; `header(\"x_custom\")` no longer also matches a header literally named `X-Custom` in either language (an intentional, documented breaking narrowing). Ruby's Rack multipart dependency (REQ-RUBY-RACK-DEP) is NOTED, not changed -- Ruby's own HTTP server IS Rack, so it is not an incremental dependency the way it would be for a language with no Rack in its request path already; hand-rolling multipart here would be a maintainability LOSS for no real gain. DO NOT REGRESS, reconfirmed not touched: client-IP trust (`remote_ip` always the raw peer, `ip` honours X-Forwarded-For only from a `TINA4_TRUSTED_PROXIES` peer, identical rightmost-non-trusted-hop algorithm) -- proven in the new fixture (real dispatch; Node via a genuine `node:http` server + loopback TCP connection, matching `test/trustedProxy.test.ts`'s own established pattern) alongside the three changed properties so a future regression on ANY of the four is caught by the same fixture the breaking fix lives in; the deeper per-language suites (`test_trusted_proxy.py`/`TrustedProxyTest.php`/`trusted_proxy_spec.rb`/`trustedProxy.test.ts`) are untouched. Framework-source LOC delta (behaviour files, numstat, docblock/comment additions included): Python +141/-54 across 9 files (`core/request.py`, `core/server.py`, `core/middleware.py`, `auth/__init__.py`, `crud/__init__.py`, `dev_admin/__init__.py`, `realtime/__init__.py`, `cli/__init__.py`, `gallery/auth/.../gallery_auth.py`); PHP +0/-0 (already correct on every REQ-DEC-01/02 point -- zero functional change, confirmed by direct source reading of `Request.php`, `Router.php`, `AutoCrud.php`, `Middleware/CsrfMiddleware.php`); Ruby +91/-41 across 7 files (`request.rb`, `dispatch_pipeline.rb`, `test_client.rb` -- a real TestClient bug fixed alongside, see below, `graphql.rb`, `cli.rb`, `realtime.rb`, `gallery/auth/.../gallery_auth.rb`); Node +40/-17 across 2 files (`request.ts`, `types.ts`). ZERO new runtime deps in any language. Incidental fix surfaced by this feature's own testing, not pre-existing: Ruby's `TestClient` mapped EVERY caller-supplied header (including `Content-Type`/`Content-Length`, the two CGI/Rack headers that must NOT carry an `HTTP_` prefix) through the generic `HTTP_<NAME>` rule, so `client.post(path, body: ..., headers: {\"Content-Type\" => ...})` silently never reached `Request#content_type` -- PHP/Node/Python's TestClients never had this gap (no CGI prefix convention to special-case in ASGI/plain-object headers); fixed with a 2-case special-case in the header-mapping loop. Mutation-proved (Python, Ruby -- the only two REQ-PARAM-POLLUTION ever affected; PHP/Node were never vulnerable): reinstating each language's old query-seeded-then-route-merged `params` construction turns `route_param_not_shadowed_by_query` RED (the unrelated `extra` query key leaks into `params`); restored after in both. Breaking (security fix + parity, pre-3.14.0): Python/Ruby apps reading a query or body value via `request.params` must switch to `request.query`/`request.body` -- `params` is route-only now; Ruby additionally loses `path_params` (renamed `params`, no alias); the malformed/empty-body sentinel changed for Ruby (`{}` -> raw string / `nil`); the header-dash remap is removed in Python and Ruby (`header(\"x_custom\")` no longer matches `X-Custom`). Verified 2026-08-12 locally (Python 527 broader-suite examples green, 2 pre-existing skips, INCLUDING the 4 new contract tests in the same combined run -- a first combined-with-everything-else run caught a real cross-file test-isolation bug in the new suite itself, fixed before this count: its module-level @get/@post route registration was fragile against OTHER files' Router.clear() calls when collected together, so it now registers routes in an autouse per-test fixture instead, matching tests/test_csrf_middleware.py's own established pattern; Ruby 576 broader-suite examples green INCLUDING the 4 new contract tests [full suite 5382 examples, 72 failures -- ALL pre-existing/environment: missing local cache/DB service backends, none request/param/auth/header/routing-related]; PHP 562/563 broader-suite tests green INCLUDING the 4 new contract tests [the 1 failure pre-existing/unrelated, reproduced identically with ZERO PHP source changes]; Node 39 targeted broader-suite files run, 38 fully green (37 via `tsx` + `requestBodyCap.test.ts` separately via `vitest run`, its correct invocation) INCLUDING the 4 new contract tests [1 pre-existing/unrelated failure inside `dispatchPipeline.test.ts`'s 9 cases, confirmed via `git diff` showing zero Node changes outside `request.ts`/`types.ts` at the time, and reproduces identically in complete isolation] + `npm run typecheck` clean); PRE-EXISTING, OUT-OF-SCOPE defects surfaced incidentally and NOT fixed here (flagged as separate follow-ups, reproduce identically with zero changes from this feature): a `request_id`-in-500-page length/format assertion (`RouterErrorEventTest`/`router_error_event_spec.rb`, expects 12 hex chars, actual output is 8 -- feature-43 territory, fails identically in PHP/Ruby with unrelated source); a Node `dispatchPipeline.test.ts` source-introspection assertion (`server.ts`/`dispatchPipeline.ts` untouched by this feature) failing in complete isolation. LAB GATE (.99 Linux lab, clean-room checkout of the exact pushed commits, `results:` line read in the same run, not backgrounded): `results: python=0 php=0 ruby=0 node=0` -- Python 4/4, PHP 31/31 (RequestV3Test + RequestContractTest), Ruby 36/36 (request_spec + request_v3_spec + request_contract_spec), Node 31/31 (request.test.ts) + 4/4 (requestContract.test.ts) + `npm run typecheck` clean. Log `/home/andre/rel-3.13.99/lab-request.20260812-215313.log`. 2026-08-12) |
| Configurable error pages | 42 | `error_pages_contract.json` | 7 | 7 | 0 | ERR-DEC-01, ERR-DEC-02 | yes (real front-controller dispatch, no mocks -- Python/PHP/Ruby TestClient, Node real `startServer()` over a real socket; ERR-DEC-02 implemented Accept-based content negotiation in all four -- unimplemented before this feature everywhere -- reusing each language's existing `{error:true,code,message,status}` envelope (CSRF's own) plus `request_id`, via ONE shared decision per language (`wants_json?`/`wantsJson`/`acceptPrefersJson`) reused by every 403/404/500 call site; ERR-DEC-01 converged the 403 (ERR-403-SPLIT) onto that SAME negotiated renderer in all four -- Python's generic middleware-denial fallback and Node's `interpretHookResult` (previously a BARE 403 with NO body) now render it, and PHP's GLOBAL class-based middleware pass (`Middleware::runBefore`/`runAfter`, distinct from its already-correct per-route path) is now ALSO threaded through `Router::renderForbidden` instead of the old un-negotiated JSON fallback baked into `Middleware::applyHookResult`; overridable code-set agreed at {403,404,500} in all four (PHP additionally keeps its pre-existing, backward-compatible 405 override, a strict superset, not removed); ERR-404-REQUESTID added `request_id` to the negotiated 404 body in PHP/Ruby/Node (Python's template data already carried it). SECURITY (feature 127/57 duty): proved no 404/403/500 template reflects raw request data unescaped in any language -- found and fixed a LIVE reflected-XSS on Ruby's 403 (`Tina4::Middleware.forbid` and `RackApp#handle_403` reflected the path with no `CGI.escapeHTML`, the SAME class feature 127 already patched on Ruby's 404; Ruby's legacy `Template`/`TwigEngine` does not auto-escape `{{ }}`, unlike Python/PHP/Node's Frond, so each Ruby call site must escape explicitly) -- fixing it surfaced a SECOND, unrelated latent Ruby bug: `403.twig` ships a literal UTF-8 em-dash and `lib/tina4/template.rb`'s six `File.read`/`File.readlines` calls never forced an encoding, so on any process whose `Encoding.default_external` isn't UTF-8 (no `LANG`/`LC_ALL` -- a bare/minimal locale, common in a container, reproduced on the dev host) rendering `403.twig` raised `ArgumentError: invalid byte sequence in US-ASCII` and the OLD code's broad `rescue` silently swallowed it on EVERY 403 response on such a host, undetected because it fell back to a plausible-looking bare string; fixed to match `lib/tina4/frond.rb`, which already forces `encoding: "utf-8"`. Node has a DIFFERENT, platform-level defence verified with a raw unencoded socket write (not just a client library): `node:http`'s own URL parsing percent-encodes `<`/`>` in the request path before the framework ever sees it, so Node's contract case proves Frond's OWN `{{ }}` escaping with an apostrophe payload (which survives URL parsing unencoded, reflecting raw as `&#x27;`) rather than the literal `<script>` tag the other three reflect-then-escape directly -- both are real, verified properties of what a live request experiences on each stack. The gated CWE-209 prod-500-no-leak guarantee (`500.twig`'s `{% if error_message %}` guard, empty message in production, trace only under `TINA4_DEBUG`) is UNCHANGED and re-proven, not reopened, for both the HTML and the new negotiated-JSON 500 branch. Mutation-proved per language: disabling the Accept branch reds `json_accept_yields_a_json_error_body` (HTML returned to a JSON client, all four); bypassing escaping (Python `SafeString`, PHP `SafeString`, Ruby dropping `CGI.escapeHTML`, Node's `htmlEscape` turned into a no-op) reds `a_reflected_path_in_an_error_page_is_escaped`; both restored. BREAKING (consistency, not a security regression): a 403 now negotiates HTML-vs-JSON like 404/500 -- Python's 403 body changes from always-JSON to negotiated, Node's from a bare empty body to a real one; Ruby can now emit a JSON 403/404/500 (was HTML-only). 2026-08-13)
| Migrations | 15 | `migrations_contract.json` | 6 | 6 | 0 | MIG-DEC-01, MIG-DEC-02, MIG-DEC-03 | yes (real SQLite + real PostgreSQL :55432 + real MySQL :3306 + real MSSQL :1433 + real Firebird 5 :3050, NO mocks. MIG-DEC-01 (required) TWO real bugs, both fixed: MIG-CLI-STATUS-BROKEN -- Python's `_migrate_status` printed `m['migration_id']` but `Migration.status()` keys its dicts `migration_name` (KeyError the instant >=1 migration existed, in BOTH the completed and pending branches, never tested); PHP's `migrate:status` handler called `new Migration($migrationsDir)` -- the migrations-DIRECTORY STRING landed in the constructor's typed `?DatabaseAdapter $db` parameter (unlike `migrate`/`migrate:rollback`, which already resolved a real `$db` from `App::getDatabase()`/`DATABASE_URL` first) -- TypeError before `status()` ever ran. Both fixed with the real field/argument; Ruby/Node were measured and found already correct (not touched) and gained the identical CLI-level regression lock for parity. MIG-NODE-CLI-DIVERGENT -- Node's `tina4 migrate` was a SECOND, weaker, untested migration engine (naive `sql.split(\";\")`, no per-file transaction, no Firebird/MSSQL idempotency skips, ledger recorded outside any transaction) living alongside the real ORM `migrate()`; unified onto ONE code path, the weaker re-implementation DELETED (`packages/cli/src/commands/migrate.ts` 122 -> 83 lines, -32%). MIG-DEC-02 (required) MIG-ROLLBACK-DROPS-LEDGER: Ruby/PHP/Node `rollback()` removed the `tina4_migration` tracking row even when the schema was NOT successfully reversed -- Ruby/PHP logged a warning for a missing `.down.sql` and still deleted; Node did the same for BOTH a missing `.down.sql` AND a down statement that genuinely FAILED (the code even carried a comment justifying it). Python was the fail-safe reference all along. All three now RAISE/THROW instead, wrapped in (Ruby/Node newly gained; PHP/Python already had) their own transaction with the ledger DELETE moved inside it, so a partially-executed down rolls back too. Exposed and fixed two PRE-EXISTING test fixtures that had been silently relying on the old drop-the-ledger-anyway behaviour to stay green: PHP's `MigrationV3Test` (`testRollback`, `testRollbackRemovesMigrationRecords`, `testRollbackOnlyLastBatch`, `testRollbackClearsAppliedRecords` -- migrations with no real `.down.sql` companion, now given real ones) and Node's `migrationClass.test.ts` `rollback(steps=2)` case (two migrations sharing the literal SAME `\"DROP TABLE users\"` down script -- the second drop always failed \"no such table\" and the old bug silently deleted the row anyway; fixed by giving the second migration a down script that reverses only what it did, `DROP COLUMN` not `DROP TABLE`). MIG-DEC-03 MIG-FBMSSQL-MOCK (required, NO-MOCK VIOLATION) -- Ruby's `migration_footguns_spec.rb` used a hand-rolled `FakeDB`; Node's `migrationFootguns.test.ts` used fake `MssqlAdapter`/`FirebirdAdapter` classes whose `constructor.name` was SPOOFED to satisfy `engineOf()`'s dialect-detection switch -- a fake of that exact class name trivially \"detects\" as the real engine without ever proving the real catalogue lookup against a live server. Both mock blocks DELETED, replaced with real-engine cases proven against REAL Firebird 5 (CREATE TABLE + the Firebird-only ALTER TABLE ADD idempotency guard) and REAL MSSQL (CREATE TABLE), driven through the exact functions the runner itself calls (Node's `shouldSkipForFirebird` gained a matching `export` alongside its already-exported `shouldSkipCreateTable` sibling so it could be driven directly, no fake needed) -- matches PHP's pre-existing `MigrationFootgunsLiveEngineTest` (\"NO DOUBLES\"), the model this release aligned Ruby/Node to. MIG-AUTO-DEFAULT-ON -- ledger's exact call taken verbatim (\"keep auto-migrate default-ON with `TINA4_AUTO_MIGRATE=false` prod opt-out\"): confirmed already-true in all four by reading the boot hook source (Python `core/server.py:_auto_migrate_on_startup`, PHP `App.php:autoMigrateOnStartup`, Ruby `Tina4.auto_migrate_on_startup!`, Node `server.ts:autoMigrateOnStartup`) -- NO default changed, nothing to fix. The ledger does not separately ratify a specific \"louder than a log line\" failure-visibility mechanism for this feature (only the default itself); per the conservative instruction, none was built (a new health-check degradation signal would also cut across feature 38's already-proven `health_contract.json`, and Python's own liveness-probe design deliberately never reacts to a restart-proof condition like a bad migration file -- exactly the CrashLoopBackOff footgun a naive wiring would reintroduce) -- FLAGGED for owner confirmation rather than a silent, undirected new-surface detour. MIG-SQLITE-DOC-DRIFT -- Python's CLAUDE.md, Ruby's docstring (`lib/tina4/migration.rb`) AND CLAUDE.md, PHP's CLAUDE.md, and Node's CLAUDE.md all wrongly grouped SQLite with MySQL/Firebird's genuine auto-commit-DDL behaviour; SQLite actually has TRANSACTIONAL DDL (autocommit off inside `start_transaction`), so a multi-statement migration failing midway rolls back cleanly on SQLite too, including an already-executed `CREATE TABLE` -- corrected in all four (the audit named only Python + Ruby; the identical bug found in PHP + Node while touching this exact subsystem was fixed too, for consistency, since shipping three-of-four-corrected would leave a visible inconsistency in the same release). Framework-source LOC delta (behaviour files, numstat): Python +2/-2 (`cli/__init__.py` KeyError fix) + CLAUDE.md doc fix; PHP +32/-9 (`Migration.php` fail-safe rollback) + `bin/tina4php` +14/-1 (migrate:status `$db` resolution) + CLAUDE.md doc fix; Ruby +23/-9 (`lib/tina4/migration.rb` fail-safe rollback + transaction wrap) + CLAUDE.md/docstring fix + `spec/migration_footguns_spec.rb` -18 (FakeDB class + mock describe block deleted); Node +125/-169 net across `packages/cli/src/commands/migrate.ts` (-39 net, the deleted re-implementation), `migrateRollback.ts` (+catch/exit wrapper), `packages/orm/src/migration.ts` (fail-safe rollback + `shouldSkipForFirebird` export), `packages/orm/src/index.ts` (+1 export line), `test/migrationFootguns.test.ts` (-45 net, fake adapters deleted), `test/migrationClass.test.ts` (fixture fix) + CLAUDE.md doc fix. ZERO new runtime deps in any language. Mutation-proved (all four, restored after): reinstated Python's `migration_id` KeyError -> `migrate_status_prints_without_crashing` RED with the exact original traceback; reinstated PHP's `new Migration($migrationsDir)` argument bug -> `migrate_status_prints_without_crashing` RED with the exact original TypeError; reinstated the Ruby/PHP/Node warn-then-unconditional-delete rollback path -> `failed_or_missing_down_does_not_drop_ledger` RED (the row is deleted despite nothing being reversed) in all three; reverted Node's CLI back onto the naive `sql.split(\";\")` re-implementation -> `cli_and_orm_paths_identical` RED (the embedded `;` fragments the statement, the second `CREATE TABLE` never runs). Verified locally before the lab (SQLite-only; real-engine cases correctly SKIPPED, no local PG/MySQL/MSSQL/Firebird): Python 81 passed/10 skipped; PHP 104 passed/13 skipped; Ruby 95 examples/0 failures/7 pending; Node `migrationContract.test.ts` 24/24 + eight sibling suites all green + `npm run typecheck` clean. Breaking (pre-3.14.0, correctness fixes -- document + migrate): rollback() in Ruby/PHP/Node now RAISES/THROWS on a missing or failed down artifact instead of silently dropping the ledger row (a caller relying on the old silent-drop must add a real `.down.sql`/down function, or catch and handle the raise); Node's CLI `tina4 migrate` now takes the SAME transactional/robust-split/idempotent path the ORM always had (strictly safer, but a caller depending on the old naive-split's specific mis-splitting behaviour, if any existed, would observe a difference). REAL-ENGINE-ONLY BUGS the .99 lab caught that SQLite-only local testing could not (fixed via follow-up commits, exactly the class of finding no-mock testing exists to surface): (1) all four languages' new real-MySQL/PostgreSQL cases pre-cleaned a stale ledger row with an unguarded DELETE that raised "table doesn't exist" on a genuinely fresh database -- guarded with a best-effort cleanup helper in all four; (2) Python: MySQLAdapter.connect() opens with autocommit=False and execute() self-commits but fetch()/fetch_one() never do, so a preceding READ (table_exists()/_get_executed()/_get_next_batch()) left an implicit transaction open, and db.start_transaction() raised "Transaction already in progress" on the FIRST migration file against an already-existing tracking table -- fixed with a best-effort db.commit() in the migration runner (not the already-proven MySQL adapter) immediately before start_transaction() in both _migrate() and _rollback(); (3) Ruby: ensure_tracking_table's Firebird DDL wrote `batch INTEGER NOT NULL DEFAULT 1` / `passed INTEGER NOT NULL DEFAULT 1` -- Firebird's grammar requires DEFAULT before NOT NULL, so a real server raised "Invalid token ... DEFAULT" the moment a real Migration was constructed against it; fixed to `DEFAULT 1 NOT NULL`, matching the Python reference's Firebird branch; (4) Ruby: firebird_column_exists?'s bare `SELECT 1 FROM RDB$RELATION_FIELDS` fails once #fetch_one's limit:1 wraps it in a derived table (`SELECT FIRST 1 SKIP 0 * FROM (...)`)  -- Firebird requires every derived-table column to be named -- fixed by aliasing the literal (`SELECT 1 AS FOUND`); (5) Ruby: redundant `db.commit` calls after a bare standalone `db.execute` (no matching `start_transaction`) raised "COMMIT TRANSACTION has no corresponding BEGIN TRANSACTION" on MSSQL/tiny_tds specifically -- removed, matching `mssqlprovider_contract_spec.rb`'s own already-proven convention of never calling `.commit` after a bare `.execute`. None of these five were reachable by any SQLite-only or mocked test; all five were caught, fixed, and re-verified in the SAME lab session before the confirming green. LAB GATE (.99 Linux lab, clean-room checkout of the exact pushed commits, `results:` line READ in the same run, not backgrounded): `results: python=0 php=0 ruby=0 node=0` -- Python 93/93, PHP 104/104 (0 skipped -- a pre-existing MigrationV3Test case reading the non-standard TINA4_TEST_PG_URL var was also given it, for a true zero-skip run), Ruby 95/95 (0 pending -- all four real engines exercised), Node 37/37 (`migrationContract.test.ts`) + 8 sibling suites + vitest `migrationKindContract.test.ts` 4/4, all against REAL PostgreSQL :55432, REAL MySQL :3306, REAL MSSQL :1433 and REAL Firebird 5 :3050 (SYSDBA), no mocks anywhere. Log `/home/andre/rel-3.13.99/lab-migrations.20260812-230837.log`. 2026-08-12) |
| Default landing page | 46 | `landing_page_contract.json` | 4 | 4 | 0 | LAND-DEC-01, LAND-DEC-02 | yes (real front-controller dispatch, no mocks -- Python/PHP `TestClient`, Ruby real `RackApp#call`, Node real `startServer` + real HTTP socket; LAND-DEC-01 end-to-end-gates the dev-only/404-in-prod info-leak guard in Python + PHP too (was Ruby+Node only); LAND-DEC-02 unifies the suppression precedence [user `/` route > `pages/index.*` template > landing] -- Ruby's `pages/index.*` path was already correct [empirically verified against a real dispatch before writing the fixture]; only Ruby's SEPARATE dead `should_show_landing_page?`/`try_serve_index_template` pair [an unwired, different `src/templates/index.*` convention] needed deleting; Node's unused `renderLandingPage(routes, port)` param + the per-request `getRoutes().map()` building it were deleted too; mutation-proved in all four: dropping the dev gate turns the prod-leak case red, restored after) |
**Totals: 268 invariants, 268 proven, 0 owed** (2026-08-13), 53 fixtures. Proven
subsystems remain held to their contract four-way. Logger (feature 2) closed
2026-08-13 -- all 8 invariant groups / 59 shared cases now proven four-way,
real runners, no mocks (see row above for the framework fixes this pass found).
Database adapter (feature 3) closed 2026-08-13 -- all 8 invariant groups / 40
cases now proven four-way, real per-language runners against real engines, no
mocks (see row above for the framework fixes this pass found, including one
documented breaking change to Node's `executeMany` return shape). Every
invariant in the ledger is now proven; 0 owed. Messenger closed
last: the read/send shapes were already unified by the 3.13.96 parity commits
(decisions G4-G7), so the suites prove shipped behaviour; ADR-0042 records the
uid-is-the-IMAP-UID rule. Both messenger follow-ups closed 2026-08-07 (#69/#70)
and are now GATED by `msg-read-item-shape`: attachment bytes fold into
`attachments[i].content` in all four (the Python-only `attachments_data` retired),
and PHP trimmed `msgno`/`message_id`/`seen`/`flagged`, so `read()` returns exactly
the ten canonical keys everywhere - proven against real GreenMail with a real
binary attachment.

## Layer 1 only: audited and decided, no fully proven machine-checked fixture yet

These closed through an audit + a `features/NNN` plan (+ an ADR where a fork
existed), but do not yet have a JSON contract fixture. They are specified by
their plan and ADR, and are the first candidates to promote to Layer 2.

| Feature | Plan | Decision / ADR | State |
|---|---|---|---|
| 1 DotEnv parser | `features/001-dotenv.md` | SYNTHESISE | **contract complete 2026-08-09; implementation pending after full audit** |
| 3 DB adapter interface | `features/003-database-adapter-interface.md` | ADR-0044 REDESIGN | **promoted to Layer 2 2026-08-13 -- `adapter_contract.json`, 8 invariant groups / 40 cases proven all four (real SQLite + real PostgreSQL/MySQL/MSSQL/Firebird where the lab provisions them, no mocks). Real framework fixes landed to meet the 14-method contract: Python's `insert`/`update`/`delete` extracted off the declared `DatabaseAdapter` base into a `SqlCrudMixin` (DBA-S03); PHP's interface was missing `connect`/`getDatabaseType`/`autocommit` and still declared `query`/`lastInsertId`/`error` as required (all 7 implementing classes fixed, including `CachedDatabase`/`Database` which would otherwise fatal at class-load); Ruby's `CONTRACT` constant was fictional (named methods no driver implements) and `get_database_type` existed on none of its 7 drivers; Node's 6 network adapters plus `CachedDatabaseAdapter`/`Database` were missing `getDatabaseType`/`autocommit`, caught by `npm run typecheck`. `executeMany` unified to delegate to the adapter's own primitive exactly once (was facade-looped in PHP/Ruby/Node) -- BREAKING for Node, whose public return shape changed from one-result-per-row to one aggregate `DatabaseResult` (see row above). See the `Database adapter` row above for the full per-language fix narrative and mutation-proof evidence.** |
| 5 Database facade + safe write path | `features/005-database-write-facade.md` | GAP (P1) | closed, 1 deferred to Feature 24 |
| 4 DATABASE_URL parser | `features/004-database-url-parser.md` | PROMOTE php | shipped all 4 |
| 33 Middleware pipeline | `features/033-middleware-pipeline.md` | ADR-0014 | closed, merged to v3 |
| 39 Graceful shutdown | `features/039-graceful-shutdown.md` | ADR-0017 | closed, merged to v3 |
| 34 CORS middleware | `features/034-cors-middleware.md` | ADR-0018 | closed (deny by default) |
| 35, 30, 32 Rate limiter / response types / route groups | - | ADR-0019 | closed, merged to v3 |
| 17 ORM base class | `features/017-orm-base-class.md` | ORM17-DEC-01 | **promoted to Layer 2 2026-08-12 -- `ormbase_contract.json`, 2 invariants proven all four (behaviour-preserving vestigial-state cleanup: Node `_exists`, PHP `tableFilter`, dead locals removed; Ruby already clean)** |
| 20 Soft delete | `features/020-soft-delete.md` | SOFTDEL-DEC-01, SOFTDEL-DEC-02 | **promoted to Layer 2 2026-08-12 -- `softdelete_contract.json`, 6 invariants (4 four-way + 1 Python-only + 1 Ruby-only) proven (real SQLite + real PostgreSQL, no mocks: create_table injects is_deleted in all four -- Ruby via the configurable soft_delete_field; Python filter routed through field_mapping; PHP restore/with_trashed now tested; force-delete-was-PHP record corrected)** |
| 21 Relationships + eager load | `features/021-relationships.md` | REL-DEC-01, REL-DEC-02 | **promoted to Layer 2 2026-08-12 -- `relationships_contract.json`, 6 invariants (5 four-way + 1 Python-only) proven (real SQLite + real PostgreSQL, no mocks): read-side-only cascade + drop Python's phantom `on_delete`; Node declarative auto-wire + lazy accessors made to function (was dead); soft-delete filter on lazy+eager traversal all four; eager IN chunked + lazy paged/uncapped; PHP static eagerLoad fatal fixed)** |
| 22 Imperative relationships | `features/022-imperative-relationships.md` | IMPREL-DEC-01, IMPREL-DEC-02 | **promoted to Layer 2 2026-08-12 -- `imperative_relationships_contract.json`, 3 invariants proven all four (real SQLite + real PostgreSQL, no mocks): IMPREL-DEC-01 per-language idiom (Ruby's imperative form is the declarative method invoked at runtime -- no new API forced); IMPREL-DEC-02 Node serialize-orphan fixed (imperative now populates _relCache), PHP parallel private impl de-duplicated onto the one public implementation (net LOC down), the imperative has_many cap unified with the lazy path in Python/PHP/Node)** |
| 23 Scopes | `features/023-scopes.md` | SCOPE-DEC-01, SCOPE-DEC-02 | **promoted to Layer 2 2026-08-12 -- `scopes_contract.json`, 3 invariants proven all four (real SQLite + real PostgreSQL, no mocks): SCOPE-DEC-01 fixed PHP's scope global-registry collision by keying the one shared static array per calling class (Python/Ruby/Node verified already per-class, reported not fixed); SCOPE-DEC-02 scopes stay terminal lists, no compose/rebind/global-scope added (not separately ratified in the ledger)** |
| 18 ORM fields + column mapping | `features/018-orm-fields.md` | FIELD-DEC-01, FIELD-DEC-02 | **promoted to Layer 2 2026-08-12 -- `ormfields_contract.json`, 5 invariants proven all four (field-model behaviour reconciled per-behaviour, declaration surface kept per-language: PHP callable defaults, real-engine DDL replacing the mock, DECIMAL(p,s) all four, PHP datetime anchor, Ruby FK name)** |
| 19 Input and request validation | `features/019-input-validation.md` | VALID-DEC-01, VALID-DEC-02 | **promoted to Layer 2 2026-08-12 -- `validation_contract.json`, 4 invariants (3 four-way + 1 Node-only) proven (real SQLite + real socket, no mocks: deleted Python's placeholder tests, tested PHP's save-writes-nothing enforcement, enriched Ruby's null-only ORM validate + deleted dead validate_fields, wired Node AutoCrud PUT validation + partial-update, unified BOTH validators on one canonical message vocabulary)** |
| 24 Paginated results | `features/024-paginated-results.md` | PROMOTE php | **RE-OPENED 2026-08-05** - `.count` means true-total in 2 of 4, rows-returned in the other 2; the envelope launders a truncation. Breaking fix pending. |
| 25 Result / ORM caching | `features/025-orm-result-caching.md` | CACHE-DEC-01 | **promoted to Layer 2 2026-08-12 -- `ormcache_contract.json`, 3 invariants proven all four** |
| 19 Input validation | `features/019-input-validation.md` | PROMOTE node | closed |
| 15 Migrations | `features/015-migrations.md` | MIG-DEC-01, MIG-DEC-02, MIG-DEC-03 | **promoted to Layer 2 2026-08-12 -- `migrations_contract.json`, 6 invariants proven all four (real SQLite + real PostgreSQL :55432 + real MySQL :3306 + real MSSQL :1433 + real Firebird 5 :3050, NO mocks): fixed Python's migrate:status KeyError + PHP's migrate:status TypeError; unified Node's CLI onto the ORM migrate() (deleted the naive-split re-implementation); fail-safe rollback in Ruby/PHP/Node (never drop the ledger row without a successful reverse); Ruby/Node Firebird+MSSQL idempotency converted from fakes to real engines; SQLite atomicity doc-drift corrected in all four CLAUDE.md (+ Ruby's docstring); auto-migrate default confirmed unchanged (ledger: keep default-ON)** |
| 48 Frond lexer | `features/048-frond-lexer.md` | historical bundle promoted Python structure | reopened / queued |
| 49 Frond parser | `features/049-frond-parser.md` | historical bundle promoted Python structure | reopened / queued |
| 50 Frond compiler | `features/050-frond-compiler.md` | historical bundle promoted Python structure | reopened / queued |
| 51 Frond runtime | `features/051-frond-runtime.md` | historical bundle promoted Python structure | reopened / queued |
| 52 Frond filters | `features/052-frond-filters.md` | SYNTHESISE | closed |
| 57 Auto-escaping | `features/057-auto-escaping.md` | UNIFORM | closed, 1 owner call |
| 58 Sandboxing | `features/058-sandboxing.md` | PROMOTE php (P1) | shipped all 4 |
| 81 Api / HTTP client | `features/081-api-client.md` | frameworks-outrank-internal | `send_request` unified 2026-08-07 (Python was the outlier; Ruby cannot use bare `send`). No fixture yet. |

## Layer 0: not yet audited

Every audited feature so far found something broken and invisible - none came
back clean - so these are unexamined, not "probably fine" (98-feature-audit.md).
Each still owes the full pipeline: audit -> plan -> ADR -> fixture -> proven.

- Every other queued packet is listed in numeric order in
  [01-FEATURE-MATRIX.md](01-FEATURE-MATRIX.md).

The authoritative live list is the flat feature matrix and catalog JSON.

## Keeping this map current (the discipline)

This map only earns its name if it stays synced. On each of these events, update
the matching row here in the SAME change:

- **A feature audit closes** -> add/move its Layer 1 row, cite its `features/NNN`
  plan and ADR.
- **A fixture is written or an invariant flips owed -> proven** -> re-run
  `scripts/audit-contract-fixtures.py`, copy its proven/owed counts into Layer 2.
- **An ADR is allocated** -> cite it on the invariant and the row.
- **A feature moves out of Layer 0** -> strike it from the not-yet-audited list.

The auditor's own numbers, never a hand count, are the source of truth for
proven/owed. A row here that disagrees with the auditor is a bug in this file.

## Snapshot (2026-08-07)

- 98 numbered rows, with 21-26 retired into database-adapter group 4; feature 27
  is now under audit. Do not derive a remaining count from the retired numbers.
- 12 contract fixtures, 90 invariants, **74 proven / 16 owed**. Logger and
  database adapter are the two decision-complete, implementation-red packets.
- 41 ADRs allocated (`decisions/`), highest ADR-0041; ADR-0042 authored this
  release for the messenger uid rule.
- The path to a formal language spec: every Layer-0 feature reaches Layer 2, the
  owed count reaches 0, and MASTER-SPEC is regenerated from the fixtures + ADRs.
