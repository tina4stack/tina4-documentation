# Implementation pass - 3.13.99 (first pass, re-numbered from 3.13.98 - the skills release took the .98 slot)

Every ratified fix, implemented in all four frameworks, one feature fully finished before the next.
Decisions: `OWNER-DECISIONS.md` (Batches 1-5 + standing compiler). Spec per feature = its doc's porting
capsule + proposed fixture.

## Execution model (owner, 2026-08-11) - read before any code

- **NO BASELINE RUN.** Owner: "No baseline run" / "everytime we do this we lose parity." No up-front
  full-suite run. Each feature is proven by ITS OWN real tests + the shared fixture, green in all four.
- **ONE FEATURE FULLY COMPLETED BEFORE THE NEXT.** Implement in ALL FOUR, write real (no-mock) tests +
  the shared conformance fixture, lab-validate GREEN in all four, commit, THEN the next feature. No
  batching, no parallel features. This is what holds parity.
- **GREEN FOR DEV, 1-133.** The dev gate is every feature 1-133 green. The ~53 below get fixed; the rest
  stay green and must not regress.
- **MAINTAINABILITY LENS.** Maintainability = less code. Each feature also deletes the dead/duplicated
  code its finding names, prefers deleting to adding, adds ZERO new runtime deps (reuse ladder). Bigger
  simplifications become their own checkbox here, never a silent detour.

**Per-feature definition of done:** logic in all four -> real tests + shared fixture (positive AND
negative, no mocks) -> dead/dup code removed -> run the fixture + the feature's tests ON THE .99 LAB BOX
-> real GREEN in all four -> commit. Never self-report, never advance on red. Markers: `[ ]` todo /
`[~]` in progress / `[L]` lab-green all four / `[x]` committed.

## Phase 0 - setup (branch only, NO baseline)

- [x] Cut `feature/release3.13.99` off v3 in each repo (renamed from feature/release3.13.98 when the skills release claimed .98); version bumped to 3.13.99 (manifest + lock)
- [ ] Carry the two audit HEADs into v3 as part of feature 37/127: feature/csrf-fail-closed (py) +
      feature/mcp-call-gate (php/ruby/node). No standalone baseline - they land with their features.

## Phase 1 - security cluster (highest value first)

- [x] 37 CSRF - DONE 2026-08-11, lab-green all four (py 59 / php 56 / ruby 44 / node 46; consolidated rc=0, independently re-verified). Pushed py 3249495, php 001f966a, ruby dbcd6a2, node cc6642a, fixture doc 3dd9608. Removed PHP default-secret + `$_ENV` mutation; aligned Node gen/validator; `TINA4_CSRF` attaches; session-bind + type=form; SEC-01 ported all four; 403 body unified to `{error,code,message,status}`.
- [x] 127 dev-admin - DONE 2026-08-11, lab-green all four (9/9/9/9; consolidated rc=0, independently re-verified). Pushed py 41b3aeb, php 698e6a6, ruby ab06e5c, node 97d4f22, fixture fc42718. Same-origin gate + `.env` denylist + localhost-default bind + mcp/call gate (ADDED to Python) + toolbar escape. Fixed in passing: PHP DEC-02 landed in a dead App::run path (real fix in `bin/tina4php resolveHostPort`); Ruby 404 error-page reflected the raw path (XSS) - audit had marked Ruby n/a for DEC-04, it was NOT (correct the 127 doc).
- [x] 41 static assets - DONE 2026-08-11, lab-green all four (40/43/30/52; consolidated rc=0, independently re-verified). Pushed py c51c686, php c422f4b, ruby e24a3fa, node 77592ad, fixture 1978abc. realpath+separator confinement (ADR-0050) ported to py/ruby/node; symlink-escape + dotfile blocked; `TINA4_PUBLIC_DIR` honoured ruby+node; one search-dir order.
- [x] 43 request-id - DONE 2026-08-11, lab-green all four (9/9/9/11; consolidated rc=0, independently re-verified). Pushed py 1d324d5, php 1d9d607, ruby 1bfc060, node 0828904, fixture 234ec05. Honour+emit X-Request-ID; sanitize inbound (CRLF/charset/length); request-scoped storage all four (py contextvars, ruby thread-local, node AsyncLocalStorage, php per-request) - mutation-proved isolation.
- [x] 53 Frond tags - DONE 2026-08-11, lab-green all four (5/5/5/5; consolidated rc=0, independently re-verified). Pushed py f858c11, php 42a0723, ruby a1ff8af, node 22e6057, fixture bc0b9ff. One shared containment helper per language (PHP extracted `resolveTemplatePath` from 4 duplicated joins - less code); include/extends/import confined under the templates dir (lexical `..`/absolute reject + realpath containment; symlink-escape refused).
- [x] 14 Mongo SQL provider - DONE 2026-08-11, lab-green all four (7/7/7/7; consolidated rc=0, independently re-verified on real Mongo). Pushed py 3315d1c, php baf1af5, ruby d45c119, node ad62c2d, fixture 837b936. Fail-closed: unparseable/partial WHERE raises (never match-all); empty-filter delete/update rejected; one shared guard per language; `truncate()` (explicit `1=1`) unaffected. Removed dead `?:[]`/`|| {}` fallbacks (less code).
- [x] 36 security headers - DONE 2026-08-11, lab-green all four (4/4/4/5; consolidated rc=0, independently re-verified). Pushed py bfc1597, php 853c47c, ruby 6e44205, node aa73dbf, fixture 1d0bcbe. Registered secure-by-default (all four); HSTS HTTPS-guarded (`TINA4_HSTS` + secure scheme); CSP `default-src 'self'` (`TINA4_CSP` to relax); PHP class renamed `SecurityHeaders`->`SecurityHeadersMiddleware` (no alias); identical header set/values; `nginx.conf.example` de-drifted.
- [x] 129 port-takeover - DONE 2026-08-12, lab-green all four ON LINUX (5/16/5/5; consolidated rc=0, re-run by the main loop on the .99 lab after the worker's macOS-only run). Pushed py fdd7d86, php 40cd4c0, ruby 75f78ae, node ae7332d, fixture 6845b2c. Identity-check (per-port PID file) before kill - a foreign process is NEVER killed; runtime path shares the CLI guard; `TINA4_NO_TAKEOVER`/`--no-kill` opt-out; dev-gated. Duplicated takeover logic collapsed to one shared helper per language (~-100 lines each).
- [x] 126 debug overlay - DONE 2026-08-12, lab-green all four ON LINUX (22/22/22/48; consolidated rc=0, independently re-verified). Pushed py 7b460ea, php df860de, ruby 93c1e06, node f9afed3, fixture a8f93c1. DELETED dead `render_production_error` (all four) + real wired prod-no-leak test; dev overlay redacts Authorization/Cookie/Set-Cookie + secret body fields (one redact helper); frame cap (MAX_FRAMES=50); self-throw guard (overlay fail -> safe 500); gate unified on `is_debug_mode()`.
- [x] 132 inline testing - DONE 2026-08-12, lab-green all four ON LINUX (py 61 / php 8 tests-37 assertions / ruby 48 / node typecheck+49+4+6; consolidated rc=0, independently re-run on the .99 lab). Pushed py d854b9d, php e797e9b, ruby e477caf, node e4202b5, fixture ec863cf. Wired ONE surface (the `@tests`/`expect_*` descriptor model) into `tina4 test` with a real exit code + discovery all four (Ruby already ran `run_all`; Python/PHP/Node newly wired); REMOVED PHP's `eval()` RCE (literal-only arg parser) + the blanket `require_once` (discovery confined to an explicit tests dir, realpath); renamed the descriptor builders `assert_*`->`expect_*` so they no longer collide with the xUnit immediate `assert_*`; Python meta-test snapshots/restores the global registry. Shared fixture `inlinetesting_contract.json` (3 invariants / 4 cases) proven by a real-CLI no-mock suite in all four; mutation-proved (exit-fold, tests-dir confinement/eval removal/marker-only discovery, collision).

## Phase 2 - data-loss / silent no-op

- [x] 44 file upload - DONE 2026-08-12, lab-green all four ON LINUX (6/6/6/8; consolidated rc=0, independently re-verified - initial red was a `$HOME`-under-sudo harness bug in the gate script, NOT the code). Pushed py 9fd483c, php a6bb4b3, ruby dab136f, node 5a9bdbe, fixture 68b5e12. Repeated FILE field -> a list (no silent drop; files-scoped, matching Node + UP-MULTIFILE-LOSS); safe-save helper (sanitize filename + realpath-confine) all four; per-chunk running size guard brought to PHP/Ruby (413 mid-stream, not after buffering).
- [x] 47 background tasks - DONE 2026-08-12, lab-green all four ON LINUX (6/6/6/7; consolidated rc=0, independently re-verified). Pushed py 1d4fb4d, php 9f62bde, ruby 32f9539, node fe6e069, fixture 506afeb. Python starts tasks from the ASGI lifespan (was a silent no-op under uvicorn/hypercorn); PHP loud FPM/Swoole SAPI guard (never a silent drop); one handle+count surface all four (`background()` -> stop-handle + `count()`); mutation-proved.
- [x] 25 ORM cache - DONE 2026-08-12, lab-green all four ON LINUX (8/8/8/8; consolidated rc=0, independently re-verified on real SQLite). Pushed py 9329fbc, php d6fca93, ruby cdbeee3, node c472089, fixture b5040f4. `cached()` now busts on ALL writes (save/delete/force_delete/restore), tagged by every FROM/JOIN table (an unrelated-table write leaves it intact), `ttl=0`=no-cache; Node's drifted-in untagged cached() rebuilt onto the shared tag-aware QueryCache (KEPT in py/php/ruby, not dropped); removed Node dead `_queryCache` + PHP wholesale cacheClear (less code).
- [x] 16 next-id - DONE 2026-08-12, lab-green all four ON LINUX (5/5/5/5; consolidated rc=0, independently re-verified on real PG/MySQL/Mongo). Pushed py f735e6b, php 833cceb, ruby d22c507, node 3fbf63b, fixture 6a6ec65. Generic next-id is now atomic (`UPDATE ... RETURNING`, was UPDATE-then-separate-SELECT TOCTOU); Postgres draws from a real SEQUENCE; Mongo uses an atomic `findOneAndUpdate $inc` counter (Ruby/Node gained the Mongo path); mutation-proved with real duplicates under 24-48 concurrent callers.
- [x] 7 SQL translator - DONE 2026-08-12, lab-green all four ON LINUX (6/6/6/6; consolidated rc=0, independently re-verified on real MySQL/PG). Pushed py 4fbe199, php d386bcd, ruby 216290f, node 93b5c1e, fixture 01e55ad. Literal-safe rewrite (mask string-literals/quoted-idents/comments -> rewrite concat/bool/ilike -> restore; `||`/bool/ILIKE inside a literal preserved), mutation-proved; PHP translator was fully DEAD - wired concat/ilike into MySQLAdapter; Ruby wired into MysqlDriver + removed dead limit_to_rows/limit_to_top/placeholder_style + duplicate query_key; BIGINT autoincrement -> BIGSERIAL (PG) proven on a real DB.
- [x] 14b Mongo truncate() parity - DONE 2026-08-12, lab-green all four ON LINUX (9/9/9/9 = 7 feature-14 guard cases + 2 new; consolidated rc=0, independently re-verified on real Mongo). Pushed py 079873e, php 7ac2c0b, ruby abc5aa5, node 5d2afea, fixture 3ad9430. The explicit `1=1` tautology now translates to match-all `{}` in py/ruby/node so `truncate()` empties (PHP already did); mutation-proved BOTH that truncate empties AND that the feature-14 guard stays intact AND that a SCOPED equality is not widened. Extended mongosql_contract.json (4th invariant).

## Phase 3 - DB providers / write-path correctness

- [x] 9 PostgreSQL - DONE 2026-08-12, lab-green all four ON LINUX (13/13/13/13; consolidated rc=0, independently re-verified on real PG). Pushed py f67e73d, php 2258bbb, ruby 09e254a, node 5fe1778, fixture 4b9e35a. ZERO framework code changed (the PG write path was already correct - the oracle is healthy); this is the shared behavioural contract pinning insert-RETURNING-last-id + affected count + filterless-write guard + executeMany atomicity (mid-batch rollback) + fetch/fetchOne shape against real PG - 5 invariants owed->proven, mutation-proved each. adapter_contract.json (feature 3, ADR-0044) stays 8-owed (the 14-method interface redesign is separate).
- [x] 10 MySQL - DONE 2026-08-12, lab-green all four ON LINUX (12/10/10/10; consolidated rc=0, independently re-verified on real MySQL). Pushed py 99487c9, php 1551c9d, ruby af1ee43, node 979b1c0, fixture f3877ff. Python's real-PK RETURNING emulation (was hardcoded `id`; the emulation is PYTHON-adapter-only, php/ruby/node use column-independent last_insert_id and were already correct); strict-quoted DESCRIBE all four (backtick-escape, was injectable); de-duped the two PHP batch-id blocks; batch-id via SQLTranslator.batch_last_id. Mutation-proved. Doc corrected (the RETURNING-emulation is Python's, not PHP/MSSQL/Firebird's).
- [x] 11 MSSQL - DONE 2026-08-12, lab-green all four ON LINUX (9/7/8/7; consolidated rc=0, independently re-verified on real MSSQL). Pushed py 4491cf8, php 1deb9d6, ruby 937a35c, node 2de3fa4, fixture 9dcc80e. Safe params: Node Buffer->VarBinary, PHP+Ruby binary via `0x` literal (FreeTDS provably can't bind raw binary via `?`), Ruby unknown-type now RAISES (was a bareword); real-PK RETURNING (Python-adapter-local, per feature 10); one OFFSET/FETCH pagination (Node dropped the TOP first-page branch). Mutation-proved.
- [x] 12 Firebird - DONE 2026-08-12, lab-green all four ON LINUX (11/10/28/10; consolidated rc=0, independently re-verified on real Firebird 5). Pushed py de441c5, php 27dd66e, ruby 286cd03, node 47d3d4e, fixture 532e277. Ruby no-mock VIOLATION replaced with a REAL reconnect test (kills the connection via MON$ATTACHMENTS, proves re-attach); Node gained a reconnect path; generator last-id + real affected-count all four; blob byte-for-byte; SRP bounded-retry; real-PK RETURNING (Python-local); CI-gate/CLAUDE.md claim fixed; node-firebird -> optionalDependency. DRIFT FIXED: the write-contract (last_id/affected) was broken in ALL FOUR on real Firebird (audit scoped it to ruby/node) - Python last_id=None, PHP read the wrong txn - all fixed. Mutation-proved.
- [x] 13 ODBC - DONE 2026-08-12, lab-green all four ON LINUX (11/10/10/11; consolidated rc=0, independently re-verified against a REAL provisioned ODBC->PostgreSQL source). Pushed py a3c89bd, php ae6e83b, ruby 6c3c8ab, node ceef906, fixture 05988cd. PROVISIONED real ODBC on the lab (unixODBC + psqlodbc + a tina4_odbc DB + DSN + a scram auth-DSN credential gate; idempotent in the gate). PK catalog (SQLPrimaryKeys / INFORMATION_SCHEMA); Python `@@IDENTITY` removed (driver-aware) + creds honoured; Node string-WHERE branch + executeMany owns-guard; fail-loud fetch (PHP). DRIFT FIXED: ODBC was so untested it could not even CONNECT in Ruby (SQLConnect vs `#drvconnect`) or instantiate in PHP (parse_url on braces) - both fixed. Mutation-proved. ODBC is now first-class + working.

## Phase 4 - ORM / validation / correctness (feature order)

- [x] 17 ORM base - remove vestigial state (Node `_exists`, PHP `tableFilter`, dead locals) - DONE, lab-green 5/5/5/5 on the .99 Linux lab (re-run by main loop as root; save->load->count->where->update->delete round-trip proves behaviour-preserving). py a18da9d / php 4e454f2 / ruby bbf35b6 / node 39931bd, fixture+CONTRACT-MAP dc05dba (ormbase_contract.json, 2 invariants, totals->172/156/33). Net framework source -19 LOC, ZERO new deps. CODE-WINS FINDINGS (reported, NOT removed): PHP `_exists` IS read in save() (finder insert-vs-update shortcut); Node `tableFilter` IS a declared static read (findById/all/where/count/withTrashed + AutoCrud); Ruby had NO vestigial state (uses `@persisted`). Removed only truly-dead: Node `_exists` (3 writes/0 reads) + `pkCol` dead locals; PHP undeclared `tableFilter` read path (routed __get->null->always skipped) + `$pkColumn` dead local; Python dead `table_sql`/`pk_db_col` (+ dead `pk`/`pk_value` in restore). 0 grep hits after; existing ORM suites green (py 519 / php 150 / node 88+10+53).
- [x] 18 ORM fields - reconcile the field model behaviour-by-behaviour (ADR-0004); real-engine DDL tests; PHP callable defaults - DONE, lab-green 22/25/23/26 on the .99 Linux lab across REAL PG/MySQL/MSSQL/Firebird (re-run by main loop as root; boolean/json/decimal/round-trip DDL cases green on all four engines). py 6dffcdc / php d97a405 / ruby df93891 / node 9476369, fixture+CONTRACT-MAP d40e208 (ormfields_contract.json, 5 invariants, totals->177/161/34). FIVE behaviours reconciled: (1) PHP callable defaults via a `fieldDefaults()` hook (per-instance, dropped from DDL); (2) DECIMAL(p,s) parity - NONE of the four had a decimal->DECIMAL(p,s) path; each got its idiomatic one (Ruby was emitting REAL, the named bug); (3) FIELD-ENGINE-DDL-MOCK DELETED (0 monkeypatch) -> real CREATE TABLE + catalog readback on PG/MySQL/MSSQL/Firebird; (4) PHP datetime heuristic anchored (`updated_by`/`runtime`/`downtime` no longer datetime); (5) Ruby FK related-name smart-pluralized (`Category`->`categories`). Declaration SURFACE deliberately NOT converged (each language keeps its idiom - owner override). ZERO new deps. Mutation-proved (DecimalField REAL -> assert 24==12 RED). Python save() now returns False+logs on non-serializable JSON (was RAISING; PHP/Ruby/Node already correct - code-wins).
- [x] 19 input validation - real tests for the untested validators; Ruby richer ORM validation; Node AutoCrud PUT validation; unify messages - DONE, lab-green 21/38/44/24 on the .99 Linux lab (re-run by main loop as root; Node AutoCrud PUT-422 + partial-update cases green over real HTTP + real SQLite). py b9fbd7e / php cd9ab0d / ruby b427f8b / node bf0ea6a, fixture+CONTRACT-MAP 0bf53c4 (validation_contract.json, 4 invariants=3 four-way+1 Node-only, totals->181/165/35). FINDINGS CLOSED: VALID-PY-UNTESTED (DELETED the two `assert True` placeholders -> real pos+neg for every rule); VALID-PHP-ENFORCE-UNTESTED (save() returns false AND COUNT(*)==0); VALID-RUBY-NULLONLY (ORM validate() was null-only -> now enforces required+length+range+format+numeric-type; field DSL gained required:/min_length:/max_length:/pattern:/min:/max:; DELETED dead zero-caller `validate_fields`, -8 LOC); VALID-NODE-PUT-NOVALIDATE (AutoCrud PUT now validate(body,fields,true)->422 + isUpdate partial-update wired into save()); VALID-TWO-MESSAGES (both validators speak ONE canonical vocabulary - Node request-Validator wording - all four). ~919 of ~1142 added lines are real no-mock tests. Mutation-proved (disable Ruby max_length -> RED; remove Node PUT validate -> RED; stub PHP validate() -> 10 RED). Code-wins: Node ORM validate() already field-prefixed (only the regex wording changed). SCOPE HELD: Ruby `length:` stays a DDL sizing hint (parity with PHP), value-cap is `max_length:` - did NOT reopen feature-18; only Node auto-wires AutoCrud request-validation (py/php/ruby = feature-27 scope).
- [x] 20 soft delete - PHP restore/with_trashed tests; correct the force-delete record; `create_table` injects `is_deleted` - DONE, lab-green 14/12/14/12 on the .99 Linux lab on REAL PostgreSQL (re-run by main loop as root; every case runs twice, SQLite + PG). py 6c190c2 / php 3d10d5e / ruby a6ce6ad / node f33ed9b, fixture+CONTRACT-MAP d5f03fc (softdelete_contract.json, 6 invariants=4 four-way+1 Python-only field_mapping+1 Ruby-only configurable-field, totals->187/171/36). SOFTDEL-CREATETABLE-COLUMN: create_table now injects the soft-delete column in ALL FOUR - Python/PHP/Node literal `is_deleted INTEGER DEFAULT 0`, and RUBY injects the CONFIGURED `soft_delete_field` (audit scoped Ruby out; worker verified Ruby had the SAME gap - code-wins - and fixed it). SOFTDEL-PY-FILTER-MAPPING: Python finder filter + delete/restore writes + injection now route through `field_mapping`. SOFTDEL-PHP-RESTORE-UNTESTED: new PHP restore()/withTrashed() tests. SOFTDEL-FORCE-WAS-PHP: record corrected (NO code) - the throw-instead-of-delete bug was PHP-only (undefined $whereParams); Python/Ruby were already correct, Node's fix had landed; force_delete's real hard-remove is proven in all four. Net framework source ~+93/-9, zero new deps. Mutation-proved (revert injection -> RED; soft-UPDATE force_delete -> RED).
- [x] 21 relationships - read-side-only; drop Python's no-op `on_delete` - DONE, lab-green 12/10/10/10 on the .99 Linux lab on real PG (re-run by main loop). py 2e9a69a / php 94ccf77 / ruby df9bbe6 / node ad51731, fixture+CONTRACT-MAP fb56c29 (relationships_contract.json, 6 invariants, totals->177 proven/193/37). NODE declarative auto-wire + lazy load now WORKS (was dead - `_wireRelationshipAccessors` reuses the imperative path + registry; `await post.author`/`author.posts` resolve, `include:` populates, proven sqlite+PG). Soft-delete traversal filter all four (lazy+eager; PHP lazy already filtered - code-wins). Eager IN chunked @500; lazy caps removed (Python 1000 / PHP-Ruby 100 silent caps gone; 1001 children reachable). PHP static `eagerLoad` fixed (resolveDb). Read-side-only: Python phantom `on_delete=` DROPPED -> now RAISES; PHP/Ruby/Node never had it (code-wins). Mutation-proved all four. FLAGGED to feature 18 (not widened): Node FK TEXT affinity round-trips `"1.0"` in toDict/serialization - traversal fixed engine-independently via `_joinKey`, but the cosmetic serialized value is a feature-18 field-coercion concern.
- [x] 22 imperative rels - Node serialize-orphan; DE-DUP PHP parallel impl; unify Python cap; Ruby stays declarative-at-runtime - DONE, lab-green 5/5/5/5 on the .99 Linux lab on real PG (re-run by main loop). py 558a000 / php 4e1d5df / ruby 236f894 / node 4126c4a, fixture+CONTRACT-MAP e598244 (imperative_relationships_contract.json, 3 inv, totals->196/180/38). IMPREL-DEC-01 = per-language idiom (OWNER-DECISIONS Batch 4): Ruby framework UNCHANGED (its imperative form is the declarative method invoked at runtime; runner exercises it, no gap scored). IMPREL-DEC-02: Node _relCache now populated (imperative relation serializes - was orphaned under table-name key); PHP DELETED the 3 private hasOneMethod/hasManyMethod/belongsToMethod, declarative path calls the ONE public impl (net -44 LOC); Python/PHP/Node imperative has_many silent 100-cap removed (pages-all, matches lazy). Mutation-proved. Zero new deps.
- [x] 23 scopes - fix PHP global-registry collision - DONE, lab-green 6/6/6/6 on the .99 Linux lab on real PG (re-run by main loop; dispatched at Sonnet/medium per the tier rule - correct four-way green). py 087826e / php 9506410 / ruby 67eb165 / node 53b2065, fixture+CONTRACT-MAP 38e35ae (scopes_contract.json, 3 inv, totals->199/183/39). SCOPE-PHP-COLLISION fixed: `static::$_scopes[$name]` -> `static::$_scopes[static::class][$name]` (per-class key) in scope()+__callStatic (ORM.php net -2). Python/Ruby/Node were ALREADY per-class (code-wins - verified by source + the two-model-same-scope-name fixture case passing unmodified). SCOPE-DEC-02 = document-as-terminal-list per the ledger (no compose/rebind/global added). Mutation-proved (revert per-class key -> collision case RED on SQLite + PG). Cases: two_models_same_scope_name_return_different_rows, scope_excludes_a_soft_deleted_row, scope_honours_limit_and_offset.
- [x] 24 pagination - clamp page>=1 (py/ruby/node); cap max per-page - DONE, lab-green 37/31/11/38 on the .99 Linux lab on real PG (re-run by main loop; Sonnet/medium, correct green; page=0 exercised on real PG which rejects negative OFFSET). py 761873a / php a8088ec / ruby c8f8a93 / node feaf660, fixture+CONTRACT-MAP 119a7a5 (pagination_contract.json extended 6->8, totals->185 proven). PAGE-NEGATIVE-OFFSET: page<1 now clamps to page 1 (non-negative offset) in Python/Ruby/Node (PHP already clamped - code-wins, locked with a regression test). PAGE-NO-MAX-LIMIT: oversized ?limit/?per_page capped at MAX_PER_PAGE=100 (reused the existing DEFAULT_ROW_CAP=100 family constant; ALL FOUR needed the cap, not Node-only as the audit suspected). Mutation-proved (revert clamp -> real PG "OFFSET must not be negative" RED; revert cap -> oversized echoed RED). Node autoCrud.ts dropped 3 duplicate recompute lines (envelope now uses buildQuery's clamped values - can't drift).
- [x] NEW: MAIL-REDIRECT (`TINA4_MAIL_REDIRECT_TO`) - owner-directed 2026-08-12 (MAIL-DEC-01), not from the audit - DONE, lab-green 3/3/3/3 on the .99 Linux lab against REAL GreenMail (SMTP :3025 / IMAP :3143; re-run by main loop; delivery proven by real IMAP fetch - dev list received, real recipient empty, `X-Tina4-Original-To` set). py ae74217 / php 521e623 / ruby e39f230 / node dd5504e, fixture+CONTRACT-MAP 35b9c5d (mail_redirect_contract.json, 2 inv, totals->203/187/40). Comma-separated dev list; send() STILL delivers via SMTP but replaces To with the full list + clears Cc/Bcc (every address on the list receives; no real recipient does); original recipients preserved in `X-Tina4-Original-To`. TINA4_MAIL_CAPTURE takes precedence (capture wins - nothing sent). Parse: comma-split + trim + drop-blank; unset/empty = no-op. Purely ADDITIVE (no breaking change). Mutation-proved (no-op the rewrite -> dev list empty / real recipient receives -> RED). ~+25-39 LOC/lang, zero deps.
- [x] 26 loading - stop Python re-enforcing write constraints on read; unify Ruby's two read paths; pin scalar read-coercion - DONE, lab-green 2/2/2/40 on the .99 Linux lab on real PG (re-run by main loop, INDEPENDENT green 190431; Sonnet/high). py 6095243 / php 4807b62 / ruby 1c6d0c5 / node 5b5cdcc, fixture+CONTRACT-MAP 579873a (instance_loading_contract.json, 5 inv = 4 four-way + 1 Node-only, totals->208/192/41). LOAD-PY-REVALIDATE: Python read now applies coercion+JSON-parse but NO business-constraint re-enforcement (a constraint-violating/later-tightened stored row hydrates instead of aborting the page); write-path validation UNCHANGED (bad save still false, proven). LOAD-RUBY-ASYMMETRY+SIGNATURE: `load()` routed through `from_hash` coercion (ONE path; now JSON-parses) + aligned to `load(filter, params, include)` with eager-load. LOAD-NODE-SERIALIZE-OMIT: toDict/toJson now Log.warning on an unloaded declared relation (was silent). LOAD-DEC-02: PIN the JSON-only scalar contract (ledger) - no Date/bool reconstitution added. Cases: json_column_round_trips_via_finder, _via_load, constraint_violating_stored_row_still_hydrates, partial_select_yields_partial_instance (+ Node unloaded_relation_serialization_warns_not_silent). Mutation-proved.
  - INCIDENT (caught by independent verification): the worker pushed the 4 framework commits then PARKED on a backgrounded lab run before reading it - the gate was RED (php=1 node=1) and NO fixture was pushed. Root cause was a TEST bug, not a framework bug: the PHP+Node JSON assertions compared objects ORDER-SENSITIVELY (`{tags,n}` vs `{n,tags}` = same object; Python/Ruby passed because dict/hash compare ignores key order). Fixed the assertions key-order-insensitive (php 562e2f1->4807b62, node 5348afd->5b5cdcc); main loop re-ran to a real four-way green + drove the fixture push. LESSON: a JSON round-trip test must compare parsed objects order-INSENSITIVELY; and a worker that backgrounds its own gate can leave red pushed - the main-loop independent re-run is what caught it.
- [x] 27 AutoCrud - invalid create -> 422 with field errors, all 4 - DONE, lab-green 6/6/6/6 on the .99 Linux lab (main-loop INDEPENDENT green 195125; Sonnet/high; real server boot + real HTTP + real JWT all four). py 9941536 / php 51cd524 / ruby d5cdd95 / node 1c4f7cd, fixture+CONTRACT-MAP 22fd6d5 (autocrud_contract.json, 6 inv, totals->198 proven/42 fixtures). CRUD-VALIDATION-STATUS: invalid create/update -> consistent **422 + FIELD errors** all four (PHP/Ruby were 500, Python was 400). CRUD-PUT-NOVALIDATE: PUT body validated all four (Node via feature-19 isUpdate; py/php/ruby load-overlay-validate). CRUD-MASS-ASSIGNMENT: writable-column guard - `is_deleted` never client-writable + PK stripped on create, all four (PHP kept a DENY-list of is_deleted+PK, not a strict allow-list, to preserve its dynamic-property models). Secure-by-default (tokenless write->401) proven all four, mutation-proved. Wire cases: tokenless_write_returns_401, valid_authenticated_post_returns_201, invalid_post_returns_422_with_field_errors, invalid_put_is_rejected, mass_assignment_is_blocked, list_is_the_seven_key_envelope.
  - EXTRA DEFECTS closed (found while implementing, same fixes): (1) IDOR-shaped PK redirect - a PUT body's own PK flowed into the row-address/SET before the WHERE was built (py/php/ruby could redirect the write to a different row; Node could rename the identity column) -> PK stripped from PUT bodies unconditionally, all four; (2) Python & PHP swallowed a `save()==False` driver failure in dead except-blocks (save never raises) and fell through to 201/200 -> now check the False return.
- [x] 28 seeder - fix PHP `seed_table` backtick quoting; REMOVE the inert `seed_table(seed=)` param - DONE, lab-green 75/133/155/265 on the .99 Linux lab on REAL PG+MSSQL+Firebird+SQLite (main-loop INDEPENDENT green 205245; Sonnet/high). py 80e897b / php 6208fbb / ruby 3dc8ee8 / node 248a0ee, fixture+CONTRACT-MAP 38c8abb (seeder_contract.json, 5 inv, totals->219/203/43). SEED-PHP-BACKTICK: PHP seed_table routed through the parameterized adapter insert (was MySQL/SQLite backticks -> dev-admin seed now works on PG/MSSQL/Firebird; mutation-proved RED on all 3 with backticks reinstated). SEED-TABLE-SEED-INERT: ledger = REMOVE - `seed_table(seed=)` now raises on a non-default (Ruby drops the kwarg); determinism = seed your own FakeData. SEED-NODE-DEFAULT variety, Ruby native boolean + opt-in idempotency (`idempotent:`), per-language-determinism + not-for-secrets docblocks, pinned generator vocabulary. EXTRAS (fix-on-discovery, surfaced only on REAL Firebird/PG): Node had the SAME SQL-quoting bug class on Firebird -> new `adapterInsert()` routes seedTable/seedOrm through the adapter's native insert (deleted hand-rolled SQL); the new helper needed a fail-loud guard (SQLite insert() returns {success:false} not throw). Mutation-proved. FOLLOW-UPS spawned: Node `Database.insert()` facade not fail-loud on SQLite (feature 3/9); Ruby Firebird String-keyed fetch() rows (feature 12).
- [x] 29 request model - route params SEPARATE from query (breaking py/ruby); add Python `request.user` - DONE, lab-green 4/31/36/31 on the .99 Linux lab (main-loop INDEPENDENT green 215648; Sonnet/high). py 7df99c8 / php 88edd95 / ruby 776b3c0 / node c3acf40, fixture+CONTRACT-MAP 7b6658c (request_contract.json, 4 inv). REQ-PARAM-POLLUTION (SECURITY + breaking): `request.params` now ROUTE-ONLY in all four - Python was query+route, Ruby was query+BODY+route; PHP/Node already route-only (code-wins, PHP net 0). REQ-PY-NO-USER: Python Request gained a mutable `user` (+__slots__). IP-trust NOT regressed (proven fresh). DERIVED REQ-DEC-02 sub-values (ledger did not spell them out; worker used the ledger's OWN tie-breaks - 3-of-4 majority + "reference" language - NOT invented; FLAG for owner confirmation at merge): route-param name = `params` (Ruby path_params->params, NO alias per no-alias rule); malformed-body = raw string (3-of-4); empty-body = each language's native null (Ruby was `{}`); immutability = PHP readonly/Ruby attr_reader are the reference (unchanged), Python added a __setattr__ guard on 14 core fields, Node added readonly to 8 TS fields; header dash = case-fold only (RFC 7230); Ruby Rack multipart = noted/kept (Ruby's server IS Rack). Fixed the cross-cutting stale tests (py 5, ruby 4). 2 REAL bugs found+fixed in the blast radius: Python CsrfMiddleware `params or query` would have gone dark on parameterized write routes post-fix; Python AuthMiddleware was ALREADY broken (assigned a nonexistent `request.auth`, HIDDEN by its own MOCK test - a no-mock-rule casualty) - fixed with the `user` add; Ruby TestClient dropped a caller Content-Type (CGI-prefix bug). Mutation-proved (reinstate the merge -> route_param_not_shadowed_by_query RED).
- [ ] 15 migrations - fix `migrate:status` (py+php); Node CLI uses the real migrator; auto-migrate default-ON + `TINA4_AUTO_MIGRATE=false` opt-out

## Phase 5 - HTTP / dev-tooling parity

- [ ] 40 compression/ETag - build gzip + dynamic ETag php/ruby/node; 304 preserves validators; one static ETag `W/"<size>-<mtime>"`
- [ ] 42 error pages - content-negotiate JSON vs HTML (resolves the 403 split); Ruby JSON error path; 404 request_id
- [ ] 45 swagger - Node live-routes snapshot (not boot); add `/__feedback` to exclusion; Python documents 401 on secured ops
- [ ] 32 route groups - converge slash-normalization on PHP's grammar
- [ ] 46 landing page - RATIFY dev-only; unify suppression conditions (remove Ruby dead branch)
- [ ] 128 dual test port - port Node's real dual-port test to py/php/ruby; single-source Ruby base port; align PHP default
- [ ] 130 version - single-resolver convergence php+node; cross-source drift test; ADD `Tina4/<version>` User-Agent all 4
- [ ] 131 test client - route Node's TestClient through the real dispatch; preserve duplicate headers all 4
- [ ] 133 carbonah - `CARBONAH.md` GENERATED (JSON + suite counts + SCI); align the 4 harnesses' workload set + parity check

## Phase 6 - Frond

- [ ] 48/49 lexer/parser - add source positions + EOF; parser/AST stage for ruby+node
- [DEFERRED -> 3.13.100] 50 Frond compiler - the AOT compiler for Ruby + Node is a FAST-FOLLOW in 3.13.100 (owner call 2026-08-12), OUT of the .99 scope. CP-DEC-01 (all four get a compiler) still stands; 48/49 (source positions + EOF) land in .99 as the foundation. The compiler build ships in 3.13.100.
- [ ] 52 Frond filters - `|date` strftime everywhere; `|join` default ", "; `|default` keeps false
- [ ] 54 Frond tests - even/odd require a real integer (no PHP int-cast)
- [ ] 55 Frond functions - add `range()` py/ruby/node; camelCase `formToken` alias; Ruby dotted-call resolution
- [ ] 56 Frond extensibility - instance-local registration (no class-registry leak)
- [ ] 57 auto-escaping - `|tojson` \u-escape model everywhere; escaped charset `& < > " '` identical
- [ ] 58 sandboxing - denied filter RAISES (not pass-through); disable the new compilers under sandbox
- [ ] 59 template caching - bound Python's caches (256); compare mtime in prod
- [ ] 60 fragment caching - bound the store; namespace keys (no cross-block collision)

## Maintainability lens (less-code items, done WITH their feature)

These are the dead/duplicated-code removals the audit already found. They are not a separate phase -
each is deleted as part of its feature so the diff nets DOWN, not up:

- 7 remove the dead/duplicated SQL-translator code + resolve the Ruby unwiring
- 17 remove Node `_exists`, PHP `tableFilter`, dead locals
- 21 drop Python's no-op `on_delete` param (phantom API)
- 22 de-duplicate PHP's parallel imperative-relationship impl
- 28 remove the inert `seed_table(seed=)` param
- 46 remove Ruby's dead landing-suppression branch
- 126 delete the dead `render_production_error` in all four + its misleading docstring
- 130 collapse PHP's three version sources + Node's four package.json readers to one resolver each
- 133 replace the hand-maintained `CARBONAH.md` with a generator

## Follow-ups surfaced during the pass

Small, orthogonal items found while implementing a feature - fold into the named later feature; do NOT expand the current feature to chase them:
- 10 (MySQL) latent parity gap (dormant, NOT fixed): Node's MySQL adapter has NO first->last-id normalization - a hand-written multi-row `INSERT` via `db.execute` would report the FIRST id in Node. Dormant because `db.insert` loops row-at-a-time. Low-pri.
- 11/12 note (from feature 10): the same hardcoded-`id` RETURNING emulation lives in Python `mssql.py` (~126/128, `WHERE id = %s`) + `firebird.py` (~306/308, `WHERE id = ?`) - fix Python-adapter-local in features 11/12 (php/ruby/node use SCOPE_IDENTITY / native RETURNING, already column-independent).
- 9 (found, Feature-3/ADR-0044 scope - NOT fixed here): Ruby `Database#autocommit_standalone_write` issues a bare `COMMIT` on the pg-gem libpq-autocommit connection, so real PG warns `there is no transaction in progress` on EVERY standalone write (86x in write_path_contract_spec). Pre-existing; correctness unaffected (rows land); it is exactly adapter-contract invariant DBA-T06. Fixing touches the autocommit/txn model (ADR-0044, owner-sensitive) - fold into feature 3 / the ORM adapter work. CONFIRMED CROSS-ENGINE 2026-08-12: real MSSQL also warns `COMMIT TRANSACTION ... no corresponding BEGIN` on every Ruby standalone write (16x in the feature-11 gate) - same bug, both engines.
- 7 (SQL translator, partial parity - documented in 007-sql-translator.md): concat/bool/ilike were wired for MySQL (matching py/node) but the MSSQL + Firebird wiring for PHP/Ruby remains OPEN, and the MSSQL-pagination-strategy unification (Node `TOP` vs the drivers' `OFFSET/FETCH`, SQLTRANS-DEAD-DUP) is a larger cross-cutting change not bundled. The literal-safe fix itself is engine-agnostic (MySQL+PG exercise every code path); this is parity-completeness, low-pri.
- 25 (ORM cache) LATENT: the underlying cache `ttl<=0` semantics DIVERGE - Python core `Cache` treats `ttl<=0` as never-expire; PHP/Ruby/Node `QueryCache` treat `ttl=0` as immediate-expiry. Feature 25's explicit `ttl<=0` gate in `cached()` neutralises it there (never reaches `set()` with ttl<=0), but the backend divergence could bite other cache callers. Low-pri: unify with the caching features (59/72).
- 44 (file upload, UP-DEC-01 - pre-existing, NOT fixed): `tina4-php/Tina4/Request.php:296` docblock still says `content => string (base64)` but the code returns raw bytes - a false docblock governed by UP-DEC-01 (descriptor-key/base64 correction), out of feature-44 scope. Fold into UP-DEC-01.
- 44 scoping note: the repeated-field->list fix is FILES-only (repeated TEXT fields stay last-wins, matching Node + the UP-MULTIFILE-LOSS finding); repeated text-field semantics are governed separately by REQ-DEC-01 (feature 29).
- LAB GATE HARNESS: `lab-fileupload.sh` used `$HOME/rel-3.13.99`, which breaks under `sudo` (HOME=/root) - the main loop's re-run showed "MISSING clone" rc=1 until run with `sudo HOME=/home/andre`. Gate scripts MUST hardcode `BASE=/home/andre/rel-3.13.99`, never `$HOME` (the other gates already do).
- SKILL DRIFT - DONE 2026-08-12 (report-a-skill issue #49): removed the `render_production_error` "Production error" row from `references/subsystems.md` in all four repos' tina4-maintainer skill (the fn was deleted in feature 126). Applied right after 132 landed (had been the fix-on-discovery live-collision defer).
- 37 (CSRF): form-token TTL env var name diverges - Python reads `TINA4_TOKEN_EXPIRES_IN`, PHP/Ruby/Node read `TINA4_TOKEN_LIMIT`. Unify in a later env-uniformity pass (or with feature 64 JWT).
- 37 (CSRF): Ruby's blank-secret hard-fail also rejects writes in RS256 mode (blank `TINA4_SECRET` + `.keys/` present + `TINA4_CSRF=true`) - kept fail-closed-uniform for parity (auto-attach is new, no existing app regresses); revisit if RS256-defer is wanted.
- 57/42 (SECURITY - Frond auto-escape parity): Ruby's Frond/TwigEngine does NOT auto-escape `{{ }}` by default, while Python and PHP Frond DO (you opt out via `{% autoescape false %}`). This surfaced a reflected XSS in Ruby's 404 error page (raw path reflected) - patched acutely in 127 (`ab06e5c`, escape at `handle_404`). ROOT fix is feature 57 (auto-escaping): make Ruby auto-escape by default at parity + confirm Node's default. Feature 42 (error pages) must also verify no 404/403/500 template reflects raw request data unescaped in any framework. Assessed 2026-08-11: Python/PHP/Node error templates show NO raw-path reflection, so this is a Ruby-centric parity gap, NOT a live 4-way vuln.
- 17 (ORM base) AUDIT DRIFT - the 017 feature doc / ORM17-DEC-01 named `_exists` and `tableFilter` as vestigial in Node AND PHP, but the code disagrees (code wins): PHP `_exists` IS read in `save()` (finder-loaded insert-vs-update shortcut), Node `tableFilter` IS a declared static read (findById/all/where/count/withTrashed + AutoCrud), and Ruby had NO vestigial state at all (`@persisted`, no `tableFilter`). Only the truly-dead subset was removed per language. ACTION: correct `features/017-orm-base-class.md` + the ORM17-DEC-01 wording so the decision reflects the real per-language state (currently overstates what is removable). Doc-only; the code is already correct + lab-green.
- 18 (ORM fields) NODE FK TEXT AFFINITY (flagged by feature 21, NOT widened): Node's `foreignKey` column has SQLite TEXT affinity, so a FK value round-trips as `"1.0"` in `toDict`/serialization output. Feature 21 fixed relationship TRAVERSAL engine-independently (a `_joinKey` normalizer), but the cosmetic serialized `"1.0"` is a field-coercion concern spanning five adapters + the ormfields contract. Fold into feature 18 (or a field-coercion follow-up). Cosmetic, low-pri.
- 22 (imperative relationships) IMPERATIVE has_many SOFT-DELETE DIVERGES (flagged, NOT in IMPREL-DEC-02 scope): PHP filters soft-deleted rows via `where()`, Python/Node run raw (no `is_deleted = 0`). The feature-22 fixture avoids soft-deleted rows so it is untested either way. Fold into the soft-delete/traversal parity work (feature 20/21 neighbourhood) - align imperative has_many to the same soft-delete exclusion in all four. Low-pri.
- 24 (pagination) NODE `?per_page` INPUT ALIAS GAP (flagged by feature 24, NOT in PAGE-DEC-01 scope): Node's `parseQueryString()` reads only `?limit`/`?page`/`?offset` as inputs; Python/PHP/Ruby ALSO accept `?per_page=` as a `limit` fallback. So a client using `?per_page=` on the Node AutoCrud list gets the default page size, not the requested one. Distinct parity gap; fold into a query-input-parity pass. Low-pri.
- MAIL FLAGS UNDOCUMENTED (doc gap surfaced with MAIL-REDIRECT): neither `TINA4_MAIL_CAPTURE` nor the new `TINA4_MAIL_REDIRECT_TO` is documented anywhere user-facing (only in framework source + tests - grep-confirmed across all four CLAUDE.md + docs/). Developers asked for exactly this (staging mail control), so add a short docs-site messenger/staging-mail page documenting both flags (capture-to-DevMailbox + redirect-to-list, precedence, comma-list parse). Docs-only, post-merge. Low-pri.
- WRITE-PATH: Node `Database.insert()` facade NOT fail-loud on SQLite (QUEUED - dispatch right after feature 15 frees tina4-nodejs; owner-flagged 2026-08-12, ex feature-28 task_74550827). `SQLiteAdapter.insert()` (adapters/sqlite.ts ~177) returns `{success:false, error}` on a constraint violation instead of THROWING, and the `Database.insert()` facade (database.ts) passes it through unchecked - so a NOT NULL/UNIQUE violation is a SILENT success on SQLite only, violating the documented `execute()`/`fetch()`-style fail-loud contract (async adapters PG/MSSQL/Firebird throw; the new `adapterInsert()` in database.ts has the correct `if (result?.success === false) throw` pattern to MIRROR). Fix `Database.insert()` itself. NOT a one-liner: (1) CHARACTERISE `BaseModel.save()`'s call site first - save() may rely on the falsy return; making insert() throw could break it (needs try/catch->false). (2) PARITY: check whether Python/PHP/Ruby facade insert() already fails loud on their SQLite (this may be Node-only). (3) real no-mock test - a real SQLite NOT NULL/UNIQUE violation through `Database.insert()` asserts it THROWS - + re-verify the write_path_contract fixture. Feature-3/9 write-path territory.
- 18 (ORM fields) FK auto-name is naive `+s` in Python, PHP AND Node too (NOT just Ruby): `Category`->`categorys`. FIELD-FK-RELATEDNAME-RUBY scoped the fix to Ruby (done, smart `Tina4.pluralize`); the same defect is confirmed in the other three. Fold the cross-language fix into the relationships work (feature 21) - each has-many auto-`related_name` should use its language's smart pluralizer. Cosmetic accessor-name defect, low-pri.
- 18 (ORM fields) FIREBIRD AUTO-INCREMENT NOT WIRED (data defect, pre-existing, feature-15/migrations scope): ORM `create_table` on Firebird does NOT create the generator/trigger in Python/PHP/Ruby (only Node does), so a `save()` on an auto-increment model yields a NULL PK on Firebird. Not introduced by 18; the provider write-path (feature 12) works via `db.insert` RETURNING. Fold the ORM-DDL generator/trigger wiring into feature 15 (migrations/DDL). Medium-pri (Firebird-only, ORM create_table path).
- 18 (ORM fields) MYSQL NATURAL-KEY DOUBLE-TXN (pre-existing, feature-17-adjacent): ORM natural-key `save()` opens a second transaction on MySQL ("Transaction already in progress"). Feature 17 is closed; fold into the next ORM-write pass (or a dedicated fix) rather than reopening 17. Low-pri (degrades to a warning; row still lands).
- 18 (ORM fields) RUBY driver quirks (benign, Ruby-only): Ruby's MSSQL `db.commit` after an auto-committed DDL logs a benign "no corresponding BEGIN" warning (this is the SAME DBA-T06 bare-COMMIT-on-autocommit already tracked above for PG+MSSQL); Ruby's `fb` driver will not bind a bare `true` to an INTEGER column (needs `1`). Both handled in-test (the engine-DDL round-trip drives `db.insert`/`db.fetch_one`, the proven provider write-path); no runtime bug.

## Breaking changes (for the 3.13.99 changelog)

Compiled as features land; each is a security/parity fix, not an accidental break. Migration detail in each feature doc.
- 37 (CSRF): 403 body unified to `{error, code, message, status}` (Ruby/Node changed from `{error: "CSRF_INVALID"}`); `TINA4_CSRF=true` now ATTACHES the middleware (was inert); a blank `TINA4_SECRET` now fails closed (no forgeable public-default token).
- 127 (dev-admin): dev server binds `127.0.0.1` by default (set `TINA4_HOST=0.0.0.0` to expose); cross-origin `/__dev` mutations refused; `.env` never served via the file endpoints.
- 41 (static): a symlink whose realpath escapes the public dir is refused; dotfiles (`.env`, `.git`) return 404; Ruby drops the `src/assets`/`assets` search dirs; PHP app-dir order is `public` before `src/public`; `TINA4_PUBLIC_DIR` now honoured in Ruby + Node.
- 43 (request-id): a hostile inbound `X-Request-ID` (CRLF / illegal charset / over-long) is now sanitized to a fresh id instead of echoed raw (response-header + log-injection fix); a well-formed id passes through unchanged. Storage moved to request-scoped (contextvars / thread-local / AsyncLocalStorage) - internal, no API change.
- 53 (Frond tags): `{% include %}`/`{% extends %}`/`{% import %}` are now confined to the templates dir - a template that referenced a path outside it (`..`, absolute, or a symlink escaping the dir) now raises instead of reading the file. Legit in-dir includes unaffected.
- 14 (Mongo SQL): an unparseable/unsupported WHERE now RAISES instead of silently matching all documents (was a mass-delete/update data-loss footgun); a DELETE/UPDATE with no WHERE is rejected. Code relying on the old silent match-all must add an explicit WHERE (or use `truncate()`).
- 36 (security headers): security headers now emit by DEFAULT (secure-by-default) in all four - notably `Content-Security-Policy: default-src 'self'`, which blocks inline scripts and third-party CDNs. Relax it with `TINA4_CSP`. HSTS emits only on HTTPS when `TINA4_HSTS` is set.
- 129 (port takeover): `tina4 serve` on a busy port no longer kills whatever holds it - it reclaims only a port held by an identifiable Tina4 dev server (per-port PID file), refuses on a foreign holder, is dev-gated, and honours `TINA4_NO_TAKEOVER`/`--no-kill`. Anyone relying on serve force-killing an arbitrary process must free the port themselves.
- 126 (debug overlay): the dead `render_production_error`/`renderProductionError` public function is removed in all four (nothing invoked it; the real prod 500 renders `500.twig`) - a stale caller must use the template path. The dev overlay now redacts `Authorization`/`Cookie`/`Set-Cookie` + secret body fields and caps the rendered stack at 50 frames.
- 44 (file upload): a repeated multipart FILE field name now yields a LIST (was last-wins silent drop) - code reading `request.files['x']` for a single upload should handle a list when multiple files are sent under one name. The new safe-save helper rejects `..`/absolute filenames. PHP/Ruby now 413 an over-limit upload mid-stream (per-chunk), not after buffering the whole body.
- 47 (background tasks): PHP `App::background()` now returns a `Tina4\BackgroundTask` handle instead of `$this` (was fluent) - split a chained `->background(a)->background(b)` into two calls. Node `handle.stop()` now returns a boolean (was void).
- 16 (next-id): PHP's MongoDB next-id now RAISES on error instead of silently returning `1` (which produced duplicate ids) - handle the exception where you call it. The generic next-id relies on an atomic `UPDATE ... RETURNING` on a sequence row (internal; concurrent callers no longer collide).
- 14b (Mongo truncate): `truncate()` on a Mongo collection now actually EMPTIES it in Python/Ruby/Node (was a silent no-op that left the collection full while reporting success; PHP was already correct). A caller that relied on the old no-op now sees the collection cleared - the correct behaviour.
- 11 (MSSQL): Ruby's MSSQL adapter now RAISES on a genuinely unbindable parameter type instead of emitting it as a bareword (a caller relying on the old stringify will now error - it prevented an injection/corruption). MSSQL pagination converged on `OFFSET/FETCH` in all four (Node no longer uses `TOP` for page one); binary params in PHP/Ruby travel as `0x` literals.
- 12 (Firebird): `db.insert().last_id` / `db.update|delete().affected_rows` now return correct values on Firebird in all four (were `None`/`0` - a silent write-contract break). `node-firebird` moved from a devDependency to an optionalDependency of `@tina4/orm` (install it only when you use Firebird on Node).
- 132 (inline testing): the inline `@tests` DESCRIPTOR builders are renamed `assert_*` -> `expect_*` (Python `expect_equal`/`expect_raises`/`expect_true`/`expect_false`, PHP `Testing::expectEqual`/..., Ruby `Tina4::Testing.expect_equal`/..., Node `expectEqual`/...). Code using the descriptor surface must rename its calls; the xUnit IMMEDIATE `assert_*` (`tina4_python.test` / `Tina4Test` / Ruby `TestContext`) is unchanged. `tina4 <lang> test` now DISCOVERS + RUNS the inline surface with a real exit code, so a previously-"green" run that executed zero of a developer's inline tests may now actually run (and possibly fail) them. PHP `Testing::discover()` now scans only an EXPLICIT tests directory and parses `@tests` args as literals (no `eval`): a `@tests` docblock in a source file OUTSIDE the tests dir, or one whose argument is not a literal, is no longer discovered/executed.
- 18 (ORM fields): Ruby `decimal_field` now emits a real `DECIMAL(p,s)` column (was `REAL`, silently dropping precision/scale) - generated DDL changes; existing REAL columns are unaffected until re-created. PHP's `datetime` name-inference is anchored, so a column whose name only SUBSTRING-matched (e.g. `runtime`, `downtime`, `updated_by`) is no longer typed datetime - declare an explicit `\DateTime` property (or an `*_at`/`*_date`/`*_time` name) to keep datetime. Ruby foreign-key auto `related_name` is now smart-pluralized (`Category` -> `categories`, was `categorys`) - code using the misspelled accessor must update. PHP callable/closure field defaults + the per-language `decimal` declaration are additive.
- 19 (validation): Ruby ORM `validate()` now ENFORCES length/type/format on save (was null-only) - a Ruby model that previously saved an over-length or wrong-format value now returns `false` from `save()` and writes nothing. Node AutoCrud PUT now validates the body (was create-only) - an update with type/length/pattern/required-violating data now gets a 422 (and the `isUpdate` partial-update mode no longer requires unrelated fields). The two validators now speak ONE canonical message vocabulary - some error strings change: PHP ORM drops the colon (`"name: is required"` -> `"name is required"`), Node ORM regex message becomes `"does not match the required format"`, Python/Ruby `in_list` renders compact JSON (`must be one of ["a","b","c"]`). All correctness/parity fixes; a client keying on the OLD wording must update.
- 20 (soft delete): `create_table()` now injects the soft-delete column for a `soft_delete = true` model (Python/PHP/Node `is_deleted INTEGER DEFAULT 0`; Ruby the configured `soft_delete_field`) - generated DDL changes for soft-delete models (additive column, default 0). A model that previously declared `is_deleted` itself is unaffected.
- 21 (relationships): soft-deleted children are no longer returned through relationship traversal (lazy or eager) in any language - consistent with the finders' default exclusion (feature 20). Python's phantom `on_delete=` FK param is removed and now RAISES a clear "read-side-only" error instead of silently doing nothing. Node declarative relationships now function and lazy-load (additive - previously non-functional). Relationships remain READ-SIDE-ONLY: no DB FK/`ON DELETE` DDL is emitted; deleting a parent does not cascade (enforce integrity in DDL/migrations).
- 22 (imperative relationships): Node `toDict` now includes an imperatively-loaded relation (was silently omitted when the table name differed from the lowercased model name). The imperative `has_many` default row cap changes from a silent 100 to the whole set (Python/PHP/Node) - a caller relying on the old 100-row truncation now gets every row. Ruby unchanged (per-language idiom, IMPREL-DEC-01).
- 24 (pagination): a REST list `?page` below 1 now clamps to page 1 (a non-negative offset) in Python/Ruby/Node - previously `page=0` handed a NEGATIVE offset to the driver, a 500 on PostgreSQL and silently-wrong on SQLite (PHP already clamped). An oversized `?limit`/`?per_page` is now capped at MAX_PER_PAGE=100 (the shared DEFAULT_ROW_CAP) in all four - a client can no longer request the whole table in one page. Both correctness/resource fixes.
- 26 (instance loading): Python no longer RE-ENFORCES write-path business constraints (required/length/range/choices) when HYDRATING a row - a stored row that violates a constraint (or one tightened later) now loads instead of raising out of `select()` and aborting the whole page (robustness fix; write-path validation is unchanged - a bad save is still rejected). Ruby `load()` now JSON-coerces json columns (was returning a raw string via that path) and its signature is `load(filter, params, include)` (was `load(arg, params)`, no include) - a caller relying on the old positional shape / raw-string json must update. Node `toDict()`/`toJson()` now emit a `Log.warning` when a declared relation is omitted because it was not eager-loaded (was silent).
- 27 (AutoCrud): an invalid create OR update now returns **422** with the field errors (PHP + Ruby previously returned HTTP 500; Python previously 400) - a client input error no longer reads as a server error. The write body is now allow-listed at the AutoCrud handler: `is_deleted` is never client-writable and a client-supplied PK is stripped on create AND on update (closes a mass-assignment hole + an IDOR-shaped PK-redirect where a PUT body PK could steer the write to a different row than the URL). A client relying on setting `is_deleted`/the PK via the CRUD body, or on the old 500/400 status, must update.
- 28 (seeder): PHP `seed_table` now routes through the parameterized adapter insert (was MySQL/SQLite backticks) - the generated SQL changes and the dev-admin seed now WORKS on PostgreSQL/MSSQL/Firebird (was broken). `seed_table(seed=)` is REMOVED (raises on a non-default value; Ruby drops the kwarg) - determinism now requires the caller to seed their own `FakeData`. Ruby `FakeData#boolean` returns a native `true`/`false` (was `0`/`1`), and Ruby `seed_orm`'s idempotency skip is now opt-in via `idempotent:` (was unconditional - it silently returned seeded=0 when the table already held >= count rows). All correctness/portability fixes.
- 29 (request model) - THE breaking feature: `request.params` is now ROUTE-PARAMS-ONLY in all four. Python previously merged query+route into `params`; Ruby merged query+BODY+route - so `request.params["id"]` could return a CLIENT `?id=`/body value that shadowed the route param (a param-pollution surface). Now client input lives ONLY in `request.query` / `request.body`; `request.params` is the route params. A handler/middleware relying on the old merge must read `query`/`body` explicitly. Ruby's route-param accessor is renamed `path_params` -> `params` (NO alias). The malformed-JSON-body result is pinned to the RAW STRING (Ruby was `{}`) and the empty-body sentinel to each language's native null (Ruby was `{}`). Python's Request gains a mutable `user` field. Header `header()` dash-normalization is now case-fold-only in all four (Python dropped `-`->`_`, Ruby dropped `_`->`-`). REQ-DEC-02 sub-values were DERIVED from the ledger's own tie-breaks (not owner-spelled) - confirm at the merge gate.

## Cross-cutting fallout fixed during the pass (feature 19 message-vocabulary change)

Feature 19 unified the validation message vocabulary, but its per-feature gate ran ONLY the validation suite - so two suites OUTSIDE that gate kept asserting the OLD wording and shipped red until feature 20's broader run surfaced them. Both fixed as fix-on-discovery, mutation-real, verified:
- Node `test/orm.test.ts:270` asserted the pattern message contained `"pattern"`; the unified message is `"does not match the required format"`. Changed the assertion to `"format"`. Fix `ceeea94`; re-verified 88/0 on the lab.
- Ruby `spec/orm_footguns_doc_spec.rb:87` asserted `get_error` contained `"null"` (the old DB NOT-NULL error); feature 19's enriched Ruby `validate()` now catches the missing-required field FIRST with `"name is required"`. Changed the assertion to `"required"`. Fix `5c3e289`; re-verified 20/0 isolated at seeds 1/4242/9999 (DETERMINISTIC - it had been misreported as an order-dependent seed flake during feature 20; isolation proved it a real regression). LESSON (banked to the maintainer skill): a change to a SHARED contract must run the broader subsystem suites, not just its own fixture; and a "flake" must be PROVEN by isolating + varying the seed before it is accepted as one.

## Close

- [ ] All ~52 features lab-green in all four (feature 50 Frond compiler DEFERRED to 3.13.100 fast-follow); 1-133 green for dev
- [ ] Merge feature/release3.13.99 -> v3; tag 3.13.99; update release notes + the book
